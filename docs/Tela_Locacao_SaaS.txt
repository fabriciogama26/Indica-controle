Tela: Locacao SaaS

Visao geral
-----------
- Tela placeholder para o modulo de locacao no dashboard.
- Disponibiliza o atalho de navegacao da secao `Operacao`.
- Ainda nao possui formulario funcional nem persistencia.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/locacao/page.tsx`
- Componente de base visual: `src/components/ui/ModulePlaceholder.tsx`
- Navegacao e guard de acesso: `src/components/layout/AppShell.tsx`
- Guard de rota por permissao: `src/lib/auth/authorization.ts`

Cadastro
--------
- Nao se aplica nesta etapa.

Filtros, auto-complete e listagem
---------------------------------
- Nao se aplica nesta etapa.

Boas praticas
-------------
- Manter `locacao` como `pageKey` explicito na matriz de permissao.
- Evitar regra de negocio local antes da definicao do contrato backend.
- Conservar a rota protegida no shell por sessao + permissao.

Cancelamento e saldo
--------------------
- Nao se aplica nesta etapa.

Atalhos
-------
- Rota ativa: `/locacao`.
- Exibida na secao `Operacao` da sidebar.

Atualizacao 2026-03
-------------------
- Arquivo `src/app/(dashboard)/locacao/page.tsx` criado.
- `AppShell` atualizado para expor o item `Locacao` na navegacao lateral.
- `authorization.ts` atualizado para mapear `/locacao` para `pageKey = locacao`.
- `PermissionsPageView` atualizado para incluir o toggle de acesso da tela `Locacao`.
- Antes: a rota nao existia.
- Depois: rota placeholder existente e protegida por permissao de pagina.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/app/(dashboard)/locacao/page.tsx`
  - Responsavel por: montar o placeholder da rota `Locacao`.
- Caminho: `src/components/layout/AppShell.tsx`
  - Responsavel por: renderizar o atalho lateral de `Locacao`.

Constantes e configuracoes
- Caminho: `src/components/layout/AppShell.tsx`
  - Responsavel por: entrada da rota em `menuSections` e titulo em `titleMap`.
- Caminho: `src/lib/auth/authorization.ts`
  - Responsavel por: mapeamento de rota em `ROUTE_PAGE_KEYS` e fallback de acesso por role.
- Caminho: `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - Responsavel por: `permissionCatalog` para liberar/bloquear a rota por usuario.

Funcoes utilitarias relacionadas
- Caminho: `src/hooks/useAuth.ts`
  - Responsavel por: sessao autenticada consumida no shell.

Servicos e integracoes externas
- Nao se aplica nesta etapa (sem chamada de API nesta tela).

Ajuda contextual
- Estado atual: tela em placeholder com proximo passo de detalhar regras de locacao.
