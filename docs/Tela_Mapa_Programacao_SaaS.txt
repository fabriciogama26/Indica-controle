Tela: Mapa de Programacao SaaS

Visao geral
-----------
- Tela operacional read-only para controle de carteira, acompanhamento de Estado Trabalho e disponibilidade de equipes.
- A tela consolida uma linha por obra/projeto, usando a programacao mais recente como referencia operacional.
- A tela classifica os cards pelo ultimo Estado Trabalho valido da obra.
- A tela indica obras nunca programadas, obras sem Estado Trabalho, obras pendentes/parciais e obras com ultima programacao cancelada ou adiada.
- A tela indica equipes ativas sem programacao quando o periodo e informado.
- A tela exibe o bloco `Prazos das Obras`, consolidado por SOB e data limite.
- A tela exibe rastreio das transferencias de equipe entre programacoes.
- Rota: `/mapa-programacao`.
- Page key: `mapa-programacao`.
- Acao server-side: `read`.

Arquitetura
-----------
- Page:
  - `src/app/(dashboard)/mapa-programacao/page.tsx`
- Endpoint:
  - `src/app/api/mapa-programacao/route.ts`
- View:
  - `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.tsx`
  - `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.module.css`
  - `src/modules/dashboard/mapa-programacao/index.ts`
- Navegacao e autorizacao:
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
- Permissao em banco:
  - `supabase/migrations/244_create_programming_map_page.sql`

Cadastro
--------
- Nao ha cadastro, edicao, cancelamento ou exclusao nesta tela.
- A tela e somente consulta/exportacao.
- Acoes exibidas nos cards direcionam para telas existentes (`Programacao` e `Projetos`).

Filtros, auto-complete e listagem
---------------------------------
- Filtros:
  - data inicial opcional
  - data final opcional
  - centro de servico
  - busca por obra/projeto/status
  - busca por equipe
- Regra do periodo:
  - se data inicial e data final estiverem vazias, a tela analisa a carteira inteira.
  - se uma data for preenchida, a outra tambem e obrigatoria.
  - os cards de carteira nao mudam com o periodo.
  - equipes sem programacao so sao calculadas quando o periodo e informado.
- Atalhos de periodo:
  - sem periodo
  - hoje
  - semana
  - 15 dias
  - 30 dias
- Cards:
  - Carteira valida
  - Concluidas
  - Para reprogramar
  - Revisao de etapas
  - Interrompidas concluidas
  - Pendentes
  - Parcial planejada
  - Parciais
  - Beneficio atingido
  - Canceladas/adiadas
  - Sem Estado Trabalho vencido
  - Nunca programadas
- Modal:
  - aberto ao clicar em um card.
  - mostra obras daquela situacao em tabela paginada.
  - ao clicar em uma linha, abre o detalhe da obra em formato de card.
  - a tabela exibe SOB, ultima data, status, Estado Trabalho, equipe, encarregado, quantidade de programacoes, quantidade de etapas, revisao de etapas e dias desde a ultima programacao.
  - o detalhe exibe SOB, projeto, ultima data, equipe, encarregado, Estado Trabalho, quantidade de programacoes, quantidade de etapas, ultima etapa, revisao de etapas, dias desde a ultima programacao e motivo/observacao quando existir.
  - no detalhe aberto pela tabela, o titulo usa `ID: {uuid}` da obra.
- Secoes:
  - Prazos das Obras com cards-resumo, carrossel por SOB, modal `Ver todos` e exportacao CSV.
  - Revisao de etapas em tabela propria, destacando obras com etapa cancelada/adiada e etapa ativa posterior.
  - Interrompidas com Estado concluido em tabela propria, destacando divergencia entre status operacional e Estado Trabalho.
  - Rastreio de transferencias, exibindo equipe, origem, destino, datas, etapas e motivo.
  - Obras prioritarias em tabela paginada
  - Obras nunca programadas em tabela paginada
  - Equipes sem programacao
- Exportacao:
  - CSV dos prazos das obras na janela selecionada.
  - CSV das obras do card aberto.
  - CSV da tabela dedicada de `Revisao de etapas`.
  - CSV da tabela dedicada de `Interrompidas com Estado concluido`.
  - CSV do rastreio de transferencias.
  - CSV de equipes sem programacao quando houver periodo.
- Prazos das Obras:
  - Calculado a partir da `Carteira valida` ja consolidada no Mapa.
  - Respeita os filtros locais de `Centro de servico` e busca de obra/projeto/status.
  - Ignora obras concluidas.
  - Considera somente obras com `Data limite` valida.
  - Exibe resumo por `Vence hoje`, `Vence em breve`, `Vencida` e `No prazo`.
  - Permite alternar janelas de `15 dias`, `30 dias`, `60 dias` e `90 dias`.
  - O carrossel mostra cards por SOB, com `Data limite` e `Status do prazo`.
  - O modal `Ver todos` exibe `SOB`, `Centro de servico`, `Prioridade`, `Tipo de obra`, `Data limite`, `Data Programacao`, `Motivo`, `Estado Trabalho`, `Status do prazo`, `Dias para vencimento` e `Faixa`.

Boas praticas
-------------
- O frontend nao envia `tenant_id`, role ou usuario como fonte de verdade.
- O endpoint resolve o tenant pela sessao autenticada.
- A rota exige `mapa-programacao/read` antes de consultar dados de negocio.
- Obras de teste, retiradas da carteira, inativas e emergenciais nao entram na carteira valida.
- A mesma obra aparece uma unica vez nos cards, mesmo quando possui varias programacoes.
- A referencia operacional da obra considera a ultima programacao por `execution_date`, com desempate por `updated_at`, `created_at` e `id`.
- A classificacao dos cards de Estado Trabalho usa o ultimo Estado Trabalho valido encontrado no historico da obra.
- A obra nao e considerada pendente por uma programacao antiga quando existe Estado Trabalho valido mais recente concluido.
- A obra tambem nao e reaberta por uma programacao `CANCELADA` ou `ADIADA` posterior sem Estado Trabalho quando o ultimo Estado Trabalho valido anterior ja era concluido.
- `Estado Trabalho` concluido segue a regra tecnica: `CONCLUIDO`, `COMPLETO` ou codigo iniciado por `CONCLUIDO`.
- `CANCELADA` e `ADIADA` sao tratadas como programacoes interrompidas no mapa quando forem a ultima programacao da obra e nao houver continuidade futura ativa.
- Etapa `CANCELADA` ou `ADIADA` preserva o numero historico da etapa; se houver etapa ativa posterior no mesmo projeto, a obra entra em `Revisao de etapas`.
- `Revisao de etapas` nao renumera etapas e nao altera dados; apenas sinaliza que as etapas futuras dependem de revisao operacional.
- Programacao `ADIADA` ou `CANCELADA` deve ficar com `Estado Trabalho` em branco apos a migration 284.
- Programacao `ADIADA` ou `CANCELADA` com `Estado Trabalho = CONCLUIDO` e tratada como divergencia legada/importacao administrativa e aparece em `Interrompidas com Estado concluido`.
- Programacao `TRANSFERIDA` e status interno de linha; nao entra como programacao ativa e nao entra no card `Canceladas/adiadas`.
- O rastreio de transferencia vem de `project_programming_history` com `action_type = TRANSFER_TEAM` e limite de 100 eventos recentes.
- Obras com divergencia `ADIADA/CANCELADA + CONCLUIDO` nao entram como concluidas no Mapa ate saneamento.
- Programacao futura ativa (`PROGRAMADA` ou `REPROGRAMADA`) evita classificar a obra como `Para reprogramar`, salvo quando a ultima situacao estiver interrompida.
- `Sem Estado Trabalho` considera a ultima programacao sem apontamento quando a data ja passou ou quando a data esta invalida/ausente.
- A quantidade de programacoes conta linhas/registros de `project_programming`.
- A quantidade de etapas conta etapas distintas por `etapa_number`, `etapa_unica` e `etapa_final`.
- Equipes sem programacao consideram apenas equipes ativas sem registros `PROGRAMADA` ou `REPROGRAMADA` no periodo.
- Falhas da tela sao registradas com `useErrorLogger("mapa_programacao")`.

Atalhos
-------
- Rota: `/mapa-programacao`
- Endpoint: `GET /api/mapa-programacao`
- Menu: `Operacao > Mapa de Programacao`

Atualizacao 2026-06
-------------------
- Tela criada para atender o controle de carteira com a regra "Nunca foi programada".
- Endpoint criado com autorizacao server-side `mapa-programacao/read`.
- Menu lateral passou a exibir `Mapa de Programacao` na secao `Operacao`.
- Catalogo de permissoes passou a incluir `mapa-programacao`.
- Migration `244_create_programming_map_page.sql` cadastra a pagina e faz backfill de permissoes ausentes.
- Mapa evoluido para consolidar a carteira por obra usando a ultima programacao e o Estado Trabalho.
- Data inicial/data final deixaram de ser obrigatorias para a analise de obras.
- `ADIADA` passou a ter o mesmo tratamento operacional de `CANCELADA` no mapa.
- Cards passaram a abrir modal com as obras da situacao.
- Obras prioritarias passaram a ser exibidas em tabela paginada, com detalhe em modal ao clicar na linha.
- Obras nunca programadas voltaram a ter bloco separado em tabela paginada.
- Modal aberto pelos cards tambem passou a exibir tabela paginada, com detalhe por linha.
- Cards de carteira passaram a ignorar o filtro de periodo; o periodo afeta somente equipes sem programacao.
- Programacao `CANCELADA`/`ADIADA` posterior sem Estado Trabalho deixou de reabrir obra cujo ultimo Estado Trabalho valido e concluido.
- Modal de detalhe por linha passou a exibir `ID: {uuid}` da obra no topo.
- Bloco `Prazos das Obras` foi movido da tela `Programacao Simples` para o `Mapa de Programacao`.
- `Prazos das Obras` no Mapa usa a carteira consolidada, respeita filtros locais de centro/busca, exclui obras concluidas e mantem modal/exportacao CSV.
- Card `Revisao de etapas` criado para localizar obra com etapa cancelada/adiada e etapa ativa posterior.
- Tabelas e cards de detalhe passaram a destacar a revisao de etapas, exibindo a etapa interrompida e a proxima etapa ativa posterior.
- Etapas historicas canceladas/adiadas nao sao renumeradas; a tela apenas marca as etapas futuras como dependentes de revisao.
- `Revisao de etapas` passou a ter uma tabela dedicada na tela principal, com exportacao CSV e destaque visual.
- A coluna `Revisao` ficou restrita a tabela dedicada e ao modal do card `Revisao de etapas`, evitando misturar esse alerta nas demais tabelas.
- Card e tabela `Interrompidas com Estado concluido` criados para expor casos legados ou divergentes em que status operacional e Estado Trabalho entram em conflito; apos a migration 284, o esperado e ficar zerado.
- Comportamento antes/depois:
  - Antes: a tela mostrava principalmente obras nunca programadas e equipes sem agenda.
  - Depois: a tela mostra carteira valida, concluidas, nao concluidas, parciais, pendentes, canceladas/adiadas, sem Estado Trabalho e nunca programadas.
  - Antes: o bloco `Prazos das Obras` ficava acima do cadastro da Programacao Simples.
  - Depois: o bloco `Prazos das Obras` fica no Mapa de Programacao, junto da visao de carteira.
  - Antes: a avaliacao de obras dependia de listas extensas.
  - Depois: a visualizacao principal usa cards e modal.
  - Antes: o periodo era obrigatorio na carga inicial.
  - Depois: periodo e opcional para obras e obrigatorio somente para analisar equipes sem programacao.
  - Antes: uma etapa intermediaria cancelada/adiada com etapa posterior ativa podia ficar misturada em `Para reprogramar`.
  - Depois: a obra aparece em `Revisao de etapas`, com destaque visual na tabela e no card.
  - Antes: a revisao aparecia apenas como coluna dentro de tabelas gerais.
  - Depois: a revisao possui tabela propria para analise operacional.
  - Antes: `ADIADA`/`CANCELADA` com Estado Trabalho `CONCLUIDO` podia aparecer como concluida.
  - Depois: o caso aparece como inconsistencia em tabela dedicada; apos a migration 284, a regra de banco limpa Estado Trabalho de interrompidas.
- Impacto multi-tenant:
  - Todas as leituras usam o `tenant_id` autenticado.
  - A mudanca nao altera RLS, grants ou schema.
  - A rota continua protegida por `mapa-programacao/read`.

Atualizacao 2026-07-02
----------------------
- Rastreio de transferencias adicionado ao Mapa de Programacao.
- O endpoint `GET /api/mapa-programacao` carrega `project_programming_history` com `action_type = TRANSFER_TEAM`, ordenado por `created_at desc` e limitado a 100 registros.
- A tela exibe tabela paginada com equipe, origem, destino, datas, etapas e motivo.
- O CSV `mapa_programacao_transferencias_YYYY-MM-DD.csv` exporta o mesmo rastreio filtrado.
- Comportamento antes/depois:
  - Antes: a transferencia ficaria visivel apenas indiretamente nas linhas/historico da Programacao.
  - Depois: o Mapa mostra explicitamente quem mudou, de onde saiu e para onde foi.
- Impacto multi-tenant:
  - O endpoint usa o `tenant_id` da sessao autenticada.
  - A consulta de historico tem `.limit(100)` e nao faz leitura global entre tenants.
- Arquivos alterados:
  - `src/app/api/mapa-programacao/route.ts`
  - `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.tsx`
  - `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.module.css`
  - `docs/Tela_Mapa_Programacao_SaaS.txt`

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/app/(dashboard)/mapa-programacao/page.tsx`
  - Responsavel por: expor a rota `/mapa-programacao`.
- Caminho: `src/app/api/mapa-programacao/route.ts`
  - Responsavel por: autenticar, autorizar, consultar carteira/programacoes/equipes e montar o payload consolidado por obra.
- Caminho: `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.tsx`
  - Responsavel por: renderizar filtros, cards, painel de prazos, modal de prazos, modal de obras, obras prioritarias, equipes sem programacao, exportacoes e mensagens da tela.
- Caminho: `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.module.css`
  - Responsavel por: estilos dos cards, modal, prioridades, filtros e listas.
- Caminho: `src/components/layout/AppShell.tsx`
  - Responsavel por: exibir o item de menu e titulo/subtitulo da rota.
- Caminho: `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - Responsavel por: incluir a tela na matriz visual de permissoes.
- Caminho: `src/lib/auth/authorization.ts`
  - Responsavel por: resolver a rota `/mapa-programacao` para `page_key = mapa-programacao` e aplicar fallback por role.

Constantes e configuracoes
- `MAP_PROGRAMMING_PAGE_KEY`
  - Define a page key usada no guard server-side.
- `ACTIVE_PROGRAMMING_STATUSES`
  - Define status ativos de agenda (`PROGRAMADA`, `REPROGRAMADA`).
- `INTERRUPTED_PROGRAMMING_STATUSES`
  - Define status interrompidos no mapa (`CANCELADA`, `ADIADA`).
- `DEADLINE_CAROUSEL_PAGE_SIZE`
  - Define quantidade de cards SOB por pagina no carrossel de prazos.
- `DEADLINE_WINDOW_SHORT_DAYS`, `DEADLINE_WINDOW_LONG_DAYS`, `DEADLINE_WINDOW_EXTENDED_DAYS`, `DEADLINE_WINDOW_MAX_DAYS`
  - Definem as janelas de prazo de 15, 30, 60 e 90 dias.

Funcoes utilitarias relacionadas
- `fetchProjects`
  - Carrega projetos ativos da carteira e remove teste, retirado e emergencial.
- `fetchProgrammingRows`
  - Carrega o historico geral de programacao do tenant.
- `fetchWorkCompletionCatalog`
  - Resolve label de exibicao do catalogo de Estado Trabalho.
- `fetchTeams`
  - Carrega equipes ativas e inativas para resolver historico, tipo, encarregado e centro de servico.
- `fetchProgrammedTeamIds`
  - Carrega equipes programadas no periodo filtrado.
- `normalizeToken`
  - Normaliza textos tecnicos para comparar status e Estado Trabalho.
- `isCompletedWorkStatus`
  - Resolve se o Estado Trabalho e conclusivo.
- `resolvePriorityLevel`
  - Classifica a prioridade visual por dias desde a ultima programacao e ausencia de Estado Trabalho.
- `resolveStageReviewIssue`
  - Identifica etapa `CANCELADA` ou `ADIADA` com etapa ativa posterior no mesmo projeto, preservando a numeracao historica e retornando dados para o card `Revisao de etapas`.
- `resolveInterruptedCompletedIssue`
  - Identifica programacao `ADIADA` ou `CANCELADA` com Estado Trabalho conclusivo e retorna dados para o card `Interrompidas concluidas`.
- `ProjectTable`
  - Renderiza tabelas paginadas de obras prioritarias, nunca programadas e cards abertos, com coluna opcional de revisao de etapas.
- `filterProjects`
  - Aplica filtros locais de busca/centro sobre cards, tabelas e modal.
- `exportCsv`
  - Gera CSV local das obras do card e das equipes.
- `resolveDeadlineWindowDays`
  - Resolve a janela ativa do painel `Prazos das Obras`.
- `formatDeadlineRangeLabel`
  - Classifica cada prazo em vencida, ate 15 dias, 16 a 30 dias, 31 a 60 dias ou 61 a 90 dias.
- `deadlineProjects`, `deadlineSummary`, `deadlineSobCards`, `deadlineSobPages`
  - Montam os dados do painel de prazos a partir da carteira filtrada.
- `handleExportDeadlineModalCsv`
  - Exporta os prazos da janela selecionada em CSV.
- `fetchTransferHistoryRows`
  - Carrega os 100 eventos mais recentes de transferencia no historico operacional da Programacao.
- `buildTransferEvents`
  - Converte metadata `TRANSFER_TEAM` em origem/destino legiveis para a tela.
- `filteredTransferEvents`
  - Aplica filtros locais de obra, equipe e centro de servico sobre o rastreio.
- `TransferTable`
  - Renderiza a tabela paginada de transferencias.
- `exportTransferEvents`
  - Exporta o rastreio de transferencias em CSV.
- `ProgrammingDeadlinePanel` e `ProgrammingDeadlineModal`
  - Componentes reaproveitados da Programacao Simples para renderizar o painel e o modal de prazos no Mapa.

Servicos e integracoes externas
- Supabase Postgres via client server-side autenticado.
- Tabelas/views consultadas:
  - `project_with_labels`
  - `project_programming`
  - `programming_work_completion_catalog`
  - `teams`
  - `team_types`
  - `people`
  - `service_centers`
  - `project_programming_history`

Ajuda contextual
- Use os cards de Estado Trabalho para acompanhar obras cujo ultimo Estado Trabalho valido ainda nao concluiu a execucao.
- Use `Canceladas/adiadas` para localizar obras cuja ultima tentativa foi interrompida e nao teve continuidade posterior.
- Use `Sem Estado Trabalho` para corrigir inconsistencias de apontamento.
- Use `Nunca programadas` para localizar carteira que ainda nao entrou em nenhuma programacao.

Atualizacao 2026-07-04 - Modal compartilhado de exportacao:
- Arquivo alterado: `src/modules/dashboard/mapa-programacao/MapProgrammingPageView.tsx`.
- Funcoes/estado tocados: `runCsvExport`, `exportProjects`, `exportTeamsWithoutProgramming`, `exportTransferEvents`, `handleExportDeadlineModalCsv`, `isExportingCsv`, `isExportingDeadlineModal`.
- Antes: exportacoes CSV da tela nao compartilhavam o modal padrao.
- Depois: listas e modal de prazos exibem `ExportProgressModal`/`CsvExportButton` durante a geracao.
- Multi-tenant: sem mudanca em API, query, schema, RLS ou permissao.


Planejado 2026-07-29 - Fase 4 do corte para a Programacao Normalizada
----------------------------------------------------------------------
- Nada foi alterado nesta tela ainda. Esta secao registra o escopo decidido para
  quando o Mapa passar a ler o modelo normalizado
  (`programming`/`programming_team`), na Fase 4 do corte. Ate la a tela continua
  lendo `project_programming`, sem mudanca de comportamento.
- Contexto: a `programacao-simples` esta congelada em somente leitura desde o
  commit `7ecd00a`; o de/para do ID legado para a etapa nova e a migration 342
  (`programming_legacy_map`).

BLOCOS QUE PERDEM A RAZAO DE EXISTIR (remover, mas SO depois da virada — ver
"Ordem" no fim desta secao)
- `Revisao de etapas` (card, tabela dedicada, coluna, CSV, `resolveStageReviewIssue`,
  o nivel `INCONSISTENCY` de `priorityLevel` e o ramo correspondente de
  `actionRequired`). O bloco detecta etapa cancelada/adiada com etapa ativa de
  numero maior depois — sinal de numeracao furada. Ele so existe porque no modelo
  antigo a renumeracao automatica nunca foi implementada (tarefa ainda aberta na
  linha 50 do `TASKS.md`). No normalizado, `reclassify_project_programming_stages`
  renumera a cada operacao e a classificacao e derivada da posicao por data, nunca
  digitada: a condicao nao se forma.
- `Interrompidas com Estado concluido` (card, tabela, CSV,
  `resolveInterruptedCompletedIssue`). Detecta ADIADA/CANCELADA com Estado do
  Trabalho concluido. Impossivel por construcao no modelo novo: o cancelamento faz
  `work_completion_status = null` (migration 318) e o adiamento tambem (migration
  326). O que hoje e expectativa apos a migration 284 vira garantia de banco.
- `programmingCount` como coluna separada de `stageCount`. Hoje
  `programmingCount = projectRows.length` conta LINHAS legadas, ou seja
  etapas x equipes. No normalizado uma linha e uma etapa e os dois numeros passam a
  ser sempre iguais — duas colunas com o mesmo valor na tabela e no detalhe. O dado
  que tinha valor de verdade ali e "quantas equipes", que vira
  `COUNT(DISTINCT team_id)` em `programming_team` (a tela normalizada ja faz isso na
  linha-pai).
- Tratamento de `TRANSFERIDA` como status de programacao. No normalizado
  `TRANSFERIDA` e status de `programming_team`, nao de `programming` — o
  `programming_status_check` da 310 so aceita PROGRAMADA/REPROGRAMADA/ADIADA/
  CANCELADA/ANTECIPADA. A regra documentada nesta tela ("TRANSFERIDA e status
  interno de linha") deixa de se aplicar.

BUGS QUE A VIRADA CRIA SE NINGUEM MEXER (corrigir JUNTO com a troca de fonte, nao
depois)
- Card `Pendentes`: `isPendingWorkStatus` casa a substring `PENDEN` em
  `work_completion_status`. A migration 318 tirou `PENDENCIA` dos DOIS eixos (status
  e Estado do Trabalho) e transformou na flag `is_pendencia`. Sem trocar a fonte, o
  card vai a zero permanente — e zero aqui nao significa "sem pendencia", significa
  que a tela parou de olhar.
- `latestTeamName` / `latestForemanName`: hoje exibem UMA equipe porque a linha
  legada tem uma so. Etapa com 3 equipes ja hoje mostra uma e esconde duas (a
  escolha sai do desempate de `compareProgrammingRows`). No normalizado a etapa tem
  N equipes explicitas e o campo deve virar lista/contador.

O QUE FICA
- A heuristica de "ultimo Estado do Trabalho valido" e a regra de nao reabrir obra
  concluida por cancelada/adiada posterior. Simplifica (some a divergencia entre
  linhas irmas do mesmo grupo), mas continua necessaria: etapa posterior sem estado
  preenchido continua existindo.
- (nada alem do acima — ver a revisao de 2026-07-29 logo abaixo, que move o
  `Rastreio de transferencias` para a lista de remocao.)

REVISAO 2026-07-29 - `Rastreio de transferencias` passa para a lista de remocao
- Quando esta secao foi escrita, o bloco estava na lista "o que fica", com a ressalva
  de que so ganharia conteudo apos a RPC de transferir equipe do modelo novo. A
  medicao em producao mostrou que a ressalva estava errada pelo motivo oposto:
  * `project_programming` com status TRANSFERIDA: 0
  * `programming_team` com status TRANSFERIDA: 0
  * eventos `TRANSFER_TEAM` no historico legado: 0 (de 2594 linhas)
- O bloco foi entregue em 2026-07-02 (commit `8f60db1`, mesma entrega da RPC 286 e do
  modal na tela antiga) e NUNCA exibiu uma linha. A tela antiga congelou em
  2026-07-28: 26 dias com o botao visivel para todo usuario que escrevia, zero usos.
- Com isso, a implementacao de `Transferir equipe` no modelo novo saiu do plano da
  Fase 4 (ver `docs/Tela_Programacao_Normalizada_SaaS.txt`), e o bloco de rastreio
  perde o unico motivo para continuar existindo. Remover junto com `Revisao de etapas`
  e `Interrompidas com Estado concluido`.
- Diferenca de tratamento em relacao aos outros dois: estes dependem de um ciclo de
  observacao em zero antes da remocao, porque dado historico migrado pode fazer os
  detectores dispararem legitimamente. O rastreio de transferencias nao — ele nunca
  teve dado, nem legado nem novo, entao pode ser removido junto com a troca de fonte.
- O `.limit(100)` global vira ponto morto junto com o bloco; nao precisa ser revisado.

ORDEM (importante)
- Nao remover `Revisao de etapas` nem `Interrompidas com Estado concluido` na mesma
  entrega que troca a fonte de dados. Dois motivos: (1) as migrations 315/335
  copiaram `status` e `work_completion_status` como estavam, entao etapa historica
  migrada pode carregar legitimamente a combinacao "interrompida + concluido", e o
  detector vai disparar em dado antigo mesmo com a regra nova em vigor; (2) e
  exatamente durante a virada que esses dois detectores valem mais — se o
  remapeamento da Fase 5 errar, eles sao o alarme.
- Sequencia: Fase 4 aponta o Mapa para o modelo novo MANTENDO os dois blocos e
  corrigindo `programmingCount`, o card `Pendentes` e a exibicao de equipes (que sao
  bug, nao limpeza). Depois de um ciclo com os detectores em zero, remover em
  entrega propria de limpeza — remocao com evidencia de que nao e usado, como pede
  o `guias/guia_backend.md`.
