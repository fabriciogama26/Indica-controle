Tela: Controle de APR

Visao geral
-----------
- Tela de cadastro, conferencia e acompanhamento de APR por Projeto, Equipe e Data do servico.
- O ID APR e unico em toda a base do sistema.
- Os demais dados e consultas permanecem segregados por `tenant_id`.
- O visual segue o padrao da tela de Medicao: cadastro, filtros, lista paginada, acoes circulares e modal.
- Situacoes: `ATIVO`, `CONFERIDO`, `DIVERGENTE` e `CANCELADO`.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/controle-apr/page.tsx`
- View: `src/modules/dashboard/controle-apr/AprControlPageView.tsx`
- Estilo: `src/modules/dashboard/controle-apr/AprControlPageView.module.css`
- API: `src/app/api/controle-apr/route.ts`
- Banco/RLS/RPC/permissao: `supabase/migrations/226_create_apr_control_module.sql`, `supabase/migrations/350_apr_control_match_normalized_programming.sql` (match com a Programacao Normalizada)
- Navegacao/autorizacao:
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`

Cadastro
--------
- Obrigatorios: Projeto, ID APR, Data do servico e Equipe.
- Opcional: Observacao.
- Projeto aceita somente codigo exato de projeto ativo cadastrado no tenant.
- ID APR aceita numero ou texto, e normalizado para maiusculas e possui unicidade global por `upper(trim(apr_id))`.
- Data futura e bloqueada no frontend, API e RPC; a RPC usa `America/Sao_Paulo`.
- Equipe exibe `Codigo/Nome - Encarregado` e aceita somente equipe ativa do tenant.
- O encarregado e exibido em campo somente leitura.
- A Programacao e vinculada automaticamente por `tenant + Projeto + Equipe + Data do servico`, lendo o modelo normalizado (`programming`/`programming_team`, desde a migration 350) em vez da tela `programacao-simples` congelada.
- Programacao cancelada nao e vinculada; se a etapa da mesma data que estiver ATIVA (`PROGRAMADA`/`REPROGRAMADA`) e a etapa historica (`ADIADA`/`ANTECIPADA`) concorrerem, a ativa vence.
- APR cancelada nao pode ser editada.
- A edicao usa `expectedUpdatedAt`.
- Editar uma APR conferida ou divergente devolve a situacao para `ATIVO`.

Filtros, auto-complete e listagem
---------------------------------
- Filtros: Data inicial/final, Projeto, Equipe, ID APR, Status e Encarregado.
- Lista: ID APR, Projeto, Data, Equipe, Encarregado, Programacao, Situacao, Observacao, Atualizado em e Acoes.
- Paginacao server-side com 20 registros por pagina.
- `Extrair para Excel` gera `.xlsx` real com todos os registros dos filtros ativos.

Boas praticas
-------------
- Leituras usam o `tenant_id` autenticado.
- `project_apr_controls` e `project_apr_control_history` possuem RLS ativa.
- `authenticated` possui somente leitura direta; escritas usam RPC `SECURITY DEFINER` liberada ao `service_role`.
- RPCs revalidam ator, tenant, projeto, equipe, data, concorrencia e duplicidade.
- Erros de tela usam `useErrorLogger('controle_apr')`.
- Snapshots preservam projeto, equipe, encarregado e status da Programacao.

Cancelamento e saldo
--------------------
- Cancelamento exige observacao minima de 10 caracteres.
- Grava status, usuario, data/hora, motivo e historico.
- Registro cancelado permanece consultavel e exportavel.
- Nao existe impacto em saldo de estoque.

Atalhos
-------
- Rota: `/controle-apr`
- Endpoints:
  - `GET /api/controle-apr`
  - `GET /api/controle-apr?meta=1`
  - `GET /api/controle-apr?historyId=<id>`
  - `POST /api/controle-apr`
  - `PUT /api/controle-apr`
  - `PATCH /api/controle-apr`
- Acoes PATCH: `CONFERIR`, `DIVERGIR` e `CANCELAR`.

Atualizacao 2026-06
-------------------
- Antes: nao existia modulo de Controle de APR.
- Depois:
  - Cadastro com trava global de ID APR duplicado.
  - Match automatico com a Programacao do dia.
  - Conferencia, divergencia e cancelamento auditados.
  - Filtros e extracao Excel.
- Arquivos criados:
  - `supabase/migrations/226_create_apr_control_module.sql`
  - `src/app/api/controle-apr/route.ts`
  - `src/app/(dashboard)/controle-apr/page.tsx`
  - `src/modules/dashboard/controle-apr/AprControlPageView.tsx`
  - `src/modules/dashboard/controle-apr/AprControlPageView.module.css`
  - `docs/Controle_APR.txt`
- Arquivos alterados:
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - `README.md`
  - `TASKS.md`

Mapa de Codigo (funcoes, hooks e constantes)
---------------------------------------------
- Funcoes principais:
  - Caminho: `src/modules/dashboard/controle-apr/AprControlPageView.tsx`
  - `loadRecords`: lista, pagina e carrega metadados.
  - `submitForm`: cadastra ou edita.
  - `submitStatusAction`: confere, diverge ou cancela.
  - `exportExcel`: gera o arquivo `.xlsx`.
  - `handleProjectInput`: valida projeto por codigo exato.
- Constantes e configuracoes:
  - `PAGE_SIZE = 20`
  - `EXPORT_PAGE_SIZE = 500`
- Funcoes utilitarias relacionadas:
  - `findProject`
  - `buildQuery`
  - `statusLabel`
  - `statusClass`
  - `downloadExcel`
- Servicos e integracoes externas:
  - Supabase/Postgres por `resolveAuthenticatedAppUser`.
  - SheetJS/XLSX.
  - RPC `save_project_apr_control`.
  - RPC `set_project_apr_control_status`.
- Ajuda contextual:
  - O cadastro informa a chave de vinculo com a Programacao.
  - A Observacao orienta justificativa de divergencia, cancelamento ou correcao.

Atualizacao 2026-07-04 - Modal compartilhado de exportacao:
- Arquivo alterado: `src/modules/dashboard/controle-apr/AprControlPageView.tsx`.
- Funcoes/estado tocados: `exportExcel`, `isExporting`, botao `Extrair para Excel`.
- Antes: exportacao usava botao local sem o modal padrao.
- Depois: exportacao usa `CsvExportButton`, herdando `ExportProgressModal`.
- Multi-tenant: sem mudanca em API, query, schema, RLS ou permissao.

Atualizacao 2026-07-31 - Fase 5a do corte: match lendo Programacao Normalizada
-------------------------------------------------------------------------------
- `project_apr_controls.programming_id` deixa de apontar para `project_programming`
  (tela `programacao-simples`, congelada em somente leitura) e passa a apontar
  para `programming` (migration 350). A FK ja nascia composta por tenant desde a
  226; so trocou a tabela referenciada.
- `save_project_apr_control` reescrita: o match, antes uma unica condicao direta
  em `project_programming` (`project_id + team_id + execution_date`), passa a ser
  `programming` (projeto+data, `status <> CANCELADA`) com `exists` em
  `programming_team` confirmando a equipe pedida `ATIVA` naquela etapa — porque no
  modelo normalizado uma etapa pode ter N equipes (tabela filha), diferente do
  legado (uma linha por equipe). Desempate igual ao usado no Cronograma (migration
  347): status `ATIVO` (`PROGRAMADA`/`REPROGRAMADA`) vence, depois `updated_at
  desc` — adianta o cenario da migration 346 (etapa historica podendo conviver com
  uma ativa na mesma data), que ainda nao ocorre nos dados de APR mas passaria a
  valer a partir desta migration.
- `src/app/api/controle-apr/route.ts` NAO mudou: a rota sempre foi somente
  leitura sobre as colunas ja gravadas (`programming_id`,
  `programming_status_snapshot`) — quem fazia o match era so a RPC.
  `set_project_apr_control_status` tambem nao muda (nunca tocava
  `project_programming`).
- Medido em producao antes de aplicar (`scripts/audit-apr-programming-match-readonly.mjs`,
  novo, somente leitura): 172 APRs, 66 com `programming_id` preenchido (106 nunca
  vinculadas, fora do escopo), 0 orfaos em `programming_legacy_map`. Remap simples
  via o mapa concorda com o recalculo do novo match em 65/66 casos (98,5%); o
  unico caso divergente e uma etapa cancelada DEPOIS do vinculo original, onde o
  remap simples preserva corretamente o valor historico (a etapa cancelada que a
  APR de fato referenciava) e o recalculo nao acharia substituta — por isso a
  migration faz remap simples via mapa, nao recalculo. 0 casos de colisao de data
  nos dados existentes (o desempate novo nao muda nenhum vinculo hoje) e 0 etapas
  com mais de uma APR ativa vinculada (a falta de indice de unicidade, diferente
  de Medicao, nao esta causando duplicidade real).
- Fica pendente (Fase 5b, escopo maior — 2 RPCs de Medicao, uma com 4 patches
  dinamicos sobre o corpo da migration 127, mais a leitura em
  `loadProgrammingMatchMap` de `src/app/api/medicao/route.ts`): ver TASKS.md.
- `project_measurement_order_items.programming_activity_id` (tabela
  `project_programming_activities`, sem cobertura em `programming_legacy_map`)
  NAO faz parte do Controle APR — decisao fica para a entrega de Medicao.
- Validado com `npx tsc --noEmit`, `npm run lint` (0 erros; 45 warnings legados,
  nenhum no script novo) e `npm run db:security-check` (sem apontamento na 350;
  achado preexistente na 338 fora do escopo). `npm run db:lint`/`db:migration-list`
  bloqueados pelo 403 conhecido do token de login (mesma limitacao ja registrada em
  outras entregas deste modulo). Migration 350 pendente de aplicacao no Supabase.
- Arquivos: `supabase/migrations/350_apr_control_match_normalized_programming.sql`,
  `supabase/migrations/README.txt`, `scripts/audit-apr-programming-match-readonly.mjs`
  (novo), `docs/Controle_APR.txt`.
- CORRECAO durante o teste do usuario: a primeira versao da migration rodava o
  UPDATE de remap antes de trocar a FK, violando a propria constraint (ainda
  apontando pra `project_programming`) ao gravar um id de `programming` —
  ordem corrigida para DROP da constraint antiga -> UPDATE -> ADD da constraint
  nova. Ver TASKS.md para o detalhe (a 344/Cronograma tem a mesma ordem
  latente, sem sintoma so porque a remessa dela e 0 linhas hoje).
