Tela: Cronograma de Solicitacoes Tecnicas

Visao geral
- CRUD de pedidos tecnicos da operacao (Inspecao, As Built, Locacao) com controle de prazo (SLA).
- Cada solicitacao referencia um projeto ja cadastrado; municipio, endereco e prioridade do projeto sao puxados automaticamente (somente leitura).
- Status de negocio: Pendente, Concluido, Cancelado. Atrasado e derivado em tempo real (nao persistido).
- Multi-tenant: todo registro pertence a um tenant_id; nenhuma consulta cruza tenants.

Arquitetura
- Pagina: src/app/(dashboard)/cronograma-solicitacoes/page.tsx -> CronogramaSolicitacoesPageView.
- Frontend (modulo): src/modules/dashboard/cronograma-solicitacoes/
  - CronogramaSolicitacoesPageView.tsx (tela: cards, filtros, grid, modal, acoes, export CSV client-side)
  - api.ts (fetch com Bearer token), types.ts, constants.ts, index.ts, CronogramaSolicitacoesPageView.module.css
- API (rotas finas): src/app/api/cronograma-solicitacoes/
  - route.ts        -> GET lista paginada, POST criar, PUT editar
  - meta/route.ts   -> GET catalogos (tipos, prioridades, status, responsaveis, projetos, ids elegiveis As Built) com cache 5 min
  - estado-programacao/route.ts -> GET estado atual da Programacao do projeto (validacao do formulario)
  - verify/route.ts -> POST concluir (botao Verificado)
  - cancel/route.ts -> POST cancelar (com motivo)
- Backend (regras): src/server/modules/cronograma-solicitacoes/
  - handlers.ts (list, meta, estado, create, update, verify, cancel), queries.ts, normalizers.ts, types.ts, authorization.ts
- Banco: supabase/migrations/304_create_cronograma_solicitacoes.sql
  - tabelas cronograma_solicitacoes e cronograma_solicitacoes_history, RLS por tenant, indices, RPC get_cronograma_asbuilt_project_ids, seed de app_pages/permissoes.

Cadastro
- Campos: Projeto (autocomplete por codigo), Tipo, Prioridade, Data de Entrada, Data Limite, Responsavel, Observacoes, Justificativa (Alta).
- Solicitante: usuario logado (automatico). Puxados do projeto: Municipio, Endereco, Prioridade do projeto.
- Prazos (dias corridos, TZ America/Sao_Paulo): Baixa = Entrada + 10 (travado), Media = Entrada + 5 (travado), Alta = manual (>= Entrada) + justificativa obrigatoria.
- Trava de estado da Programacao: somente As Built. So aceita projeto cujo estado atual seja CONCLUIDO ou PARCIAL_PLANEJADO_BENEFICIO_ATINGIDO. Inspecao e Locacao aceitam qualquer projeto ativo.
- Estado atual do projeto = ultima linha de project_programming por execution_date/etapa. A Data Limite nao e chave de consulta.
- Normalizacao das grafias do catalogo: BENFICIO e BENEFICIO sao tratadas como o mesmo estado.
- Duplicidade: constraint unica (tenant_id, projeto_id, data_entrada, tipo_solicitacao).
- Tipo padrao por usuario (pre-selecao): um admin define, por usuario, o Tipo que ja vem selecionado ao abrir Nova Solicitacao. E apenas pre-selecao (o usuario pode trocar). Gerenciado no modal "Tipo padrao por usuario" (botao visivel so para admin/master). Tabela cronograma_user_tipo_default (migration 306); meta retorna defaultTipo do usuario logado (fora do cache por tenant).

Filtros, auto-complete e listagem
- Filtros combinaveis (colunas indexadas): tipo, prioridade, status, responsavel, municipio, periodo de prazo, mais pesquisa (projeto/codigo/responsavel/solicitante) e ordenacao.
- Status Atrasado e um predicado SQL: status = PENDENTE and data_limite < hoje (hoje em America/Sao_Paulo). Pendente no filtro = PENDENTE e data_limite >= hoje (sem dupla contagem com Atrasadas).
- Cards agregados por predicado SQL (count/head): Total, Pendentes, Concluidas, Atrasadas, Vencendo Hoje, Vencendo em 3 dias.
- Paginacao real via count + range. Autocomplete de As Built restrito aos ids elegiveis (RPC).
- Exportacao Excel: CSV client-side (buildCsvContent + downloadCsvFile), somente a pagina filtrada atual.

Boas praticas
- meta separado da lista, com cache 5 min; frontend carrega meta e lista em paralelo.
- Escrita direta com o client service-role, sempre com escopo .eq(tenant_id) e ator explicito (created_by/updated_by).
- Historico registrado em toda operacao (CREATE, UPDATE, VERIFY, CANCEL).
- Lock otimista por updated_at (expectedUpdatedAt) em edicao/verificacao/cancelamento,
  aplicado de forma atomica na propria clausula WHERE do UPDATE desde 2026-08-15 (ver
  secao "Atualizacao 2026-08-15" abaixo) — antes disso a checagem era so em memoria.

Cancelamento e conclusao
- Verificado (Concluido): pelo criador do pedido ou admin/master; grava data_conclusao. So a partir de Pendente.
- Cancelado: pelo criador ou admin/master; exige motivo. So a partir de Pendente.
- Edicao permitida somente enquanto Pendente. Concluido e Cancelado sao imutaveis.

Mapa de Codigo (funcoes, hooks e constantes)
- Funcoes principais
  - listSolicitacoes / getMeta / getEstadoProgramacao / createSolicitacao / updateSolicitacao / verifySolicitacao / cancelSolicitacao
    Caminho: src/server/modules/cronograma-solicitacoes/handlers.ts
    Responsavel por: autorizacao por acao, validacao, montagem do item, agregacao de cards e escrita + historico.
  - fetchLatestProgrammingState / fetchAsbuiltEligibleProjectIds / applySolicitacaoFilters / insertHistory
    Caminho: src/server/modules/cronograma-solicitacoes/queries.ts
    Responsavel por: acesso a dados, filtros nativos e RPC de elegibilidade As Built.
  - authorizeCronogramaAction (pageKey cronograma-solicitacoes)
    Caminho: src/server/modules/cronograma-solicitacoes/authorization.ts
- Constantes e configuracoes
  - TIPOS_SOLICITACAO, PRIORIDADES, STATUS_ARMAZENADOS, PRAZO_DIAS_POR_PRIORIDADE, ASBUILT_ESTADOS_PERMITIDOS
    Caminho: src/server/modules/cronograma-solicitacoes/normalizers.ts
- Funcoes utilitarias relacionadas
  - businessToday, addDaysIso, diffDays, resolveDataLimite, resolveStatusEfetivo, normalizeWorkCompletionToken
    Caminho: src/server/modules/cronograma-solicitacoes/normalizers.ts
  - parsePagination, addChange, normalizeText (compartilhados): src/lib/server/apiHelpers.ts
  - buildCsvContent, downloadCsvFile: src/lib/utils/csv.ts
- Servicos e integracoes externas
  - RPC get_cronograma_asbuilt_project_ids(tenant): ids de projetos elegiveis para As Built (estado atual CONCLUIDO/PARCIAL_PLANEJADO_BENEFICIO_ATINGIDO).
  - Views/tabelas: project_with_labels, project_programming, people, app_users, job_titles.

Observacoes de implementacao (pendencias tecnicas)
- Escrita usa o client service-role no handler (nao RPC security-definer). Reforca tenant scope no codigo; pode ser migrado para RPC no futuro sem mudar o contrato da tela.
- A resolucao de estado do As Built usa project_programming.work_completion_status (texto). Tenants que so gravam work_completion_status_id (catalogo) podem precisar de ajuste na RPC/query.
- Responsavel referencia public.people (cargo VISTORIADOR). Solicitante referencia public.app_users (usuario logado).

Atualizacao 2026-07
- Criacao inicial da tela e do modulo.

Atualizacao 2026-07-29 - Fase 3a do corte: leitura na Programacao Normalizada (migration 344)
-----------------------------------------------------------------------------------------------
- O "estado atual da Programacao" desta tela passa a vir de `programming` (modelo
  normalizado) em vez de `project_programming` (tela `programacao-simples`, congelada
  em somente leitura desde o commit `7ecd00a`).
- A leitura deixa de ser query propria e passa pela fachada
  `@/server/modules/programacao-normalizada` (`fetchWorkCompletionByProject`). Antes
  do corte a mesma regra estava reescrita em cinco telas com criterios divergentes;
  agora existe uma vez so. `fetchLatestProgrammingState` e
  `fetchLatestProgrammingStateMap` continuam existindo com a MESMA assinatura e
  semantica — mudou o que esta por baixo.
- As duas semanticas distintas foram preservadas de proposito:
  * `fetchLatestProgrammingState` (individual) devolve null quando o projeto nunca
    teve Estado do Trabalho preenchido;
  * `fetchLatestProgrammingStateMap` (lista) coloca no mapa qualquer projeto com
    etapa nao-cancelada, mesmo sem estado — a tela usa a AUSENCIA no mapa para exibir
    "A PROGRAMAR" e a presenca com estado vazio para exibir "-".
- Regras mantidas 1:1: etapa CANCELADA nao conta, ANTECIPADA conta, "ultimo" = maior
  `execution_date` com desempate por `updated_at`.
- Pendencia: a migration 318 tirou 'PENDENCIA' de status e de Estado do Trabalho e
  transformou na flag `is_pendencia`. A fachada expoe a flag em campo proprio, sem
  dobrar dentro do status. Nao muda nada aqui, porque pendencia nunca liberou As
  Built.

FK `programacao_id` repontada (por que junto e nao na Fase 5)
- O plano separava "trocar leitura" (Fase 3) de "remapear FKs" (Fase 5). Para esta
  tela isso nao se sustenta: o `programmingId` que a leitura devolve e exatamente o
  valor gravado em `cronograma_solicitacoes.programacao_id`. Trocar a fonte sem
  repontar a FK faria todo cadastro novo violar a constraint.
- A FK era simples, por `id`, para `project_programming` (migration 304). Agora e
  composta com `tenant_id` para `programming`, seguindo a regra 12 do guia_sql.md.
- Custo real: 0 linhas para migrar (medido em producao). O Cronograma sai da Fase 5.

CORRECAO OBRIGATORIA: `BENEFICIO_ATINGIDO` entra nos estados que liberam As Built
- O catalogo tem os DOIS codigos ativos: `PARCIAL_PLANEJADO_BENFICIO_ATINGIDO` (com o
  typo, legado) e `BENEFICIO_ATINGIDO` (correto, criado pela migration 310). As
  cargas 315/335 remapearam o typo para o codigo novo ao migrar para `programming`.
- `ASBUILT_ESTADOS_PERMITIDOS` conhecia apenas `CONCLUIDO` e
  `PARCIAL_PLANEJADO_BENEFICIO_ATINGIDO`. Trocar a fonte sem corrigir isso tiraria As
  Built de 7 projetos em producao, sem nenhuma regra de negocio ter mudado.
- `BENEFICIO_ATINGIDO` foi adicionado nos DOIS lugares, que precisam andar juntos:
  `src/server/modules/cronograma-solicitacoes/normalizers.ts` e a RPC
  `get_cronograma_asbuilt_project_ids` (migration 344).

IMPACTO MEDIDO NA ELEGIBILIDADE DE AS BUILT (producao, 2026-07-29, 141 projetos)
- 134 projetos: sem mudanca.
- 6 projetos PASSAM a permitir As Built. Todos porque a tela nova ja avancou o estado
  para CONCLUIDO e a base legada ficou parada — o merge conservador da migration 335
  nao sobrescreveu o destino de proposito. O valor novo e o atual; a tela estava
  bloqueando As Built com dado velho.
  A051378598, A051696331, A052579602, A051905773, A049563851, 2307202601.
- 1 projeto DEIXA de permitir: A053714340. NAO e efeito desta entrega — e resultado da
  carga 315/335. No legado havia duas linhas em 2026-06-18 (uma ADIADA sem estado e
  uma PROGRAMADA com CONCLUIDO); como o modelo novo tem uma etapa por projeto+data, as
  duas foram fundidas e o vencedor (por `updated_at` mais recente) foi a ADIADA sem
  estado. No modelo novo o projeto ficou sem nenhuma etapa ativa: 2026-07-12 CANCELADA,
  2026-06-18 ADIADA, 2026-05-20 CANCELADA, 2026-04-24 CANCELADA.
  PENDENTE DE DECISAO DO USUARIO: o dado nao foi corrigido por esta migration de
  proposito — reescrever um Estado do Trabalho definido pela politica de merge nao e
  papel de uma migration de leitura. Se o projeto realmente esta concluido, o ajuste e
  feito na tela nova.

- Arquivos alterados:
  - `supabase/migrations/344_cronograma_read_normalized_programming.sql` (novo)
  - `src/server/modules/programacao-normalizada/index.ts` (novo, fachada publica)
  - `src/server/modules/programacao-normalizada/queries.ts`
  - `src/server/modules/cronograma-solicitacoes/queries.ts`
  - `src/server/modules/cronograma-solicitacoes/normalizers.ts`

Atualizacao 2026-08-03 - `executionDate` agora e anulavel na fachada
- `fetchLatestProgrammingState` passou a devolver `executionDate: string | null`.
  A coluna `programming.execution_date` e anulavel desde a migration 318 (etapa
  "em espera" = ADIADA sem data) e o tipo antigo (`string`) escondia isso —
  a fachada estourava `Cannot read properties of null (reading 'localeCompare')`
  ao ordenar as etapas do projeto. Correcao e criterio de ordenacao em
  `docs/Tela_Programacao_Normalizada_SaaS.txt`, secao "Atualizacao 2026-08-03".
- Comportamento da tela NAO muda: nenhum handler consome `executionDate` (usam
  `stateToken` e `programmingId`). O que muda e que projeto com etapa em espera
  deixa de derrubar a listagem e a verificacao de As Built.
- Regra de "ultimo" complementada: etapa SEM data nunca vence etapa COM data.
- Arquivos alterados:
  - `src/server/modules/programacao-normalizada/queries.ts`
  - `src/server/modules/cronograma-solicitacoes/queries.ts`

Atualizacao 2026-08-15 - Escrita concorrente deixa de ter janela de corrida real
--------------------------------------------------------------------------------------------
- Problema encontrado (auditoria de concorrencia entre telas, disparada por duvida sobre a
  tela de Medicao): `updateSolicitacao`, `verifySolicitacao` e `cancelSolicitacao` liam o
  registro (`fetchSolicitacaoById`), decidiam em memoria JS se a acao era permitida
  (`status === "PENDENTE"`, `expectedUpdatedAt` OPCIONAL — so validado se o cliente
  mandasse) e so entao faziam `UPDATE ... WHERE tenant_id = X AND id = Y`, sem `status` nem
  `updated_at` na clausula WHERE e sem `SELECT ... FOR UPDATE`. Cenario real: dois usuarios
  agindo quase ao mesmo tempo sobre a mesma solicitacao PENDENTE (ex.: um edita, outro
  cancela) passavam os dois na checagem em memoria; o UPDATE que chegasse por ultimo no
  Postgres vencia (last-write-wins) sem retornar erro para nenhum dos dois e sem registrar o
  conflito — unico modulo do sistema com essa lacuna (os demais ja usam RPC com
  `FOR UPDATE` + `expectedUpdatedAt` obrigatorio).
- Depois: as tres funcoes passam a condicionar o UPDATE final a
  `.eq("status", "PENDENTE").eq("updated_at", current.updated_at)` (valor lido pelo proprio
  handler no inicio da requisicao, nao o que o cliente mandou) e trocam `.single()` por
  `.maybeSingle()`. Se nenhuma linha for afetada (outra escrita venceu a corrida entre a
  leitura e este UPDATE), a funcao nova `buildConflictResponse` reconsulta o registro e
  devolve 409 com `reason: "CONCURRENT_MODIFICATION"`, `currentRecord`, `currentUpdatedAt`,
  `updatedBy` e `changedFields` (o que esta requisicao tentava mudar). Nao foi necessaria
  migration nem RPC nova: o UPDATE ja roda pelo client service-role (fora de RLS) e o
  Postgres resolve a corrida de forma atomica so com a clausula WHERE — mesma garantia
  pratica de um `SELECT ... FOR UPDATE`, sem reescrever a regra de negocio em PL/pgSQL.
- A checagem antiga de `expectedUpdatedAt` em memoria (regra 604/700/772 do handlers.ts) foi
  mantida como falha-rapido opcional (feedback mais cedo quando o cliente envia o campo); a
  protecao real agora vem do UPDATE condicional, que vale mesmo se o cliente nao enviar
  `expectedUpdatedAt`.
- Frontend: `src/modules/dashboard/cronograma-solicitacoes/api.ts`, `parseJson` (usado por
  `saveSolicitacao`, `verifySolicitacao`, `cancelSolicitacao`) anexa
  `" (alterada por <nome>")` na mensagem de erro quando `reason === "CONCURRENT_MODIFICATION"`.
- Arquivos alterados:
  - `src/server/modules/cronograma-solicitacoes/handlers.ts` (`buildConflictResponse` novo;
    `updateSolicitacao`, `verifySolicitacao`, `cancelSolicitacao` com UPDATE condicional)
  - `src/modules/dashboard/cronograma-solicitacoes/api.ts` (`parseJson`)
- Mesma auditoria cobriu 20+ modulos de escrita do backend (`activities`, `materials`,
  `people`, `job-titles`, `teams`, `composicao-equipe`, `controle-apr`, `faturamento`,
  `medicao-asbuilt`, `projects`, `programacao-normalizada`, `stockRequisitions`,
  `stockTransfers`, `teamStockOperations`, `warehouse-addressing`, `trafo-positions`) — todos
  ja usam RPC com `FOR UPDATE`/`expectedUpdatedAt` obrigatorio; Cronograma de Solicitacoes
  era o unico com janela de corrida real. Ver tambem `docs/Tela_Medicao_SaaS.txt`, secao
  "Atualizacao 2026-08-15" (409 enriquecido, mesma tarefa).
- Validado com `npx tsc --noEmit` (sem erro nos arquivos tocados) e `npm run lint` (0 erros).
  `npm run build` falha neste ambiente por um erro pre-existente e nao relacionado (arquivos
  Deno em `Backup/` sendo varridos pelo typecheck do Next) — confirmado com `git stash` antes
  desta tarefa.
