Tela: Arquitetura SaaS

Visao geral
-----------
- Documento de referencia da base web criada para o SaaS.
- Cobre stack, rotas iniciais e fronteira com Supabase.

Arquitetura
-----------
- Frontend: Next.js App Router com `src/app` para rotas/layouts e `src/modules` para as telas.
- Backend: Supabase (Edge Functions, Auth, tabelas com RLS e migrations versionadas).
- Login web: `auth-login-web`.
- Login local: `src/app/api/auth/local-login/route.ts`.

Cadastro
--------
- Modulos previstos: Cadastro Base, Pessoas e Materiais.

Filtros, auto-complete e listagem
---------------------------------
- Nao se aplica nesta etapa.

Boas praticas
-------------
- `app_users` separado de `people`.
- `login_name` unico globalmente enquanto o tenant nao for resolvido antes do login.
- RLS e tenant scope mantidos no Supabase.
- React Query como base de chamadas ao servidor desde o inicio.

Cancelamento e saldo
--------------------
- Nao se aplica nesta etapa inicial do frontend.

Atalhos
-------
- Rota publica: `/login`
- Rotas protegidas: `/home`, `/cadastro-base`, `/pessoas`, `/materiais`, `/entrada`, `/saida`, `/estoque`

Atualizacao 2026-03
-------------------
- Base do `SaaS (Web)` criada.
- Edge Function `auth-login-web` criada.
- Migration `016_add_login_name_to_auth_tables.sql` criada.
- Migration `017_sync_auth_users_to_app_users.sql` criada.
- Rotas reorganizadas para separar `app = rotas/layouts` de `modules = telas`.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/app/(public)/login/page.tsx`
  - Responsavel por: expor a rota publica `/login` como wrapper fino.
- Caminho: `src/modules/auth/login/LoginPageView.tsx`
  - Responsavel por: implementar a tela de login.
- Caminho: `src/modules/dashboard/home/HomePageView.tsx`
  - Responsavel por: implementar a home autenticada.
- Caminho: `src/services/auth/auth.service.ts`
  - Responsavel por: login remoto/local e limpeza da sessao.
- Caminho: `supabase/edge_functions/auth-login-web/index.ts`
  - Responsavel por: autenticar `login_name`, aplicar rate limit e devolver tokens.

Constantes e configuracoes
- Caminho: `SaaS (Web)/.env.example`
  - Responsavel por: ambiente do login.
- Caminho: `supabase/migrations/016_add_login_name_to_auth_tables.sql`
  - Responsavel por: suporte de banco ao login web.
- Caminho: `supabase/migrations/017_sync_auth_users_to_app_users.sql`
  - Responsavel por: vincular `auth.users` com `public.app_users`.

Funcoes utilitarias relacionadas
- Caminho: `src/context/AuthContext.tsx`
  - Responsavel por: hidratar e manter a sessao.
- Caminho: `src/lib/react-query/provider.tsx`
  - Responsavel por: provider global de cache e chamadas remotas.

Servicos e integracoes externas
- Supabase Auth
- Supabase Edge Functions
- Supabase RLS

Ajuda contextual
- Sessao por inatividade, recovery e touch/revoke ainda nao entraram nesta fase.
- O objetivo atual e manter `src/app` limpo, com page/layout finos, e concentrar texto/UI das telas em `src/modules`.
