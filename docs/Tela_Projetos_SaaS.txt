
Tela: Projetos SaaS

Visao geral
-----------
- Tela operacional para cadastro, filtro e listagem de projetos por tenant.
- Implementa fluxo completo com criacao, edicao, inativacao e historico de alteracoes.
- O formulario de cadastro/edicao trabalha apenas com `Projeto (SOB)`, sem limite maximo de caracteres e sem padrao fixo de prefixo por prioridade.
- O fluxo operacional da tela nao expõe nem exige `FOB`.
- Mantem padrao visual com blocos separados: cadastro, filtros e lista em colunas.
- Inclui aba `Materiais previstos` para manter a lista inicial de materiais planejados por projeto, com importacao em massa e inclusao manual.
- Inclui aba `Atividades previstas` para manter a base operacional de atividades planejadas por projeto, com importacao em massa, inclusao manual e edicao em linha.
- Mantem indicador operacional `has_locacao` no projeto para consumo futuro em visoes gerais e cruzamentos com `Locacao`.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/projetos/page.tsx`
- View: `src/modules/dashboard/projetos/ProjectsPageView.tsx`
- Estilo: `src/modules/dashboard/projetos/ProjectsPageView.module.css`
- API de projetos: `src/app/api/projects/route.ts`
- API de metadados/filtros: `src/app/api/projects/meta/route.ts`
- API de materiais previstos (listagem): `src/app/api/projects/forecast/route.ts`
- API de busca de materiais previstos por projeto: `src/app/api/projects/forecast/catalog/route.ts`
- API de atividades previstas (listagem e salvamento): `src/app/api/projects/activity-forecast/route.ts`
- API de importacao XLSX de atividades previstas: `src/app/api/projects/activity-forecast/import/route.ts`
- API de busca de atividades previstas por projeto: `src/app/api/projects/activity-forecast/catalog/route.ts`
- Edge Function de template XLSX: `supabase/edge_functions/get_project_forecast_template/index.ts`
- Edge Function de importacao XLSX: `supabase/edge_functions/import_project_forecast/index.ts`
- Edge Function de template XLSX de atividades: `supabase/edge_functions/get_project_activity_forecast_template/index.ts`
- Edge Function legada de importacao XLSX de atividades: `supabase/edge_functions/import_project_activity_forecast/index.ts`
- Banco: `supabase/migrations/029_create_project_table.sql`
- Banco (regras de SOB por prioridade): `supabase/migrations/030_project_sob_priority_rules.sql`
- Banco (tabelas de dominio por tenant): `supabase/migrations/031_create_project_lookup_tables.sql`
- Banco (responsavel contratada via pessoas supervisoras): `supabase/migrations/034_use_people_for_project_contractor_responsible.sql`
- Banco (status ativo + historicos): `supabase/migrations/036_create_project_history_and_cancellation.sql`
- Banco (regras de ativacao em historico): `supabase/migrations/037_project_activation_history_rules.sql`
- Banco (colunas de dominio em UUID + view de labels): `supabase/migrations/038_project_lookup_uuid_columns.sql`
- Banco (materiais previstos por projeto): `supabase/migrations/041_create_project_material_forecast.sql`
- Banco (protecao de importacao por RPC): `supabase/migrations/043_project_forecast_import_guards.sql`
- Banco (tenants + vinculo de acesso por usuario): `supabase/migrations/045_create_tenants_and_user_tenant_access.sql`
- Banco (FK tenant global em tabelas com tenant_id): `supabase/migrations/046_add_tenant_fk_to_all_tenant_tables.sql`
- Banco (flag operacional de locacao): `supabase/migrations/060_add_project_has_locacao.sql`
- Banco (atividades previstas por projeto + seed da locacao): `supabase/migrations/064_create_project_activity_forecast.sql`
- Banco (materiais manuais + importacao protegida de atividades): `supabase/migrations/065_project_forecast_manual_and_activity_import.sql`
- Banco (concorrencia e limites dos previstos): `supabase/migrations/066_harden_location_and_project_forecast_rpcs.sql`
- Banco (RPC transacional da tela principal): `supabase/migrations/077_create_admin_write_rpcs.sql`
- Banco (remocao da constraint de formato por prioridade no SOB): `supabase/migrations/118_remove_project_sob_priority_format_constraint.sql`
- Banco (marcador retirado da carteira): `supabase/migrations/174_add_project_is_withdrawn.sql`

Cadastro
--------
- Campos obrigatorios:
  - `Prioridade`
  - `Projeto (SOB)`
  - `Centro de Servico`
  - `Tipo de Servico`
  - `Data limite`
  - `Valor estimado`
  - `Responsavel Contratada`
  - `Responsavel Distribuidora`
  - `Gestor de campo Distribuidora`
  - `Municipio`
  - `Logradouro`
  - `Bairro`
- Campos opcionais:
  - `Nivel de Tensao`
  - `Porte`
  - `Descricao do servico`
  - `Observacao`
  - `Obra de teste` (checkbox para excluir da carteira e das consolidacoes de medicao)
  - `RETIRADO DA CARTEIRA` (checkbox para excluir dos cards de carteira sem tratar como obra de teste)
  - `TERCEIROS` (checkbox para registrar obras de outra empresa apenas para rastrear saida de material)
- `Centro de Servico` e preenchido automaticamente quando o `SOB` informado ja existe no catalogo do tenant.
- `Parceira` nao aparece no frontend; o backend preenche automaticamente com `contract.name` do tenant ativo.
- `Valor estimado` aceita digitacao monetaria em formato brasileiro (`22.035,33`) e formatos compactos equivalentes (`22.03533`, `22035,33`), normalizando para numero decimal antes de salvar em `project.estimated_value numeric(14,2)`.
- Campos carregados de tabelas de dominio:
  - `Prioridade`
  - `Centro de Servico`
  - `Tipo de Servico`
  - `Nivel de Tensao`
  - `Porte`
  - `Municipio`
  - `Responsavel Distribuidora`
  - `Gestor de campo Distribuidora`
- Campo `Responsavel Contratada` carregado de `people` ativo, filtrando apenas cargos `SUPERVISOR` em `job_titles`.
- Regra de validacao do `SOB` antes do banco:
  - O campo aceita texto livre (normalizado em maiusculas), sem validacao de prefixo por prioridade.
  - `SOB` duplicado no mesmo tenant nao pode ser cadastrado.
- Regra de habilitacao no formulario:
  - `Projeto (SOB)` so fica ativo depois que `Prioridade` for selecionada.
- Regra de edicao no formulario:
  - Botao `Editar` carrega o projeto no bloco de cadastro e move a pagina para o topo.
  - Em modo edicao, o bloco `Cadastro de Projeto` recebe borda amarela de destaque.
  - A rolagem para topo usa o container interno do dashboard (`mainContent`), nao `window`.
  - A edicao envia `expectedUpdatedAt` e bloqueia o save quando outro usuario salva ou altera o status do projeto antes.
- Aba `Materiais previstos`:
  - Recebe selecao de projeto pela lista (acao `Materiais previstos`).
  - Permite selecionar projeto por `Projeto (SOB)` em campo de texto com sugestao (datalist), no estilo de busca digitavel.
  - Permite pesquisar material ativo por `codigo` ou `descricao`.
  - Permite incluir material previsto manualmente com `quantidade`.
  - Permite editar `quantidade` e `observacao` diretamente na linha.
  - Permite abrir o modal `Cadastro em massa` para baixar o modelo e importar planilha XLSX com `projeto`, `codigo` e `quantidade`, adicionando materiais previstos de varios projetos no mesmo arquivo.
  - Quando a importacao possui erro, gera CSV de erros com `linha`, `coluna`, `valor` e `erro`.
  - Quando o codigo ja existe no projeto, a linha e ignorada e as demais linhas novas continuam sendo cadastradas.
  - A inclusao manual e a edicao em linha passam pela RPC `save_project_material_forecast`.
  - A inclusao manual e a edicao em linha limitam quantidade a `100000` no frontend e na RPC.
  - A edicao em linha envia `expectedUpdatedAt` para bloquear sobrescrita silenciosa por concorrencia.
  - A importacao valida via RPC e bloqueia codigo duplicado no arquivo e codigo ja importado no projeto.
- Aba `Atividades previstas`:
  - Recebe selecao de projeto pela lista (acao `Atividades previstas`).
  - Permite selecionar projeto por `Projeto (SOB)` em campo de texto com sugestao (datalist).
  - Permite pesquisar atividade ativa por `codigo` ou `descricao`.
  - Permite incluir atividade prevista com `quantidade`.
  - Exibe `Pontos`, `Valor unitario` e `Valor previsto`, calculado como `Pontos x Quantidade prevista x Valor unitario x taxa`, com taxa `1` nesta versao.
  - O botao `Adicionar atividade` segue o mesmo posicionamento visual do `Adicionar material` da aba `Materiais previstos`.
  - Permite abrir o modal `Cadastro em massa` para baixar o modelo e importar planilha XLSX com `projeto`, `codigo` e `quantidade`, adicionando atividades previstas de varios projetos no mesmo arquivo.
  - Quando a importacao possui erro, gera CSV de erros com `linha`, `coluna`, `valor` e `erro`.
  - Quando o codigo ja existe no projeto, a linha e ignorada e as demais linhas novas continuam sendo cadastradas.
  - Permite editar `quantidade` e `observacao` diretamente na linha.
  - Bloqueia quantidade `<= 0` no backend via RPC `save_project_activity_forecast`.
  - A inclusao manual e a edicao em linha limitam quantidade a `100000` no frontend e na RPC.
  - A edicao em linha envia `expectedUpdatedAt` para bloquear sobrescrita silenciosa por concorrencia.
  - A importacao valida via RPC e bloqueia codigo duplicado no arquivo e codigo ja importado no projeto.

Filtros, auto-complete e listagem
---------------------------------
- Filtros implementados:
  - `Projeto (SOB)` (texto)
  - `Data Execucao` (data)
  - `Prioridade` (select)
  - `Centro de Servico *` (select com opcoes do tenant)
  - `Tipo de Servico` (select com opcoes do tenant)
  - `Municipio` (select)
  - `Situacao da carteira` (select com `Todos`, `Cancelados`, `Retirados da carteira` e `Terceiros`)
  - `Estado Trabalho` (`Todos`, opcoes do catalogo do tenant e `Nao informado`)
  - `Tipo SGD` (`Todos` + itens ativos do catalogo `programming_sgd_types`)
- Auto-complete implementado para:
  - `SOB`
  - `Prioridade`
  - `Centro de Servico`
  - `Municipio`
  - `Responsaveis`
- Listagem em colunas com paginacao server-side (`page` + `pageSize`) para evitar truncamento por limite de retorno em uma unica chamada.
- Botao `Exportar Excel (CSV)` na lista para exportar os projetos filtrados.
- O botao de exportacao da lista fica bloqueado por `10` segundos apos cada clique para evitar disparos repetidos.
- Texto fixo de pagina no cabecalho da lista removido para manter header mais limpo.
- Coluna `Parceira` removida da lista para reduzir redundancia visual.
- Coluna `Acoes` com botoes: `Ver detalhes`, `Editar`, `Historico`, `Materiais previstos` e `Cancelar`.
- Quando a obra estiver inativa, o botao `Cancelar` vira botao `Ativar`.
- `Ver detalhes` abre modal com todos os campos da obra e mostra o `ID do projeto` abaixo do titulo.
- `Historico` abre modal com apenas os campos alterados por evento e paginacao interna para nao crescer demais.
- `Cancelar` abre modal com motivo obrigatorio e valida a inativacao da obra.
- `Ativar` abre modal com motivo obrigatorio e valida a reativacao da obra.
- Colunas `Prioridade` e `Responsavel Contratada` removidas da lista.
- Coluna `Registrado por` exibida com `app_users.login_name` do `created_by`.
- Coluna de data na lista padronizada como `Registrado em` (com base em `created_at`).
- Lista principal ordenada por `Registrado em` decrescente, exibindo os ultimos projetos registrados primeiro.
- Bloco `Status da Carteira de Projetos` adicionado no topo da tela (acima de `Cadastro de Projeto`) com 2 cards:
  - `Total de projetos` (todos os `SOB` unicos retornados pelos filtros atuais, excluindo obras de teste).
  - `Concluidas` (projetos por `SOB` unico com `Estado Trabalho = CONCLUIDO` na `Programacao`, conforme filtros atuais; nao soma por equipe).
- Obras marcadas como `TERCEIROS` permanecem disponiveis para fluxos de material, mas ficam fora de Programacao, Mapa de Programacao, Medicao, Dashboards de producao, Faturamento, Locacao, APR, Composicao de Equipe, Cronograma de Solicitacoes e Apuracao de Fator Minimo.
- Chamadas autenticadas da tela usam `cache: no-store` para evitar exibir dados de sessao antiga apos troca de usuario.
- Quando a aba `Materiais previstos` esta ativa, os blocos de `Filtros` e `Lista de Projetos` ficam ocultos e a tela mostra:
  - `Filtros de Materiais Previstos` (codigo, descricao, tipo).
  - `Lista de Materiais Previstos` (somente materiais do projeto selecionado).
  - O campo `Projeto (SOB)` usa o catalogo completo de SOB do tenant para resolver o projeto, nao apenas a pagina atual da lista principal.
  - Botao `Exportar Excel (CSV)` para exportar os materiais previstos filtrados.
  - O botao tambem respeita bloqueio de `10` segundos por clique.
- Quando a aba `Atividades previstas` esta ativa, os blocos de `Filtros` e `Lista de Projetos` ficam ocultos e a tela mostra:
  - `Filtros de Atividades Previstas` (codigo, descricao, tipo).
  - `Lista de Atividades Previstas` (somente atividades do projeto selecionado).
  - O campo `Projeto (SOB)` usa o catalogo completo de SOB do tenant para resolver o projeto, nao apenas a pagina atual da lista principal.
  - Botao `Exportar Excel (CSV)` para exportar as atividades previstas filtradas.
  - O botao tambem respeita bloqueio de `10` segundos por clique.

Boas praticas
-------------
- Sempre filtrar e gravar com `tenant_id` no backend.
- Usar validacao de campos obrigatorios na API antes de gravar.
- Manter `Projeto (SOB)` sem limite fixo de caracteres e sem regra de prefixo por prioridade no frontend e na API.
- Manter paginacao server-side para listas grandes.
- Garantir auditoria em `project` com `created_by`, `updated_by`, `created_at` e `updated_at`.
- Exigir `expectedUpdatedAt` na edicao e no cancelamento/ativacao para evitar `last-write-wins`.
- Preferir RPC transacional para escrita da tela principal, concentrando update, status e historico no banco.
- Tratar `Materiais previstos` como planejamento: nao movimenta saldo fisico nem saldo liquido de projeto.
- Validar importacao XLSX por tenant (projeto/material e projeto/atividade) antes de persistir.
- A planilha de importacao de previstos deve conter `projeto`, `codigo` e `quantidade`; descricao/umb/tipo vem do cadastro base `materials`.
- A planilha de importacao de atividades previstas deve conter `projeto`, `codigo` e `quantidade`; descricao/unidade/tipo/pontos/valor unitario vem do cadastro base `service_activities`.
- Importacoes invalidas devem gerar arquivo CSV de erros no padrao `linha;coluna;valor;erro`.
- Codigos ja existentes no projeto nao bloqueiam o arquivo inteiro; a linha e ignorada, registrada no CSV de erros/avisos e as linhas novas sao cadastradas.
- `Atividades previstas` do projeto devem ser mantidas com quantidade maior que zero e servem de base para bootstrap inicial da aba `Atividades previstas` na `Locacao` e para o valor previsto do Dashboard Carteira Operacional.
- Exigir controle de concorrencia por `updated_at` na edicao em linha de `Materiais previstos` e `Atividades previstas`.
- Aplicar limite superior `<= 100000` para quantidades manuais e para edicao em linha dos previstos do projeto.
- Bloquear a inativacao da obra quando ainda existir programacao operacional com status `PROGRAMADA` ou `ADIADA`.

Cancelamento e saldo
--------------------
- Cancelamento implementado para Projetos:
  - Inativa a obra (`project.is_active = false`).
  - Exige motivo antes de validar cancelamento.
  - E bloqueado quando a obra ainda possui programacao `PROGRAMADA` ou `ADIADA`.
  - Registra em `project_cancellation_history` com `action_type = CANCEL`.
  - Registra no `project_history` como evento `CANCEL`.
- Ativacao implementada para Projetos:
  - Reativa a obra (`project.is_active = true`).
  - Exige motivo antes de validar ativacao.
  - Registra em `project_cancellation_history` com `action_type = ACTIVATE`.
  - Registra no `project_history` como evento `ACTIVATE`.
- Saldo de estoque nao e alterado por `Materiais previstos`.
- `Materiais previstos` registra somente planejamento de consumo.
- `has_locacao` passa a indicar se o projeto ja teve algum salvamento real no fluxo de `Locacao`.

Atalhos
-------
- Rota ativa: `/projetos`.
- Exibida na secao `Operacao` da sidebar.

Atualizacao 2026-03
-------------------
- Antes: rota `/projetos` era placeholder sem CRUD.
- Depois: tela com cadastro, filtros, lista paginada e acoes completas.
- Criada migration `029_create_project_table.sql` para tabela `public.project` com RLS e auditoria.
- Criada migration `030_project_sob_priority_rules.sql` para normalizacao de `SOB` e unicidade case-insensitive por tenant.
- Criada migration `031_create_project_lookup_tables.sql` para normalizar campos de dominio e vincular `project` com FKs.
- Criada migration `034_use_people_for_project_contractor_responsible.sql` para usar `people` (SUPERVISOR) em `Responsavel Contratada`.
- Criada migration `036_create_project_history_and_cancellation.sql` para status ativo e historicos de projeto/cancelamento.
- Criada migration `037_project_activation_history_rules.sql` para suportar ativacao no historico do projeto e no historico de cancelamento.
- Criada migration `038_project_lookup_uuid_columns.sql` para usar UUID nas colunas de dominio da `project` e expor textos pela view `project_with_labels`.
- API `src/app/api/projects/route.ts` evoluida para:
  - `GET` lista e historico por `historyProjectId` com paginacao (`historyPage`, `historyPageSize`).
  - `GET` agora le dados em `public.project_with_labels` para exibir textos de lookup sem redundancia na tabela base.
  - `GET` possui fallback de compatibilidade para banco/view ainda sem `has_locacao` exposto em `project_with_labels`.
  - `POST` cadastro.
  - `PUT` edicao com diff em `project_history` e controle de concorrencia por `expectedUpdatedAt`.
  - `PATCH` cancelamento/ativacao com motivo obrigatorio, bloqueio de inativacao quando houver agenda operacional pendente e controle de concorrencia por `expectedUpdatedAt`.
- API `src/app/api/projects/meta/route.ts` ajustada para ler `project_with_labels` nos fallbacks de filtros e auto-complete.
- Campo `Parceira` removido do formulario; valor preenchido automaticamente no backend por `contract.name`.
- Botoes da coluna `Acoes` implementados de ponta a ponta na tela.
- Adicionado botao `Exportar Excel (CSV)` para exportar a lista filtrada de projetos.
- Ajustada coluna da lista de `Atualizado em` para `Registrado em`.
- Ajustado `Registrado por` para exibir sempre `app_users.login_name` de quem registrou.
- Antes: a tela de projetos nao tinha cadastro de planejamento de materiais.
- Depois: adicionada aba `Materiais previstos` com importacao em massa via planilha XLSX.
- Antes: a tela de projetos nao tinha base propria para atividades previstas do projeto.
- Depois: adicionada aba `Atividades previstas` com inclusao/edicao em linha e persistencia dedicada.
- Criada migration `041_create_project_material_forecast.sql` para registrar materiais previstos por projeto com RLS e auditoria.
- Criada migration `043_project_forecast_import_guards.sql` com RPCs `precheck_project_material_forecast_import` e `append_project_material_forecast`.
- Criada migration `060_add_project_has_locacao.sql` para adicionar o flag operacional `project.has_locacao` e expor o campo na view `project_with_labels`.
- Criada migration `064_create_project_activity_forecast.sql` para registrar atividades previstas por projeto, criar a RPC `save_project_activity_forecast` e seedar a `Locacao` a partir dessa base.
- Criada migration `065_project_forecast_manual_and_activity_import.sql` para:
  - adicionar a RPC `save_project_material_forecast`
  - proteger a importacao de `Atividades previstas` por RPC
  - permitir inclusao manual e edicao de `Materiais previstos` no projeto
- Criada migration `066_harden_location_and_project_forecast_rpcs.sql` para adicionar controle de concorrencia por `updated_at` e limite maximo de quantidade nas RPCs de `Materiais previstos` e `Atividades previstas` do projeto.
- O formulario de `Projetos` remove `FOB` do fluxo operacional.
- Antes: nao havia marcador simples para saber se o projeto passou pela tela de `Locacao`.
- Depois: o projeto passa a registrar `has_locacao = true` quando existe salvamento real na `Locacao`.
- Importacao e download de modelo de `Materiais previstos` passaram a ser feitos por Edge Functions (`get_project_forecast_template` e `import_project_forecast`).
- Importacao e download de modelo de `Atividades previstas` passam a ser feitos por Edge Functions (`get_project_activity_forecast_template` e `import_project_activity_forecast`).
- A rota Next `src/app/api/projects/forecast/route.ts` deixa de ser apenas listagem e passa a aceitar inclusao/edicao manual via RPC.
- As rotas de previstos do projeto passam a enviar `expectedUpdatedAt` na edicao em linha para bloquear sobrescrita concorrente.
- A tela principal de `Projetos` passa a enviar `expectedUpdatedAt` na edicao e na troca de status para bloquear sobrescrita silenciosa e cancelamento/ativacao com dados stale.
- A tela principal de `Projetos` passa a delegar escrita para as RPCs `save_project_record` e `set_project_record_status`.
- Ajustado `src/lib/server/concurrency.ts` para comparar `updated_at` por instante (epoch) antes da validacao de conflito, evitando falso bloqueio de `Salvar alteracoes` quando o timestamp chega com formatos equivalentes.
- Ajustado `saveProjectViaRpc` em `src/app/api/projects/route.ts` para compatibilidade com assinatura legada da RPC (retry sem `p_fob`) e para retornar mensagem detalhada quando houver erro real de RPC.
- Criada migration `081_add_jsonb_object_length_compat.sql` para compatibilizar ambientes Postgres sem a funcao nativa `jsonb_object_length(jsonb)`, evitando falha em RPCs administrativas.
- Incluido botao `Materiais previstos` na coluna `Acoes` da lista de projetos.
- Ajustada UX da aba `Materiais previstos` para ter filtros e lista proprios de materiais, sem exibir os blocos de filtros/lista de projetos nessa aba.
- Ajustado o modelo XLSX de `Materiais previstos` para aceitar apenas as colunas `codigo` e `quantidade`.
- Ajustado o modelo XLSX de `Atividades previstas` para aceitar apenas as colunas `codigo` e `quantidade`.
- Ajustada UX das abas `Materiais previstos` e `Atividades previstas` para manter simultaneamente importacao em massa e inclusao manual.
- UX: em modo edicao do projeto, o bloco de cadastro recebe destaque visual com borda amarela.
- Ajustadas tela/API/migration para remover a regra de formato por prioridade e o limite de 10 caracteres do `Projeto (SOB)`.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - Responsavel por: estado da tela, cadastro, edicao, filtros, paginacao, listagem e modais de detalhes/historico/cancelamento, incluindo envio de `expectedUpdatedAt` no save e no status.
- Caminho: `src/app/api/projects/route.ts`
  - Responsavel por: validar payload, inserir, editar, cancelar, listar e consultar historico por projeto, ignorando `FOB` no fluxo operacional atual, delegando update/status para RPCs transacionais e aplicando fallback de compatibilidade de assinatura no save via RPC.
- Caminho: `src/app/api/projects/meta/route.ts`
  - Responsavel por: carregar catalogos de filtros e auto-preenchimento.
- Caminho: `src/app/api/projects/forecast/route.ts`
  - Responsavel por: listar, incluir e editar materiais previstos do projeto, enviando `expectedUpdatedAt` para a RPC na edicao em linha.
- Caminho: `src/app/api/projects/forecast/catalog/route.ts`
  - Responsavel por: pesquisar materiais ativos do tenant para incluir no previsto do projeto.
- Caminho: `src/app/api/projects/activity-forecast/route.ts`
  - Responsavel por: listar, incluir e editar atividades previstas do projeto, enviando `expectedUpdatedAt` para a RPC na edicao em linha.
- Caminho: `src/app/api/projects/activity-forecast/import/route.ts`
  - Responsavel por: validar XLSX de atividades previstas, resolver projeto/atividade no tenant, ignorar codigos ja existentes e inserir o lote por projeto sem depender das RPCs legadas `precheck_project_activity_forecast_import` e `append_project_activity_forecast`.
- Caminho: `src/app/api/projects/activity-forecast/catalog/route.ts`
  - Responsavel por: pesquisar atividades ativas do tenant para incluir no previsto do projeto.
- Caminho: `supabase/edge_functions/get_project_forecast_template/index.ts`
  - Responsavel por: autenticar usuario e entregar o modelo XLSX oficial.
- Caminho: `supabase/edge_functions/import_project_forecast/index.ts`
  - Responsavel por: validar planilha e importar materiais previstos via RPC de protecao.
- Caminho: `supabase/edge_functions/get_project_activity_forecast_template/index.ts`
  - Responsavel por: autenticar usuario e entregar o modelo XLSX oficial de atividades previstas.
- Caminho: `supabase/edge_functions/import_project_activity_forecast/index.ts`
  - Responsavel por: fluxo legado de importacao de atividades previstas via RPCs de protecao.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - Responsavel por: `PAGE_SIZE`, `INITIAL_FORM`, `INITIAL_FILTERS` e `HISTORY_FIELD_LABELS`.
- Caminho: `supabase/migrations/029_create_project_table.sql`
  - Responsavel por: tabela `project`, indices, policies RLS e trigger de auditoria.
- Caminho: `supabase/migrations/030_project_sob_priority_rules.sql`
  - Responsavel por: normalizacao de `SOB` e unicidade case-insensitive por tenant.
- Caminho: `supabase/migrations/031_create_project_lookup_tables.sql`
  - Responsavel por: normalizar e disponibilizar tabelas de dominio do formulario de projetos.
- Caminho: `supabase/migrations/034_use_people_for_project_contractor_responsible.sql`
  - Responsavel por: remover tabela `project_contractor_responsibles` e vincular `project.contractor_responsible_id` a `people`.
- Caminho: `supabase/migrations/036_create_project_history_and_cancellation.sql`
  - Responsavel por: adicionar status ativo em `project` e criar `project_history` e `project_cancellation_history`.
- Caminho: `supabase/migrations/037_project_activation_history_rules.sql`
  - Responsavel por: permitir `ACTIVATE` no `project_history` e adicionar `action_type` no `project_cancellation_history`.
- Caminho: `supabase/migrations/038_project_lookup_uuid_columns.sql`
  - Responsavel por: migrar colunas de dominio da `project` para UUID e criar a view `project_with_labels` para listas e detalhes.
- Caminho: `supabase/migrations/041_create_project_material_forecast.sql`
  - Responsavel por: tabela `project_material_forecast`, policies RLS, trigger de auditoria e constraint unica por material no projeto.
- Caminho: `supabase/migrations/064_create_project_activity_forecast.sql`
  - Responsavel por: tabela `project_activity_forecast`, RPC `save_project_activity_forecast` e alteracao do bootstrap da `Locacao` para seed de atividades.
- Caminho: `supabase/migrations/043_project_forecast_import_guards.sql`
  - Responsavel por: RPC de pre-check e RPC de append para proteger importacoes contra duplicidade.
- Caminho: `supabase/migrations/065_project_forecast_manual_and_activity_import.sql`
  - Responsavel por: RPC `save_project_material_forecast` e RPCs de pre-check/importacao de `Atividades previstas`.
- Caminho: `supabase/migrations/066_harden_location_and_project_forecast_rpcs.sql`
  - Responsavel por: endurecer as RPCs de previstos do projeto com concorrencia por `updated_at` e limite maximo de quantidade.
- Caminho: `supabase/migrations/077_create_admin_write_rpcs.sql`
  - Responsavel por: criar `save_project_record` e `set_project_record_status` para escrita transacional da tela principal de projetos.
- Caminho: `supabase/migrations/118_remove_project_sob_priority_format_constraint.sql`
  - Responsavel por: remover a constraint `chk_project_sob_priority_format`, liberando `SOB` sem padrao de prefixo por prioridade.
- Caminho: `supabase/migrations/150_add_project_is_test_and_status_filters.sql`
  - Responsavel por: adicionar `project.is_test`, republicar `project_with_labels` e evoluir a RPC `save_project_record` com suporte ao marcador de obra de teste.
- Caminho: `supabase/migrations/174_add_project_is_withdrawn.sql`
  - Responsavel por: adicionar `project.is_withdrawn`, republicar `project_with_labels` e evoluir a RPC `save_project_record` com suporte ao marcador `RETIRADO DA CARTEIRA`.
- Caminho: `supabase/migrations/045_create_tenants_and_user_tenant_access.sql`
  - Responsavel por: criar entidade `tenants`, vinculo `app_user_tenants` e atualizar `user_can_access_tenant`.
- Caminho: `supabase/migrations/046_add_tenant_fk_to_all_tenant_tables.sql`
  - Responsavel por: aplicar integridade referencial de `tenant_id` para `tenants(id)` em todas as tabelas de negocio com coluna `tenant_id`.
- Caminho: `supabase/migrations/060_add_project_has_locacao.sql`
  - Responsavel por: adicionar `has_locacao` em `project`, backfill a partir de `project_location_plans` e republicar `project_with_labels`.

Funcoes utilitarias relacionadas
- Caminho: `src/lib/server/appUsersAdmin.ts`
  - Responsavel por: resolver sessao autenticada e contexto de tenant para as rotas API.
  - Suporta selecao de tenant por requisicao via header `x-tenant-id` (quando o usuario possui vinculo em `app_user_tenants`).
- Caminho: `src/hooks/useAuth.ts`
  - Responsavel por: expor `accessToken` para chamadas autenticadas no frontend.
- Caminho: `src/lib/server/concurrency.ts`
  - Responsavel por: normalizar e comparar `expectedUpdatedAt` por instante, mantendo bloqueio de concorrencia real e removendo falso conflito por formato textual.
- Caminho: `supabase/edge_functions/import_project_forecast/index.ts`
  - Responsavel por: validar parse do XLSX (`projeto`, `codigo`, `quantidade`), gerar erros por linha/coluna e preparar payload por projeto para RPC.
- Caminho: `supabase/edge_functions/import_project_activity_forecast/index.ts`
  - Responsavel por: validar parse do XLSX (`projeto`, `codigo`, `quantidade`), gerar erros por linha/coluna e preparar payload por projeto para RPC de atividades.

Servicos e integracoes externas
- Supabase Postgres com RLS.
- Next.js Route Handlers (`/api/projects`, `/api/projects/meta`, `/api/projects/forecast`).
- Supabase Edge Functions (`get_project_forecast_template`, `import_project_forecast`, `get_project_activity_forecast_template`, `import_project_activity_forecast`).

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/api/projects/route.ts`
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `docs/Tela_Projetos_SaaS.txt`
  - `README.md`
  - `TASKS.md`
  - `src/modules/dashboard/projetos/ProjectsPageView.module.css`
  - `src/app/api/projects/forecast/route.ts`
  - `src/app/api/projects/forecast/catalog/route.ts`
  - `src/app/api/projects/activity-forecast/route.ts`
  - `src/app/api/projects/activity-forecast/catalog/route.ts`
  - `src/lib/server/concurrency.ts`
  - `src/app/api/projects/forecast/template/route.ts`
  - `supabase/edge_functions/get_project_forecast_template/index.ts`
  - `supabase/edge_functions/import_project_forecast/index.ts`
  - `supabase/edge_functions/get_project_activity_forecast_template/index.ts`
  - `supabase/edge_functions/import_project_activity_forecast/index.ts`
  - `supabase/migrations/041_create_project_material_forecast.sql`
  - `supabase/migrations/064_create_project_activity_forecast.sql`
  - `supabase/migrations/043_project_forecast_import_guards.sql`
  - `supabase/migrations/065_project_forecast_manual_and_activity_import.sql`
  - `supabase/migrations/066_harden_location_and_project_forecast_rpcs.sql`
  - `supabase/migrations/077_create_admin_write_rpcs.sql`
  - `supabase/migrations/081_add_jsonb_object_length_compat.sql`
  - `supabase/migrations/045_create_tenants_and_user_tenant_access.sql`
  - `src/lib/server/appUsersAdmin.ts`
- Funcoes/hooks/constantes tocadas:
  - `getFobRuleError`
  - `ProjectsPageView`, `openForecastTab`, `loadForecast`, `handleForecastProjectSelection`, `handleForecastProjectSearchChange`, `addProjectForecast`, `saveProjectForecastRow`, `applyForecastFilters`, `clearForecastFilters`, `handleDownloadForecastTemplate`, `submitForecastImport`
  - `openActivityForecastTab`, `loadActivityForecast`, `handleActivityForecastProjectSelection`, `handleActivityForecastProjectSearchChange`, `addProjectActivityForecast`, `saveProjectActivityForecastRow`, `handleDownloadActivityForecastTemplate`, `submitActivityForecastImport`, `handleExportActivityForecastItems`
  - `save_project_material_forecast`
  - `save_project_activity_forecast`
  - `parseProjectInput`
  - `validateRequiredProjectFields`
  - `buildProjectWritePayload`
  - `buildProjectUpdateChanges`
  - `editingProjectUpdatedAt`
  - `normalizeExpectedUpdatedAt`, `hasUpdatedAtConflict`, `buildConcurrencyConflictResponse`
  - `saveProjectViaRpc`
  - `setProjectStatusViaRpc`
  - `fetchProjectsSummaryCompat`
  - Mapeamento de `hasLocacao` no retorno de `/api/projects`
  - Mapeamento de `summary.totalProjects` e `summary.completed` no retorno de `/api/projects`
  - `projectListSummary`
  - `INITIAL_PROJECT_LIST_SUMMARY`
  - `GET`, `POST` e `PUT` de `/api/projects/forecast`
  - `GET` de `/api/projects/forecast/catalog`
  - `GET`, `POST` e `PUT` de `/api/projects/activity-forecast`
  - `GET` de `/api/projects/activity-forecast/catalog`
  - `GET` de `/api/projects/forecast/template` bloqueado para forcar download via Edge Function
  - `serve` de `get_project_forecast_template`, `import_project_forecast`, `get_project_activity_forecast_template` e `import_project_activity_forecast`
  - `PAGE_SIZE`, `INITIAL_FORM`, `INITIAL_FILTERS`, `HISTORY_FIELD_LABELS`
  - Antes: comparacao de `updated_at` em `Projetos` era textual no backend (`string`), podendo bloquear save com timestamps equivalentes em formatos diferentes.
  - Depois: comparacao de `updated_at` passa a usar o instante da data (epoch), mantendo o lock concorrente real e liberando edicoes validas.
  - Antes: falha de assinatura/execucao da RPC de save podia retornar somente erro generico `Falha ao salvar projeto.`.
  - Depois: o save da tela tenta assinatura legada sem `p_fob` quando necessario e retorna detalhes tecnicos quando a RPC falha de fato.
  - Antes: ambientes Postgres sem `jsonb_object_length(jsonb)` quebravam o save da RPC com erro de funcao inexistente.
  - Depois: migration de compatibilidade adiciona `public.jsonb_object_length(jsonb)` e elimina essa quebra.

Atualizacao adicional 2026-03-24
--------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
- Funcoes/hooks/constantes tocadas:
  - `buildProjectsCsv`
  - `buildForecastCsv`
  - `buildActivityForecastCsv`
- Comportamento antes/depois:
  - Antes: os CSVs de `Projetos`, `Materiais previstos` e `Atividades previstas` exportavam apenas subconjuntos de colunas.
  - Depois: os CSVs passam a incluir campos obrigatorios de cadastro de projeto e metadados extras operacionais/auditoria disponiveis (`Status`, `Registrado por/em`, `Atualizado por/em`, `Origem`, `Importado em`, `Criado em`, `Atualizado em`).
  - Depois: modal de detalhes e CSV de Projetos aplicam fallback `Nao identificado` para nomes de auditoria vazios.

Atualizacao adicional 2026-03-26
--------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/route.ts`
  - `supabase/migrations/118_remove_project_sob_priority_format_constraint.sql`
  - `docs/Tela_Projetos_SaaS.txt`
- Funcoes/hooks/constantes tocadas:
  - `handleSubmit`
  - `validateRequiredProjectFields`
  - `parseProjectInput`
  - `normalizeSob`
  - `chk_project_sob_priority_format` (constraint de banco removida)
- Comportamento antes/depois:
  - Antes: `Projeto (SOB)` era bloqueado por formato baseado em `Prioridade` (ex.: `A`, `ZX`, `FS`) e por limite de `10` caracteres no frontend/API.
  - Depois: `Projeto (SOB)` aceita texto livre (normalizado para maiusculas), sem validacao de prefixo por prioridade e sem limite fixo de 10 caracteres na tela/API.
  - Depois: a constraint de banco `chk_project_sob_priority_format` foi removida para manter o comportamento consistente ponta a ponta.

Atualizacao adicional 2026-04
-----------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/modules/dashboard/projetos/ProjectsPageView.module.css`
  - `src/app/api/projects/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `fetchProjectsSummaryCompat`
  - `projectListSummary`
  - `INITIAL_PROJECT_LIST_SUMMARY`
- Comportamento antes/depois:
  - Antes: a tela de `Projetos` nao mostrava consolidado visual de carteira x concluidas.
  - Depois: a tela passa a exibir o bloco `Status da Carteira de Projetos` no topo (acima de `Cadastro de Projeto`) com 2 cards (`Total de projetos` e `Concluidas`).
  - Antes: o frontend so tinha acesso a lista paginada e ao total geral.
  - Depois: `GET /api/projects` retorna `summary.totalProjects` e `summary.completed`; ambos contam apenas projetos ativos por `SOB` unico, e `Concluidas` considera `work_completion_status = CONCLUIDO` na tabela `project_programming`.

Atualizacao 2026-04-13
----------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/modules/dashboard/projetos/ProjectsPageView.module.css`
  - `src/app/api/projects/route.ts`
  - `supabase/migrations/150_add_project_is_test_and_status_filters.sql`
  - `docs/Tela_Projetos_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `parseProjectInput`
  - `buildProjectWritePayload`
  - `buildProjectUpdateChanges`
  - `saveProjectViaRpc`
  - `fetchProjectsPageCompat`
  - `fetchProjectsSummaryCompat`
  - `normalizeProjectStatusFilter`
  - `INITIAL_FORM`
  - `INITIAL_FILTERS`
  - `HISTORY_FIELD_LABELS`
- Comportamento antes/depois:
  - Antes: nao existia marcador de obra de teste no cadastro de projetos.
  - Depois: o formulario de `Projetos` passa a ter `Obra de teste`, com tag visual na lista e exibicao no modal de detalhes.
  - Antes: a lista de `Projetos` nao tinha filtro por status operacional.
  - Depois: a lista passa a aceitar filtro `Status` com `Cancelado`, `Ativo` e `Concluido`.
  - Antes: os cards de carteira consideravam obras de teste junto com obras operacionais.
  - Depois: o resumo (`Total de projetos` e `Concluidas`) passa a excluir obras de teste no backend.

Atualizacao adicional 2026-04-16 (filtros por Estado Trabalho e Tipo SGD)
---------------------------------------------------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/meta/route.ts`
  - `src/app/api/projects/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `INITIAL_FILTERS`
  - `buildQuery`
  - `loadMeta`
  - `normalizeProjectWorkCompletionStatusFilter`
  - `normalizeUuid`
  - `fetchProjectIdsByProgrammingFiltersCompat`
  - `fetchProjectsPageCompat`
  - `fetchProjectsSummaryCompat`
- Comportamento antes/depois:
  - Antes: a tela de `Projetos` filtrava por `Status` da obra (`Cancelado`, `Ativo`, `Concluido`) e nao tinha filtro de `Tipo SGD`.
  - Depois: o filtro principal passa a ser `Estado Trabalho` (catalogo por tenant + `Nao informado`) e a tela recebe filtro adicional `Tipo SGD`.
  - Antes: o backend de `GET /api/projects` nao cruzava `sgd_type_id` nem o catalogo de Estado Trabalho na tabela `project_programming`.
  - Depois: o backend aplica os dois filtros via programacao (`work_completion_status_id`/fallback legado de texto e `sgd_type_id`) antes de paginar e consolidar os cards.
  - Antes: `GET /api/projects/meta` nao devolvia catalogo de `Estado Trabalho` e `Tipo SGD`.
  - Depois: a rota de meta devolve `workCompletionCatalog` e `sgdTypes` por tenant para alimentar os selects da tela.

Atualizacao 2026-05
-------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/modules/dashboard/projetos/ProjectsPageView.module.css`
  - `src/app/api/projects/route.ts`
  - `supabase/migrations/174_add_project_is_withdrawn.sql`
  - `supabase/migrations/README.txt`
  - `docs/Tela_Projetos_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `parseProjectInput`
  - `buildProjectWritePayload`
  - `buildProjectUpdateChanges`
  - `saveProjectViaRpc`
  - `fetchProjectsPageCompat`
  - `fetchProjectsSummaryCompat`
  - `INITIAL_FORM`
  - `HISTORY_FIELD_LABELS`
- Comportamento antes/depois:
  - Antes: a classificacao do cadastro tinha apenas `Obra de teste`.
  - Depois: o cadastro passa a ter tambem `RETIRADO DA CARTEIRA`, persistido em `project.is_withdrawn`.
  - Antes: retirar uma obra dos cards exigiria reaproveitar `is_test`, com impacto em Programacao/Medicao.
  - Depois: `RETIRADO DA CARTEIRA` exclui apenas os cards de carteira da tela de Projetos, mantendo a obra disponivel nos demais fluxos operacionais.

Atualizacao adicional 2026-05-11
--------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `FilterState`
  - `INITIAL_FILTERS`
  - `buildQuery`
  - `fetchProjectsPageCompat`
  - `fetchProjectsSummaryCompat`
  - `GET /api/projects`
- Comportamento antes/depois:
  - Antes: os filtros da tela de `Projetos` nao permitiam recorte por `Centro de Servico`.
  - Depois: o bloco `Filtros` exibe `Centro de Servico *`, alimentado por `meta.serviceCenters`, e envia `serviceCenter` para `/api/projects`.
  - Depois: o backend aplica `service_center_text` dentro do mesmo `tenant_id` na listagem paginada, exportacao CSV e consolidacao dos cards da carteira.

Atualizacao adicional 2026-05-20
--------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/modules/dashboard/projetos/ProjectsPageView.module.css`
  - `supabase/edge_functions/get_project_forecast_template/index.ts`
  - `supabase/edge_functions/import_project_forecast/index.ts`
  - `supabase/edge_functions/get_project_activity_forecast_template/index.ts`
  - `supabase/edge_functions/import_project_activity_forecast/index.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `ProjectForecastImportResponse`
  - `buildImportErrorReport`
  - `openForecastImportModal`
  - `openActivityForecastImportModal`
  - `submitForecastImport`
  - `submitActivityForecastImport`
  - `parseWorkbook` das Edge Functions de importacao
  - `buildWorkbookArrayBuffer` das Edge Functions de modelo
- Comportamento antes/depois:
  - Antes: a importacao em massa de `Materiais previstos` e `Atividades previstas` exigia projeto selecionado na tela e aceitava apenas `codigo` e `quantidade`.
  - Depois: o XLSX passa a aceitar `projeto`, `codigo` e `quantidade`, permitindo importar itens de varios projetos no mesmo arquivo.
  - Antes: erros da importacao eram exibidos apenas no feedback resumido da tela.
  - Depois: erros de validacao retornam estrutura por linha/coluna e a tela gera CSV de erros no padrao `linha;coluna;valor;erro`.
  - Depois: a validacao continua restrita ao `tenant_id`, resolvendo `project.sob`, `materials.codigo` e `service_activities.code` dentro do tenant autenticado.
  - Depois: o acionamento visual da importacao passa a seguir o padrao das demais telas, com botao e modal nomeados como `Cadastro em massa`.
  - Depois: o botao externo `Baixar modelo (.xlsx)` foi removido das abas; o download do modelo fica apenas dentro do modal `Cadastro em massa`.
  - Depois: codigos ja existentes no projeto deixam de bloquear a importacao inteira; a linha e ignorada, entra no CSV de erros/avisos e os codigos novos sao cadastrados.
  - Depois: a selecao de `Projeto (SOB)` nas abas de previstos passa a usar `meta.sobCatalog` e buscar o ID via `/api/projects` quando o projeto nao esta na pagina atual da lista principal, evitando lista vazia para SOB valido fora da pagina.

Atualizacao adicional 2026-06
-----------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `parseEstimatedValue`
  - `formatEstimatedValueInput`
  - `normalizeEstimatedValueInput`
  - `handleSubmit`
  - `normalizeEstimatedValue`
- Comportamento antes/depois:
  - Antes: o campo `Valor estimado` usava `input type=number` com `step=0.01`, bloqueando valores digitados com separador brasileiro ou com ponto de milhar, como `22.03533` para representar `R$ 22.035,33`.
  - Depois: o campo da tela usa entrada textual com `inputMode=decimal`, formata no blur para `pt-BR` e envia valor decimal com 2 casas para a API.
  - Antes: a API substituia apenas virgula por ponto, fazendo `22.035,33` virar valor invalido.
  - Depois: a API aceita `22.035,33`, `22.03533`, `22035,33` e `22035.33`, normalizando para `22035.33` antes de persistir.
  - Depois: o banco permanece sem alteracao de schema, mantendo `project.estimated_value` como `numeric(14,2)` por tenant.

Atualizacao 2026-06 - fiscalizacao cruzada com Programacao
-----------------------------------------------------------
- Arquivos alterados/criados:
  - `src/server/modules/projects/authorization.ts`
  - rotas `src/app/api/projects`
  - `supabase/edge_functions/_shared/page_authorization.ts`
  - Edge Functions de importacao de previstos
  - `supabase/migrations/233_harden_projects_programming_cross_flow.sql`
- Comportamento antes:
  - Projeto podia ser inativado enquanto outra transacao criava Programacao;
  - RPCs/tabelas criticas aceitavam chamada direta de authenticated;
  - rotas auxiliares e imports nao validavam page key `projetos`;
  - fallback de assinatura podia repetir chamada depois de erro real.
- Comportamento depois:
  - Projeto e Programacao compartilham lock por tenant + projeto;
  - Projeto inativo nao recebe agenda e Projeto com agenda pendente nao e inativado;
  - `PROGRAMADA`, `REPROGRAMADA` e `ADIADA` seguem a mesma regra;
  - historicos e previstos usam FK composta com tenant;
  - APIs e imports validam page/action server-side;
  - escrita direta de authenticated foi removida no escopo auditado;
  - fallback ocorre apenas quando a assinatura da RPC estiver ausente.
- Nenhuma policy RLS ou permissao de `DELETE` foi criada.

Atualizacao 2026-07-04 - Modal compartilhado de exportacao:
- Arquivo alterado: `src/modules/dashboard/projetos/ProjectsPageView.tsx`.
- Funcoes/estado tocados: botoes de exportacao de lista, materiais previstos e atividades previstas; estados `isExporting*`.
- Antes: exportacoes usavam botoes locais sem o modal padrao.
- Depois: exportacoes usam `CsvExportButton`, herdando `ExportProgressModal`.
- Multi-tenant: sem mudanca em API, query, schema, RLS ou permissao.

Atualizacao adicional 2026-07 - selecao de previstos por datalist
------------------------------------------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/activity-forecast/import/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `normalizeCatalogSelection`
  - `findProjectForecastCatalogItem`
  - `findProjectActivityForecastCatalogItem`
  - `handleForecastSearchChange`
  - `handleActivityForecastSearchChange`
  - `buildImportErrorReport`
  - `isImportResponseFailure`
  - `submitForecastImport`
  - `submitActivityForecastImport`
  - `POST /api/projects/activity-forecast/import`
  - estados `selectedProjectForecastOption` e `selectedProjectActivityForecastOption`
- Comportamento antes/depois:
  - Antes: ao selecionar um item no `datalist`, o texto completo `codigo - descricao` disparava nova busca; quando a API nao retornava essa string composta, o catalogo local era esvaziado e a inclusao manual falhava como item invalido.
  - Depois: a opcao selecionada fica persistida separadamente do resultado da busca, aceita comparacao normalizada por `codigo` ou `codigo - descricao`, preenche a unidade/UMB e permite adicionar o previsto selecionado.
  - Depois: troca de projeto, limpeza do campo e inclusao bem-sucedida limpam a selecao para evitar reaproveitamento indevido.
  - Antes: `Cadastro em massa` podia receber HTTP 207 de importacao parcial e ainda assim ser tratado pelo frontend como sucesso, sem destacar `projectsFailed` no CSV de erros.
  - Depois: respostas `success=false` ou `partial=true` sao tratadas como falha parcial, mantem o modal aberto, mostram os projetos com erro no feedback e adicionam `projectsFailed` ao CSV.
  - Depois: quando existe projeto selecionado na aba, a lista e recarregada mesmo em importacao parcial para refletir linhas que foram inseridas com sucesso.
  - Antes: o Cadastro em massa de `Atividades previstas` dependia da Edge Function `import_project_activity_forecast` e das RPCs `precheck_project_activity_forecast_import`/`append_project_activity_forecast`, que ja haviam apresentado `PGRST202` no remoto.
  - Depois: a tela envia o XLSX para `/api/projects/activity-forecast/import`, que valida tenant/permissao no servidor, resolve projeto/atividade por tenant e insere os itens por projeto com `created_by`/`updated_by`.
- Multi-tenant: a nova rota deriva `tenant_id` e usuario da sessao, valida permissao `projetos/create`, filtra projeto e atividade por tenant e grava com service role no backend controlado; sem mudanca de schema/RLS.

Atualizacao adicional 2026-07 - valor previsto das atividades
--------------------------------------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/activity-forecast/route.ts`
- Comportamento antes/depois:
  - Antes: a lista de `Atividades previstas` mostrava unidade, valor unitario e quantidade, mas nao mostrava os pontos nem o valor financeiro previsto da linha.
  - Depois: a API devolve `voicePoint` e `totalValue`, e a tela/CSV exibem `Pontos` e `Valor previsto`.
  - Depois: o valor previsto da linha segue a formula da Medicao com taxa `1`: `voicePoint x qtyPlanned x unitValue x 1`.
- Multi-tenant: sem mudanca de schema, RLS ou permissao; a rota continua derivando `tenant_id` da sessao.

Atualizacao adicional 2026-08 - filtros de carteira e Tipo de Servico
----------------------------------------------------------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/projetos/ProjectsPageView.tsx`
  - `src/app/api/projects/route.ts`
  - `docs/Tela_Projetos_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `FilterState`
  - `INITIAL_FILTERS`
  - `buildQuery`
  - `normalizeProjectPortfolioStatusFilter`
  - `fetchProjectsPageCompat`
  - `fetchProjectsSummaryCompat`
  - `GET /api/projects`
- Comportamento antes/depois:
  - Antes: a lista tinha apenas o checkbox `Somente cancelados` e nao filtrava diretamente por `Tipo de Servico`, `RETIRADO DA CARTEIRA` ou `TERCEIROS`.
  - Depois: o bloco `Filtros` troca o checkbox por `Situacao da carteira`, com `Cancelados`, `Retirados da carteira` e `Terceiros` no mesmo select, e adiciona o select `Tipo de Servico`.
  - Depois: `/api/projects` recebe `portfolioStatus` e `serviceType`, aplica ambos no banco antes da paginacao/exportacao e preserva compatibilidade com o parametro legado `canceledOnly=true`.
  - Depois: os cards do topo passam a descrever o resumo como calculado conforme os filtros atuais, mantendo exclusao de obras de teste.
- Multi-tenant: sem mudanca de schema, RLS ou permissao; todos os filtros continuam sob `tenant_id` derivado da sessao.


Atualizacao 2026-07-29 - Fase 3b do corte: Projetos e os tres dashboards
--------------------------------------------------------------------------
- Quatro consumidores deixaram de ler `project_programming` e passaram a ler
  `programming` pela fachada `@/server/modules/programacao-normalizada`:
  * `src/app/api/projects/route.ts` (resumo "concluidas", filtro por Estado do
    Trabalho/Tipo de SGD, guarda de inativacao de projeto);
  * `src/server/modules/dashboard-measurement/controller.ts` (timeline);
  * `src/server/modules/dashboard-portfolio/controller.ts` (timeline);
  * `src/app/api/dash-operacional-faturamento/route.ts` (ultimo Estado do Trabalho).
- Com isso, dos 7 consumidores mapeados no inicio do corte sobram 2:
  `src/app/api/mapa-programacao/route.ts` (Fase 4) e `src/app/api/medicao/route.ts`
  (Fase 5, junto com Controle APR).
- Funcoes novas na fachada: `fetchWorkCompletionTimelineByProject`,
  `fetchProjectIdsWithCompletedWork`, `fetchProjectIdsByProgrammingFilter`,
  `countActiveStagesForProject` e `toCanonicalWorkCompletionCode`.

SIMPLIFICACAO QUE VEIO DE GRACA
- A leitura antiga de Projetos consultava por DOIS caminhos: ids do catalogo em
  `work_completion_status_id` e texto em `work_completion_status`, com deteccao de
  schema legado sem a coluna de id (`isMissingWorkCompletionStatusIdColumnError`).
  `programming` nao tem coluna de id — `work_completion_status` guarda o CODIGO e a
  FK e por codigo (migration 310). Os dois caminhos, o fallback e ~120 linhas de
  compatibilidade sairam junto com a troca de fonte.

DUAS QUEBRAS SILENCIOSAS ENCONTRADAS E CORRIGIDAS
- `PENDENCIA` no filtro de Estado do Trabalho da tela Projetos: o codigo continua
  ATIVO no catalogo e por isso e oferecido no filtro, mas a migration 318 tirou o
  valor de status E de Estado do Trabalho e transformou na flag `is_pendencia`.
  Comparar pelo codigo devolveria ZERO projeto (13 -> 0 na medicao), um "nenhum
  resultado" falso que parece boa noticia. A fachada passa a traduzir esse filtro
  para `is_pendencia = true`. Medido depois da correcao: 14 projetos (13 legados +
  1 marcado na tela nova desde o corte).
- `NAO_INFORMADO` passou a exigir tambem `is_pendencia = false`: etapa em pendencia
  nao e "sem informacao", so guarda a informacao em outra coluna.
- Os dois codigos de catalogo que significam o mesmo estado
  (`PARCIAL_PLANEJADO_BENFICIO_ATINGIDO`, legado com typo, e `BENEFICIO_ATINGIDO`,
  correto pela 310) sao reconciliados por `toCanonicalWorkCompletionCode`. Antes,
  escolher `BENEFICIO_ATINGIDO` no filtro devolvia 0 projetos; agora as duas opcoes
  devolvem os mesmos 10.

MUDANCA DELIBERADA DE COMPORTAMENTO: DASH OPERACIONAL FATURAMENTO
- Esta tela era a divergente das cinco que calculavam "ultimo Estado do Trabalho por
  projeto": NAO excluia etapa CANCELADA e ordenava so por `updated_at`, ignorando
  `execution_date`. Um apontamento antigo, editado depois, vencia o mais recente, e
  etapa cancelada podia definir o estado do projeto.
- Agora usa o criterio unico da fachada (ignora CANCELADA; ultimo = maior
  `execution_date`, desempate por `updated_at`). O dashboard MUDA DE NUMERO em 26
  projetos e passa a concordar com Cronograma, Projetos e os outros dashboards.
- A maioria das mudancas e o legado estando desatualizado: a tela nova ja avancou o
  estado para CONCLUIDO e `project_programming` ficou parado, porque o merge
  conservador da 335 nao sobrescreveu o destino de proposito.

IMPACTO MEDIDO EM PRODUCAO (2026-07-29)
- Dash Operacional Faturamento: 26 projetos mudam de Estado do Trabalho exibido;
  total com estado vai de 112 para 111.
- Projetos, resumo "concluidas": 56 -> 61 (5 projetos que a tela nova concluiu e o
  legado nao registrou, mais A053714340 que sai — ver abaixo).
- Projetos, filtro por Estado do Trabalho: BENEFICIO_ATINGIDO 0 -> 10,
  CONCLUIDO 56 -> 61, PENDENCIA 13 -> 14 (apos a correcao),
  PARCIAL_PLANEJADO_BENFICIO_ATINGIDO 10 -> 10, RETIRADO 1 -> 1.
- Projetos, guarda de inativacao: 138 -> 141 projetos bloqueados. Os tres novos
  (2307202601, CT0324197309, A055472265) tem etapa em aberto criada na tela nova,
  que o legado nunca conheceu. A guarda passa a refletir a realidade atual.
- A053714340 continua sendo o unico caso que PERDE estado concluido, pelo motivo ja
  registrado na Fase 3a: a carga 315/335 fundiu duas linhas da mesma data e o
  vencedor foi a ADIADA sem estado. Segue pendente de decisao do usuario.

- Arquivos alterados:
  - `src/server/modules/programacao-normalizada/queries.ts`
  - `src/server/modules/programacao-normalizada/index.ts`
  - `src/app/api/projects/route.ts`
  - `src/app/api/dash-operacional-faturamento/route.ts`
  - `src/server/modules/dashboard-measurement/controller.ts`
  - `src/server/modules/dashboard-portfolio/controller.ts`

Atualizacao 2026-08-15 - Lock canonico por projeto (Cancelar x Programacao/APR)
--------------------------------------------------------------------------------------------
- Problema (Auditoria-Concorrencia/2026-08-15-relatorio.md, achado CRITICO #4 e MEDIO
  #5): `set_project_record_status` (cancelar/ativar projeto) trava a linha de `project`
  com `for update`, mas sua re-checagem interna de "programacao pendente" consultava
  `public.project_programming` — a tabela LEGADA, sem escrita viva desde o corte Fase
  3b/3a acima. O guard real era so o pre-check em JS (`countActiveStagesForProject`, ja
  correto contra `programming`), que roda ANTES da RPC e sem lock nenhum — e as RPCs de
  criar/reabrir etapa (Programacao Normalizada) so tomam um advisory lock com chave
  diferente. As duas operacoes nao se bloqueavam: cancelar o projeto e criar/reabrir uma
  etapa quase ao mesmo tempo podia terminar com projeto CANCELADO e etapa PROGRAMADA
  ativa. `save_project_apr_control` tinha o mesmo problema pela outra ponta: lia
  `project.is_active` sem lock nenhum antes de criar/editar APR.
- Correcao: `supabase/migrations/370_project_programming_apr_shared_lock.sql` estende o
  MESMO lock canonico ja usado pelas RPCs de Programacao Normalizada
  (`pg_advisory_xact_lock(hashtextextended(tenant_id::text || ':' || project_id::text,
  0))`, migrations 318/322/337/340/369) para `set_project_record_status` (acao CANCEL) e
  `save_project_apr_control`, e troca a re-checagem interna de `set_project_record_status`
  de `project_programming` para `programming`, incluindo o status `REPROGRAMADA` que
  faltava. As 4 operacoes (cancelar projeto, criar/reabrir etapa, criar/editar APR) ficam
  mutuamente exclusivas por `(tenant, project)`, sem RPC nova nem mudanca de contrato.
- Achado registrado, NAO corrigido nesta migration: `233_harden_projects_programming_cross_flow.sql`
  ja criou 2 triggers (`enforce_programming_active_project`,
  `prevent_project_inactivation_with_active_programming`) com advisory lock proprio para
  essa mesma fronteira, mas vigiando `project_programming` (legada) — continuam
  instalados e ativos, so que sem efeito pratico hoje porque a tabela que vigiam nao
  recebe mais escrita. Redirecionar ou remover esses triggers fica para uma investigacao
  separada (confirmar antes que nada mais depende do comportamento atual sobre a tabela
  legada).
- Arquivos alterados: `supabase/migrations/370_project_programming_apr_shared_lock.sql`.
- Validado por leitura estrutural do SQL (mesmo padrao ja usado nas migrations 318/322/
  337/340/369/350); `npm run db:lint`/`db:migration-list` bloqueados neste ambiente por
  403 de permissao de conta no CLI (limitacao pre-existente). Validacao manual pendente:
  duas abas, cancelar projeto numa e criar/reabrir etapa (ou criar APR) na outra quase ao
  mesmo tempo — confirmar que uma espera a outra e a que perde recebe erro de negocio
  coerente, nunca as duas com sucesso.

Atualizacao 2026-08-15 (2) - Formulario nao e mais apagado em conflito de concorrencia
--------------------------------------------------------------------------------------------
- Mesmo achado transversal (ponto 7 da auditoria de concorrencia): ao receber 409
  `CONCURRENT_MODIFICATION`/`RECORD_INACTIVE`/`STATUS_ALREADY_CHANGED`,
  `ProjectsPageView.tsx` chamava `resetFormState()` automaticamente nos 2 fluxos de
  escrita (salvar, cancelar/ativar) antes do usuario decidir o que fazer.
- Depois: `resetFormState()` so roda apos sucesso; a lista continua sendo recarregada,
  mas o formulario fica preenchido e a mensagem de erro fica visivel.
- Arquivos alterados: `src/modules/dashboard/projetos/ProjectsPageView.tsx`.
