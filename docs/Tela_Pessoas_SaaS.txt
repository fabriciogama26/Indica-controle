Tela: Pessoas SaaS

Visao geral
-----------
- Tela para cadastro e manutencao de pessoas operacionais por tenant.
- Permite cadastrar, editar, consultar, cancelar/ativar e auditar alteracoes.
- Consome catalogos de `Cargo`, `Tipo por Cargo` e `Nivel`.
- Segue o mesmo padrao visual de `Projetos`, `Atividades` e `Equipes`.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/pessoas/page.tsx`
- View: `src/modules/dashboard/pessoas/PeoplePageView.tsx`
- Estilo: `src/modules/dashboard/pessoas/PeoplePageView.module.css`
- API principal: `src/app/api/people/route.ts`
- API de metadados: `src/app/api/people/meta/route.ts`
- Banco (base people/job_titles): `supabase/migrations/014_create_people_job_titles.sql`
- Banco (matricula): `supabase/migrations/035_add_matriculation_to_people.sql`
- Banco (tipos por cargo): `supabase/migrations/047_create_job_title_types_and_people_type_link.sql`
- Banco (niveis): `supabase/migrations/048_create_job_levels_and_people_level_link.sql`
- Banco (historico generico): `supabase/migrations/051_create_app_entity_history_and_activity_status.sql`
- Banco (status/cancelamento people): `supabase/migrations/055_add_people_status_and_cancellation_fields.sql`
- Banco (bloqueio de duplicidade people): `supabase/migrations/056_prevent_people_duplicate_identity.sql`
- Banco (RPC transacional de escrita/status): `supabase/migrations/079_create_people_and_invite_write_rpcs.sql`

Cadastro
--------
- Campos obrigatorios:
  - `Nome`
  - `Matricula`
  - `Cargo`
  - `Tipo`
- Campos opcionais:
  - `Nivel`
- Regras:
  - `Tipo` so lista opcoes vinculadas ao `Cargo` selecionado.
  - `Nivel` precisa existir em `job_levels` ativo no tenant.
  - `Tipo` precisa existir em `job_title_types` ativo no tenant e no mesmo cargo.
  - Bloqueio de duplicidade por combinacao: `Nome + Matricula + Cargo + Tipo + Nivel` no mesmo tenant.
  - Edicao de registro inativo e bloqueada ate reativacao.
  - A edicao e a troca de status exigem `expectedUpdatedAt` para bloquear sobrescrita silenciosa.
- Fluxos:
  - `POST /api/people` para cadastro.
  - `PUT /api/people` para edicao.
  - `PATCH /api/people` para cancelamento/ativacao com motivo obrigatorio.

Filtros, auto-complete e listagem
---------------------------------
- Filtros:
  - `Nome`
  - `Matricula`
  - `Cargo`
  - `Tipo` (dependente de cargo no filtro)
  - `Nivel`
  - `Status`
- Listagem:
  - Paginacao server-side (`page`, `pageSize`, `total`)
  - Colunas: Nome, Matricula, Cargo, Tipo, Nivel, Registrado em, Acoes
  - Acoes: `Detalhes`, `Editar`, `Historico`, `Cancelar/Ativar`
  - Botao `Exportar Excel (CSV)` no cabecalho
  - O botao de exportacao fica bloqueado por `10` segundos apos cada clique.
- Historico:
  - Fonte: `app_entity_history`
  - Modal com paginacao fixa de `5` registros por pagina

Boas praticas
-------------
- Sempre validar `tenant_id` em todas as operacoes.
- Nao aceitar `Tipo` fora do `Cargo` escolhido.
- Nao aceitar `Nivel` inexistente/inativo no tenant.
- Registrar alteracoes em `app_entity_history` para `UPDATE`, `CANCEL`, `ACTIVATE`.
- Manter motivo obrigatorio em cancelamento/ativacao.
- Recarregar a lista e limpar o formulario stale quando houver conflito de concorrencia.
- Preferir RPC transacional para `POST`, `PUT` e `PATCH`, concentrando escrita, historico e concorrencia no banco.

Cancelamento e saldo
--------------------
- Cancelamento/ativacao implementado em `people`.
- Campos de status:
  - `ativo`
  - `cancellation_reason`
  - `canceled_at`
  - `canceled_by`
- Regra de consistencia:
  - Ativo: sem dados de cancelamento.
  - Inativo: com data e motivo.
- Saldo:
  - Nao se aplica ao modulo de pessoas.

Atalhos
-------
- Rota principal: `/pessoas`
- APIs:
  - `GET/POST/PUT/PATCH /api/people`
  - `GET /api/people/meta`

Atualizacao 2026-03
-------------------
- Tela `Pessoas` saiu de placeholder para implementacao completa.
- Criadas APIs de listagem/cadastro/edicao/status/historico e metadados.
- Criada migration `055_add_people_status_and_cancellation_fields.sql` para status com motivo.
- Criada migration `056_prevent_people_duplicate_identity.sql` para bloquear duplicidade no cadastro/edicao.
- Criada migration `079_create_people_and_invite_write_rpcs.sql` para adicionar `save_person_record` e `set_person_record_status`.
- Implementada exportacao CSV na listagem.
- Integracao com catalogos `job_titles`, `job_title_types` e `job_levels`.
- Revisao de cadastro (2026-03-20): formulario agora bloqueia `Cadastrar/Atualizar` quando nao houver `Cargo` ativo carregado em metadados.
- Revisao de cadastro (2026-03-20): adicionada mensagem explicita na tela orientando cadastrar `Cargo` antes de tentar salvar pessoa.
- Revisao de cadastro (2026-03-20): `Matricula` e `Tipo` passaram a ser obrigatorios no formulario e na API (`POST`/`PUT`).
- Revisao de cadastro (2026-03-20): adicionado fallback no backend para cadastro quando a RPC `save_person_record` nao existir no banco (compatibilidade temporaria), com retorno de erro tecnico mais claro.
- Revisao de cadastro (2026-03-20): reforcado o fallback do `POST/PUT` para matricula numerica quando a RPC retorna erro de tipo (`numeric` x `text/varchar`) em `result.success=false`, evitando `500` generico.
- Antes: tela apenas informativa (placeholder).
- Depois: CRUD completo com filtros, paginacao, exportacao, historico, controle de status e bloqueio de sobrescrita concorrente por `expectedUpdatedAt`.
- Depois: a escrita sensivel da tela passa a ser executada em RPC transacional no banco, em vez de update/historico distribuido na route.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/modules/dashboard/pessoas/PeoplePageView.tsx`
  - Responsavel por: formulario, filtros, listagem, exportacao, detalhes, historico e status, incluindo envio de `expectedUpdatedAt` e refresh quando houver conflito.
- Caminho: `src/app/api/people/route.ts`
  - Responsavel por: validar tenant/cargo/tipo/nivel, listar/historico e delegar insert/update/status para as RPCs `save_person_record` e `set_person_record_status`.
- Caminho: `src/app/api/people/meta/route.ts`
  - Responsavel por: carregar metadados de cargos, tipos por cargo e niveis.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/pessoas/PeoplePageView.tsx`
  - Responsavel por: `PAGE_SIZE`, `HISTORY_PAGE_SIZE`, `EXPORT_PAGE_SIZE`, `INITIAL_FORM`, `INITIAL_FILTERS`.
- Caminho: `supabase/migrations/055_add_people_status_and_cancellation_fields.sql`
  - Responsavel por: campos de cancelamento em `people` e regra de consistencia ativo/inativo.
- Caminho: `supabase/migrations/056_prevent_people_duplicate_identity.sql`
  - Responsavel por: bloqueio de duplicidade por nome + matricula + cargo + tipo + nivel no tenant.
- Caminho: `supabase/migrations/079_create_people_and_invite_write_rpcs.sql`
  - Responsavel por: criar `save_person_record` e `set_person_record_status` para escrita transacional da tela de pessoas.

Funcoes utilitarias relacionadas
- Caminho: `src/lib/server/appUsersAdmin.ts`
  - Responsavel por: validar sessao e resolver tenant nas rotas `/api/people*`.
- Caminho: `src/hooks/useAuth.ts`
  - Responsavel por: token da sessao para chamadas autenticadas do frontend.

Servicos e integracoes externas
- Supabase Postgres com RLS.
- Next.js Route Handlers (`/api/people`, `/api/people/meta`).

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/(dashboard)/pessoas/page.tsx`
  - `src/modules/dashboard/pessoas/PeoplePageView.tsx`
  - `src/modules/dashboard/pessoas/PeoplePageView.module.css`
  - `src/app/api/people/route.ts`
  - `src/app/api/people/meta/route.ts`
  - `supabase/migrations/055_add_people_status_and_cancellation_fields.sql`
  - `supabase/migrations/056_prevent_people_duplicate_identity.sql`
  - `src/lib/server/concurrency.ts`
  - `supabase/migrations/079_create_people_and_invite_write_rpcs.sql`
- Funcoes/hooks/constantes tocadas:
  - `loadMeta`, `loadPeople`, `handleSubmit`, `openHistoryModal`, `confirmStatusChange`, `handleExportPeople`
  - `hasJobTitles`, `hasTypeOptionsForSelectedJob`, `canSubmitPersonForm` (bloqueio de submit sem pre-requisitos de cargo/tipo)
  - `savePersonViaRpc`, `setPersonStatusViaRpc`, `isMatriculationNumericTypeMismatchError`
  - `save_person_record`, `set_person_record_status`
  - `normalizeExpectedUpdatedAt`, `hasUpdatedAtConflict`, `buildConcurrencyConflictResponse`
- Comportamento antes/depois:
  - Antes: tela de Pessoas em placeholder.
  - Depois: tela operacional completa no padrao SaaS de cadastros.
  - Depois: a edicao e a troca de status passam a recusar dados stale em vez de aplicar `last-write-wins`.
  - Depois (2026-03-20): o submit fica bloqueado quando nao houver `Cargo` ativo no tenant, com feedback explicito ao usuario.
  - Depois (2026-03-20): `Matricula` e `Tipo` tornam-se obrigatorios para cadastro/edicao.
  - Depois (2026-03-20): cadastro/edicao de pessoa tenta fallback com cast numerico da matricula tambem quando a RPC devolve `success=false` por incompatibilidade de tipo.
  - Atualizacao (2026-03-24): exportacao `Exportar Excel (CSV)` passou a incluir os campos obrigatorios do cadastro e metadados de auditoria da lista (`Status`, `Registrado por/em`, `Atualizado por/em`).
- Arquivos alterados/criados (2026-03-24):
  - `src/modules/dashboard/pessoas/PeoplePageView.tsx`
- Funcoes/hooks/constantes tocadas (2026-03-24):
  - `buildPeopleCsv`
- Comportamento antes/depois (2026-03-24):
  - Antes: CSV de Pessoas exportava colunas reduzidas sem dados de `Atualizado por` e `Atualizado em`.
  - Depois: CSV exporta colunas completas para preenchimento/consulta operacional e rastreabilidade de auditoria.
  - Depois: `Criado por`/`Atualizado por` no CSV e no modal de detalhes aplicam fallback `Nao identificado` quando o nome vier vazio.
