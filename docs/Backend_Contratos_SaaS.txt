Tela: Backend Contratos SaaS

Visao geral
-----------
- Documento para registrar quais partes do Supabase o frontend web depende hoje.
- Serve para evitar perda de contexto ao separar o SaaS do app Android.

Arquitetura
-----------
- Backend versionado neste repositorio em `supabase/`.
- Frontend publicado no Vercel, sem portar Edge Functions para serverless functions do Vercel.
- O contrato minimo atual do SaaS cobre autenticacao web, recuperacao, logout, auditoria e base de permissao por pagina.
- O contrato atual tambem cobre a edicao de credenciais por usuario na tela `/permissoes`.
- O contrato atual tambem cobre a leitura das permissoes salvas por usuario no bootstrap da sessao web.

Cadastro
--------
- Nao se aplica diretamente.

Filtros, auto-complete e listagem
---------------------------------
- Nao se aplica diretamente.

Boas praticas
-------------
- Publicar migrations no Supabase e manter docs atualizadas.
- Nao acoplar frontend web a SQL embutido dentro do Next.js.
- Usar Edge Functions como porta de entrada do frontend quando a regra exigir autenticacao, rate limit e auditoria.
- Manter `PASSWORD_REDIRECT_URL` alinhado com o dominio publicado no Vercel para invite e recuperacao de senha.

Cancelamento e saldo
--------------------
- Regras de saldo e materiais existem no backend, mas ainda nao foram consumidas pelo frontend web.

Atalhos
-------
- Function de login web: `supabase/edge_functions/auth-login-web/index.ts`
- Function de recover web: `supabase/edge_functions/auth-recover/index.ts`
- Function de logout: `supabase/edge_functions/logout/index.ts`
- Function de log de erro: `supabase/edge_functions/log_error/index.ts`

Atualizacao 2026-03
-------------------
- Login web depende de `login_name` em `app_users`.
- Migration `016_add_login_name_to_auth_tables.sql` criada.
- Migration `017_sync_auth_users_to_app_users.sql` criada.
- Migration `018_make_auth_user_sync_fail_open.sql` criada.
- Migration `019_login_audit_event_log.sql` criada.
- Migration `020_harden_rls_auth_uid_active.sql` criada.
- Migration `021_rls_to_authenticated.sql` criada.
- Migration `022_create_page_permissions.sql` criada.
- Migration `023_normalize_roles_to_app_roles.sql` criada.
- Migration `024_create_user_page_permissions.sql` criada.
- Migration `025_app_users_admin_tenant_select.sql` criada.
- Migration `026_simplify_user_page_permissions.sql` criada.
- Migration `027_create_permission_change_history.sql` criada.
- Migration `028_add_operation_menu_pages.sql` criada.
- Migration `029_create_project_table.sql` criada.
- Migration `030_project_sob_priority_rules.sql` criada.
- Migration `031_create_project_lookup_tables.sql` criada.
- Migration `032_create_contrato_table.sql` criada.
- Migration `033_rename_contrato_to_contract.sql` criada.
- Migration `034_use_people_for_project_contractor_responsible.sql` criada.
- Migration `035_add_matriculation_to_people.sql` criada.
- Migration `036_create_project_history_and_cancellation.sql` criada.
- Migration `037_project_activation_history_rules.sql` criada.
- `auth-login-web` implementada.
- `auth-recover` implementada para reset por `login_name`.
- `auth-login-web` e `logout` ajustadas para tratar `login_audit` como log de eventos imutaveis com `session_ref`.
- `logout` passou a registrar `TOKEN_EXPIRED` no `login_audit` usando `session_ref`/`login_audit_id`, mesmo quando o token ja venceu.
- `auth-login-web` passou a registrar `console.error` detalhado quando o insert em `login_audit` falhar.
- `auth-login-web` passou a separar o client administrativo do client de autenticacao para nao executar o insert de auditoria com o JWT do usuario final.
- Base de permissao por pagina criada com `app_pages` e `role_page_permissions`.
- Perfis normalizados em `app_roles`, com `app_users.role_id` e `role_page_permissions.role_id`.
- Busca administrativa de usuarios do tenant usa `SUPABASE_SERVICE_ROLE_KEY` no backend, com validacao explicita do operador admin/master e filtro por tenant.
- A tela `/permissoes` passou a salvar `app_users.role_id`, `app_users.ativo` e `app_user_page_permissions`.
- A tela `/permissoes` passou a gravar historico em `app_user_permission_history`.
- O fluxo oficial de provisionamento web passou a ser `pre-cadastro em app_users + invite no Auth`.
- A route `src/app/api/auth/session-access/route.ts` passou a devolver `pageAccess` e `hasCustomPermissions` para o frontend montar o shell conforme as permissoes salvas.
- Rotas `src/app/api/projects/route.ts` e `src/app/api/projects/meta/route.ts` criadas para cadastro, filtros e listagem paginada da tela `/projetos`.
- A rota `src/app/api/projects/route.ts` passou a suportar `PATCH` para cancelamento e ativacao com motivo obrigatorio.
- A rota `src/app/api/projects/route.ts` passou a consultar historico por `historyProjectId` usando a tabela `project_history`.
- A rota `src/app/api/projects/route.ts` passou a validar formato de `SOB` por prioridade antes do insert.
- A rota `src/app/api/projects/meta/route.ts` passou a ler opcoes de cadastro das tabelas de dominio de projetos, com excecao de `Responsavel Contratada` que passou a vir de `people` com cargo `SUPERVISOR`.
- A rota `src/app/api/projects/route.ts` passou a preencher `partner` automaticamente com `contract.name` do tenant, sem campo `Parceira` no frontend.
- O frontend passou a ter preparo explicito para deploy no Vercel, mantendo `auth-login-web`, `auth-recover`, `logout` e demais Edge Functions no Supabase.
- `tsconfig.json` do frontend passou a excluir `supabase/edge_functions` do type-check do Next para evitar conflito entre runtime Node e runtime Deno.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `supabase/edge_functions/auth-login-web/index.ts`
  - Responsavel por: autenticar `login_name + senha`, aplicar rate limit e registrar evento `LOGIN` no `login_audit`.
- Caminho: `supabase/edge_functions/auth-recover/index.ts`
  - Responsavel por: resolver `login_name -> email`, aplicar rate limit e disparar reset de senha.
- Caminho: `supabase/edge_functions/logout/index.ts`
  - Responsavel por: registrar evento `LOGOUT` no `login_audit` usando `session_ref`.
- Caminho: `supabase/edge_functions/log_error/index.ts`
  - Responsavel por: registrar erros do frontend/app.
- Caminho: `src/app/api/app-users/search/route.ts`
  - Responsavel por: listar usuarios reais do tenant atual para a tela `/permissoes`.
- Caminho: `src/app/api/app-users/[userId]/permissions/route.ts`
  - Responsavel por: carregar e salvar `role`, `ativo/inativo`, permissoes por tela e historico de alteracoes do usuario selecionado.
- Caminho: `src/app/api/app-users/[userId]/invite/route.ts`
  - Responsavel por: enviar o convite de primeiro acesso a partir do pre-cadastro existente em `app_users`.
- Caminho: `src/app/api/auth/session-access/route.ts`
  - Responsavel por: resolver o usuario autenticado atual e devolver as telas liberadas em `app_user_page_permissions`.
- Caminho: `src/app/api/projects/route.ts`
  - Responsavel por: cadastrar, editar, cancelar, listar e retornar historico de projetos por tenant.
- Caminho: `src/app/api/projects/meta/route.ts`
  - Responsavel por: carregar opcoes de filtros/auto-complete da tela de projetos.
- Caminho: `vercel.json`
  - Responsavel por: declarar o deploy do frontend como projeto `nextjs`, sem substituir o backend Supabase.

Constantes e configuracoes
- Caminho: `supabase/migrations/016_add_login_name_to_auth_tables.sql`
  - Responsavel por: adicionar `login_name` ao backend de autenticacao.
- Caminho: `supabase/migrations/017_sync_auth_users_to_app_users.sql`
  - Responsavel por: sincronizar `auth.users` com `app_users`.
- Caminho: `supabase/migrations/018_make_auth_user_sync_fail_open.sql`
  - Responsavel por: impedir que falhas na sincronizacao bloqueiem `Authentication > Invite user`.
- Caminho: `supabase/migrations/019_login_audit_event_log.sql`
  - Responsavel por: transformar `login_audit` em log de eventos com `event_type`, `event_at` e `session_ref`.
- Caminho: `supabase/migrations/020_harden_rls_auth_uid_active.sql`
  - Responsavel por: reforcar as policies RLS para usar `auth.uid()` com `app_users.ativo = true`.
- Caminho: `supabase/migrations/021_rls_to_authenticated.sql`
  - Responsavel por: limitar as policies multi-tenant ao role `authenticated`.
- Caminho: `supabase/migrations/022_create_page_permissions.sql`
  - Responsavel por: criar `app_pages`, `role_page_permissions` e a base RLS para permissao por `role`.
- Caminho: `supabase/migrations/023_normalize_roles_to_app_roles.sql`
  - Responsavel por: criar `app_roles`, migrar `role` texto para `role_id` e atualizar os fluxos de auth/permissao.
- Caminho: `supabase/migrations/024_create_user_page_permissions.sql`
  - Responsavel por: criar `app_user_page_permissions` e a funcao `user_has_page_action(text, text)` para a matriz por usuario e por tela.
- Caminho: `supabase/migrations/025_app_users_admin_tenant_select.sql`
  - Responsavel por: liberar leitura de `app_users` do mesmo tenant apenas para perfis administrativos autenticados.
- Caminho: `supabase/migrations/026_simplify_user_page_permissions.sql`
  - Responsavel por: simplificar `app_user_page_permissions` para usar apenas `can_access`.
- Caminho: `supabase/migrations/027_create_permission_change_history.sql`
  - Responsavel por: criar `app_user_permission_history` para auditoria de alteracoes administrativas e convites.
- Caminho: `supabase/migrations/028_add_operation_menu_pages.sql`
  - Responsavel por: incluir `projetos`, `locacao` e `programacao` no catalogo `app_pages`, mover `materiais` para `Operacao` e atualizar labels de entrada/saida.
- Caminho: `supabase/migrations/029_create_project_table.sql`
  - Responsavel por: criar a tabela `project` com campos da tela, auditoria (`created_by`, `updated_by`, `created_at`, `updated_at`) e policies RLS por tenant.
- Caminho: `supabase/migrations/030_project_sob_priority_rules.sql`
  - Responsavel por: validar formato do `SOB` por prioridade e garantir unicidade case-insensitive de `SOB` por tenant.
- Caminho: `supabase/migrations/031_create_project_lookup_tables.sql`
  - Responsavel por: criar tabelas de dominio de projetos por tenant e vincular `project` por FKs.
- Caminho: `supabase/migrations/032_create_contrato_table.sql`
  - Responsavel por: criar a tabela `contrato` com um registro por tenant, coluna `name` e `valor` derivado do `tenant_id`.
- Caminho: `supabase/migrations/033_rename_contrato_to_contract.sql`
  - Responsavel por: renomear `contrato` para `contract` e alinhar indice/policies/trigger ao novo nome.
- Caminho: `supabase/migrations/034_use_people_for_project_contractor_responsible.sql`
  - Responsavel por: remover o lookup `project_contractor_responsibles` e usar `people` com cargo `SUPERVISOR` no campo `contractor_responsible`.
- Caminho: `supabase/migrations/035_add_matriculation_to_people.sql`
  - Responsavel por: adicionar a coluna `matriculation` na tabela `people`.
- Caminho: `supabase/migrations/036_create_project_history_and_cancellation.sql`
  - Responsavel por: adicionar status ativo em `project` e criar tabelas `project_history` e `project_cancellation_history`.
- Caminho: `supabase/migrations/037_project_activation_history_rules.sql`
  - Responsavel por: permitir `ACTIVATE` no `project_history` e adicionar `action_type` no `project_cancellation_history`.
- Caminho: `supabase/migrations/011_create_rate_limit.sql`
  - Responsavel por: rate limit das functions.
- Caminho: `.env.example`
  - Responsavel por: listar as variaveis que precisam existir no Vercel e no Supabase para login, invite e reset.
- Caminho: `tsconfig.json`
  - Responsavel por: impedir que o build web tente tipar imports Deno das Edge Functions.

Funcoes utilitarias relacionadas
- Caminho: `src/services/auth/auth.service.ts`
  - Responsavel por: consumo de `auth-login-web`, `logout` e hidratacao de `pageAccess` via `session-access`.

Servicos e integracoes externas
- Supabase Auth
- Postgres com RLS
- Edge Functions Deno

Ajuda contextual
- Migrations minimas para o login web funcionar em ambiente novo:
  - `000_create_auth_and_audit_tables.sql`
  - `011_create_rate_limit.sql`
  - `015_add_audit_columns.sql`
  - `016_add_login_name_to_auth_tables.sql`
  - `017_sync_auth_users_to_app_users.sql`
  - `018_make_auth_user_sync_fail_open.sql`
  - `019_login_audit_event_log.sql`
  - `020_harden_rls_auth_uid_active.sql`
  - `021_rls_to_authenticated.sql`
  - `022_create_page_permissions.sql`
  - `023_normalize_roles_to_app_roles.sql`
  - `024_create_user_page_permissions.sql`
  - `025_app_users_admin_tenant_select.sql`
  - `026_simplify_user_page_permissions.sql`
  - `027_create_permission_change_history.sql`
  - `028_add_operation_menu_pages.sql`
  - `029_create_project_table.sql`
  - `030_project_sob_priority_rules.sql`
  - `031_create_project_lookup_tables.sql`
  - `032_create_contrato_table.sql`
  - `033_rename_contrato_to_contract.sql`
  - `034_use_people_for_project_contractor_responsible.sql`
  - `035_add_matriculation_to_people.sql`
  - `036_create_project_history_and_cancellation.sql`
  - `037_project_activation_history_rules.sql`
- Functions minimas para o SaaS web:
  - `auth-login-web`
  - `auth-recover`
  - `logout`
  - `log_error`
- O Vercel nao substitui essas functions; ele hospeda apenas o frontend Next.js e as rotas `src/app/api/*`.
- Nenhuma RPC nova e obrigatoria para login, recover ou shell nesta fase alem da `rate_limit_check_and_hit`.
- Criar usuario apenas em `Authentication > Users` nao deve mais falhar por causa da sincronizacao. O vinculo com `app_users` ainda depende de email unico correspondente ou metadata minima no Auth (`tenant_id` e `matricula`).
- `login_audit` agora registra duas linhas por sessao do SaaS web: uma com `event_type = 'LOGIN'` e outra com `event_type = 'LOGOUT'`, correlacionadas por `session_ref`.
- As policies RLS multi-tenant agora devem considerar `auth.uid()` e `app_users.ativo = true` para evitar acesso de usuarios desativados com sessao ainda valida.
- As policies multi-tenant agora tambem devem ser `TO authenticated`, evitando depender apenas de `auth.uid() is null` para bloquear `anon`.
- `app_pages` funciona como catalogo global de rotas controladas e `role_page_permissions` armazena acesso por tenant e `role_id`.
- `app_roles` virou a fonte de verdade dos perfis (`admin`, `master`, `user`).
- A funcao `user_is_admin_in_tenant(uuid)` agora resolve o papel administrativo via `app_roles.is_admin`.
- A busca da tela `/permissoes` valida o operador atual como admin/master e consulta `app_users` do mesmo tenant com `service_role` no backend, sem expor esse acesso ao frontend.
- O operador da tela `/permissoes` precisa ser admin/master, mas a busca retorna qualquer `app_user` do mesmo tenant para configuracao de acesso.
- O salvamento da tela `/permissoes` nao usa `delete`; ele faz `upsert` em `app_user_page_permissions` usando apenas `can_access`.
- O shell e a protecao de rotas nao devem confiar so em `role`; quando `hasCustomPermissions = true`, a navegacao passa a ser definida pelas linhas salvas em `app_user_page_permissions`.
- `TOKEN_EXPIRED` deve continuar gerando evento `LOGOUT` no `login_audit`, agora usando fallback por `session_ref` quando o JWT ja expirou.
- O provisionamento oficial no painel web deve partir de um usuario ja pre-cadastrado em `app_users`, com envio de invite para o email salvo no cadastro.
