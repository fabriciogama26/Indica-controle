Tela: Dashboard Estoque SaaS

Visao geral
-----------
- Tela read-only de indicadores do estoque.
- Consolida 6 graficos iniciais:
  - Dispersao Requisicao/Devolucao
  - Materiais criticos e zerados
  - Top materiais por saldo
  - Materiais sem giro
  - Curva ABC do estoque
  - Evolucao de movimentacoes
- Centro de estoque e usado somente como filtro operacional, sem ranking por centro.
- A tela preserva o escopo multi-tenant por `tenant_id` e valida permissao pela pagina `dash-estoque`.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/dash-estoque/page.tsx`
- View principal: `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
- Estilo dedicado: `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
- API da tela: `src/app/api/dash-estoque/route.ts`
- Menu e titulo: `src/components/layout/AppShell.tsx`
- Autorizacao de rota: `src/lib/auth/authorization.ts`
- Matriz de permissoes: `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
- Migration de pagina/permissao: `supabase/migrations/186_create_stock_dashboard_page.sql`
- Hook de erro usado pela tela: `src/hooks/useErrorLogger.ts`

Filtros, auto-complete e listagem
---------------------------------
- Filtros:
  - `Data inicial`
  - `Data final`
  - `Centro de estoque`
  - `Equipe`
  - `Projeto`
  - `Material`
  - `Tipo`
  - `Limite critico`
- Graficos:
  - `Dispersao de materiais`
    - Botao `Requisicao`
    - Botao `Devolucao`
    - Botao `Exportar Excel`
    - Botao `Expandir`
    - Cards por UMB da dispersao, alternando quantidade movimentada conforme `Requisicao` ou `Devolucao`
    - Os cards por UMB funcionam como filtro: clicar em uma UMB restringe grafico, lista lateral e CSV aquela unidade; clicar de novo ou em `Todas as UMB` volta para todas
    - O ranking de materiais retornado pela API e cortado por UMB (top 25 por operacao + UMB), garantindo representacao de todas as unidades
    - Alternancia de escala entre `Raiz` e `Linear`
    - Lista lateral completa com rolagem para selecionar material
    - Lista lateral exibe `Material`, `UMB`, `Quantidade`, `Operacoes` e `Projetos`
    - Legenda `Quantidade` fica acima da area plotada para nao cobrir os pontos
    - Foco individual: ao selecionar um material na lista, o grafico exibe somente o circulo correspondente
    - Eixo X: quantidade movimentada
    - Eixo Y: numero de operacoes, com rotulo `Operacoes` por extenso e na horizontal
    - Tamanho do ponto: saldo atual do material
    - Cor do ponto: faixa de quantidade movimentada, com legenda no grafico (`<= 10`, `11-20`, `21-30`, `31-40`, `41-50`, `51-100`, `101-200` e depois intervalos de 200 em 200)
    - Faixas dinamicas acima da paleta fixa recebem cor deterministica propria; faixas como `1001-1200`, `2801-3000` e `12801-13000` nao reutilizam automaticamente a ultima cor da paleta.
  - `Materiais criticos e zerados`
    - Materiais com saldo menor ou igual ao limite critico
  - `Top materiais por saldo`
    - Materiais com maior saldo consolidado
  - `Materiais sem giro`
    - Faixas por ultima movimentacao conhecida
    - Clique na barra/faixa abre modal com os materiais daquela faixa.
    - O modal permite exportar os materiais exibidos em CSV pelo componente padrao `CsvExportButton`.
  - `Curva ABC do estoque`
    - Alternancia por botao entre `Valor` e `Quantidade`
    - Modo `Valor`: classificacao por `saldo x unit_price`
    - Modo `Quantidade`: classificacao por quantidade em estoque
    - Clique na linha da classe `A`, `B` ou `C` abre modal com os materiais daquela classe.
    - O modal permite exportar os materiais exibidos em CSV pelo componente padrao `CsvExportButton`.
  - `Evolucao de movimentacoes`
    - Quantidade mensal de operacoes realizadas por tipo
    - Cada combinacao `data da movimentacao + equipe + projeto + status` e contada uma unica vez no mes e tipo, mesmo quando possui varios materiais ou varias linhas do mesmo evento
    - O evento operacional tambem e persistido em `stock_transfers.operation_event_id` por `tenant_id + data da movimentacao + equipe + projeto + status`
    - A contagem da evolucao nao depende da hidratacao de `materials` quando nao ha filtro de material/tipo
    - O total de cada tipo e exibido diretamente acima da respectiva barra mensal
    - O valor estimado mensal e exibido abaixo do mes e soma `quantidade movimentada x unit_price` dos itens validos do mes.
    - Icone `i` ao lado do total de operacoes usa tooltip nativo do navegador para explicar a formula: soma de `quantidade movimentada x preco unitario do material`, respeitando filtros aplicados e excluindo estornos/movimentos estornados.
    - Nao soma quantidades de UMB diferentes no mesmo grafico
- Cards:
  - Materiais
  - Criticos
  - Zerados
  - Valor estimado
  - Bloco `Estoque` com resumo por UMB
  - Resumo por UMB da dispersao selecionada (`Requisicao`/`Devolucao`)

Boas praticas
-------------
- Manter toda consulta filtrada por `tenant_id`.
- Nao expor centros de estoque proprios de equipes como centros publicos de estoque.
- Nao recalcular saldo no frontend.
- Usar a API agregada para preparar os dados dos graficos.
- Registrar erros com `useErrorLogger("dash_estoque")`.
- Manter `Centro de estoque` apenas como filtro, sem ranking por centro.
- Aplicar filtros somente pelo botao `Filtrar`; alteracoes nos campos nao recarregam automaticamente a tela.
- Para evolucao temporal, contar operacoes distintas por `data + equipe + projeto + status`, sem usar a quantidade de itens ou transferencias como quantidade de operacoes.
- `operation_event_id` persiste a mesma regra de negocio com `tenant + data + equipe + projeto + status`.
- Excluir movimentacoes estornadas e movimentacoes de estorno dos agregados quando houver vinculo em `stock_transfer_reversals` ou `stock_transfer_item_reversals`.
- Migracao recomendada para RPC:
  - Consolidar em RPC/view materializada os agregados de saldos por material/UMB, criticidade, top saldo, giro, Curva ABC, evolucao mensal e dispersao.
  - Manter no endpoint Next apenas autenticacao/permissao da pagina, normalizacao de filtros e chamada da RPC.
  - A RPC deve receber `tenant_id`, periodo, centro, equipe, projeto, material, tipo e limite critico, aplicar filtros nativos no banco e excluir estornos pelo relacionamento de reversao.
  - O ganho esperado e reduzir volume trafegado para Node e remover loops de agregacao em memoria do dashboard.

Cancelamento e saldo
--------------------
- A tela nao grava movimentacao, cancelamento ou estorno.
- A tela apenas consulta:
  - `stock_center_balances`
  - `stock_transfers`
  - `stock_transfer_items`
  - `stock_transfer_team_operations`
  - `materials`
  - `teams`
  - `project`
- O saldo atual vem do backend e e exibido de forma consolidada por material.
- O grafico de evolucao usa o ledger de movimentacoes dentro do periodo filtrado e conta eventos operacionais distintos por tipo de operacao.

Atualizacao 2026-08-12 - Teto de movimentacoes deixa de truncar em silencio (HTTP 422)
----------------------------------------------------------------------------------------
- Origem: auditoria de performance/Disk I/O (`Auditoria/`, item P0.1 de `Auditoria/06-plano-de-acao.md`).
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - A correcao de 2026-07-03 (2) resolveu o truncamento em 1000 linhas do PostgREST paginando via `.range()`, mas manteve um SEGUNDO teto silencioso: o laco de `loadTransfers` para em `DASH_TRANSFERS_MAX_ROWS = 20000` e simplesmente encerra, sem sinal nenhum.
  - Periodo com mais de 20.000 movimentacoes fazia TODOS os indicadores derivados serem calculados sobre um recorte parcial e apresentados como se fossem o total: `movementCount`, `totalMovementQuantity`, `summaryByUnit`, evolucao, Curva ABC, sem giro e dispersao.
  - Isso e bug de CORRECAO, nao de performance: quem le o card nao tem como saber que faltam dados. E o mesmo tipo de falha da 2026-07-03, so que num teto maior.
- Depois:
  - `loadTransfers` detecta a truncagem de forma exata: quando o teto e atingido, faz UMA sondagem de 1 linha em `.range(20000, 20000)`. A sondagem distingue "existem exatamente 20.000 movimentacoes" (carga valida, segue) de "existem mais de 20.000" (recusa) — sem ela, um periodo com exatamente o teto seria recusado por engano.
  - Havendo excedente, a rota devolve **HTTP 422** com `code: "DASH_ESTOQUE_PERIODO_EXCEDE_LIMITE"`, `limit: 20000` e mensagem acionavel ("Reduza o intervalo de datas ou filtre por centro de estoque"). NENHUM KPI parcial e renderizado.
  - Decisao de produto tomada com o usuario: recusar e pedir periodo menor, em vez de renderizar com banner de "dados parciais". Numero parcial rotulado continua sendo numero que alguem printa fora de contexto; a unica opcao em que ninguem le KPI errado e a que nao renderiza.
  - 422 e nao 500 de proposito: nao e falha do servidor, e pedido que o dashboard nao consegue responder inteiro.
  - Frontend NAO precisou mudar: `hooks.ts` ja propaga `payload.message` de qualquer resposta nao-ok para o feedback de erro existente.
- Estado conhecido / divida:
  - O teto continua existindo — o que mudou e que ele parou de mentir. A remocao definitiva e o item P2.1 da auditoria: subir a agregacao para o banco via RPC, no padrao ja aprovado em `dashboard-portfolio` (`dashboard_portfolio_asbuilt_factor`, `dashboard_portfolio_forecast_gap_summary`). Agregacao no banco nao precisa de teto de linhas, e a sondagem e o 422 saem junto.
  - A sondagem usa `.range(20000, 20000)`, que obriga o Postgres a descartar 20.000 linhas. Custo aceito por ser um caso de borda, ja degenerado, e temporario ate o P2.1.
- Impacto multi-tenant:
  - Sem mudanca de schema, RLS, policy ou permissao. A sondagem repete o mesmo filtro `tenant_id` das paginas de `loadTransfers`.

Atualizacao 2026-08-05 (2) - Refatoracao estrutural do modulo (piloto do padrao)
----------------------------------------------------------------------------------
- Arquivos alterados:
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/hooks.ts` (novo)
  - `src/modules/dashboard/dash-estoque/helpers.ts` (novo)
  - `src/modules/dashboard/dash-estoque/types.ts` (novo)
  - `src/modules/dashboard/dash-estoque/components/*.tsx` (11 novos)
  - `file-size-baseline.json`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - `StockDashboardPageView.tsx` concentrava 1.383 linhas: tipos do payload, formatadores, paleta e faixas da dispersao, 8 componentes visuais, todo o estado da tela, o fetch da API, os memos derivados e as duas exportacoes CSV.
  - Os controles segmentados de operacao e escala da dispersao estavam duplicados literalmente no cabecalho do card e no do modal ampliado.
- Depois:
  - Estrutura do modulo: `components/` (11 componentes), `hooks.ts` (`useStockDashboard`), `helpers.ts` (funcoes puras), `types.ts` (tipos) e `StockDashboardPageView.tsx` como composicao da pagina (210 linhas).
  - `ScatterViewControls.tsx` passa a ser a fonte unica dos controles segmentados; o markup renderizado continua identico nos dois lugares.
  - O botao `Exportar CSV` do modal ampliado recebe `canExport` do hook em vez de recalcular o filtro localmente, mantendo uma unica fonte da regra.
  - REFATORACAO EXCLUSIVAMENTE ESTRUTURAL: nenhuma mudanca de layout, texto, classe CSS, atributo de acessibilidade, regra de calculo, filtro, dado exibido ou chamada de API. O CSS module nao foi tocado.
  - Equivalencia verificada por comparacao automatizada do arquivo antigo contra o conjunto novo: classes CSS, atributos de acessibilidade, textos e chamadas de helper batem token a token; as unicas divergencias sao a deduplicacao dos controles segmentados (`.segmented` 5 -> 3 ocorrencias escritas, `.segment`/`.segmentActive` 10 -> 6, `<button>` 20 -> 16, `<div>` 81 -> 79) e os imports novos.
  - Baseline do ratchet: `StockDashboardPageView.tsx` saiu de `file-size-baseline.json` (1.383 -> 210 linhas, abaixo do limite de 1.000); baseline caiu de 31 para 30 entradas.
- Impacto multi-tenant:
  - Nenhum; o modulo e read-only e a chamada a `GET /api/dash-estoque` nao mudou.
- Validacao:
  - Automatizada: `npm run lint`, `npm run lint:size`, `npx tsc --noEmit`, `npm run build` e comparacao estrutural token a token contra o arquivo anterior.
  - A comparacao visual interativa nao pode ser executada pelo agente (o ambiente nao permite subir o servidor de desenvolvimento) e ficou pendente do usuario.
  - Validacao manual (2026-08-05, registrada pelo usuario): Validacao manual executada em ambiente autenticado. Dash Estoque e modais da Programacao Normalizada verificados sem regressoes funcionais ou visuais identificadas.
  - Cobre tambem a fachada publica `index.ts` do modulo, entregue logo apos esta refatoracao.

Atualizacao 2026-08-05 - Filtro por UMB na Dispersao de materiais
-------------------------------------------------------------------
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - `buildScatterRows` ordenava todos os materiais por quantidade e aplicava um corte global unico (`slice(0, 80)`), somando as duas operacoes.
  - Como quantidades de UMB diferentes tem ordens de grandeza distintas (ex.: `M` na casa dos milhoes contra `UN` na casa dos milhares), a UMB dominante ocupava quase todo o ranking e as demais quase nao apareciam no grafico; `Devolucao` tambem ficava com poucos pontos por disputar o mesmo corte com `Requisicao`.
  - Os cards por UMB acima do grafico eram apenas informativos (`div`), sem acao.
  - O eixo X misturava materiais de UMB diferentes na mesma escala de quantidade, tornando a comparacao entre pontos pouco significativa.
- Depois:
  - `buildScatterRows` aplica o corte por grupo `operacao + UMB` (`DASH_SCATTER_ROWS_PER_UNIT = 25`), com teto de seguranca por operacao (`DASH_SCATTER_ROWS_PER_OPERATION = 200`); cada UMB passa a ter representacao propria no payload.
  - A UMB de cada ponto e normalizada para `SEM UMB` quando vazia, alinhando com `buildScatterUnitSummary` e permitindo o casamento entre card e filtro.
  - `ScatterUnitStrip` passa a renderizar botoes (`aria-pressed`) com um card inicial `Todas as UMB`; a selecao alimenta o estado `scatterUnit` do `PageView`.
  - `ScatterChart`, a lista lateral, o contador de materiais e a exportacao CSV respeitam a UMB selecionada; o nome do arquivo CSV inclui a UMB (`dispersao_materiais_<operacao>_<umb>_<data>.csv`).
  - Clicar na UMB ja ativa desativa o filtro; trocar de UMB limpa o foco individual de material; novo carregamento da API reseta a selecao de UMB e de material.
  - Subtitulo do card e cabecalho do modal ampliado exibem a UMB ativa; o estado vazio informa a UMB filtrada.
  - Novos estilos `.unitPillButton` e `.unitPillActive`; o card informativo `.unitPill` do bloco `Estoque` permanece inalterado.
- Impacto multi-tenant:
  - Nenhum; a mudanca altera apenas a regra de corte e a apresentacao do payload ja autenticado e filtrado por `tenant_id` em `GET /api/dash-estoque`.
  - O filtro por UMB e client-side sobre dados ja carregados do recorte atual, sem nova chamada a API.
- Divida tecnica registrada (CLAUDE.md secao 5 - limite de 1.000 linhas por `PageView`):
  - `StockDashboardPageView.tsx` ja estava acima do limite antes desta tarefa e cresceu com ela.
  - RESOLVIDA na `Atualizacao 2026-08-05 (2)`: o plano de modularizacao foi executado e o arquivo caiu para 210 linhas.
  - Observacao de medicao: os numeros de linha citados originalmente nesta secao (1.235 -> 1.291) vieram de `Measure-Object -Line`, que ignora linhas em branco; em linha fisica os valores reais eram 1.327 -> 1.383.

Atualizacao 2026-07-07 - Descricao do material na Dispersao de materiais
--------------------------------------------------------------------------
- Arquivos alterados:
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - O tooltip nativo (`<title>`) de cada ponto do grafico de Dispersao exibia apenas `materialCode`, sem a descricao do material.
  - A lista lateral da Dispersao exibia somente o codigo do material na coluna `Material`, sem descricao, mesmo `ScatterPoint.description` ja vindo preenchido da API.
- Depois:
  - O tooltip do ponto passa a exibir `materialCode - description` antes dos demais dados (quantidade, operacoes, saldo).
  - A coluna `Material` da lista lateral passa a exibir codigo e descricao truncada (`truncateLabel`) empilhados, com `title` completo no botao, seguindo o mesmo padrao visual ja usado em `BarList`.
  - Novo estilo `.scatterRowButton strong/span` no CSS module para o layout em duas linhas.
- Impacto multi-tenant:
  - Nenhum; o campo `description` ja fazia parte do payload autenticado retornado por `GET /api/dash-estoque`, apenas nao era renderizado no frontend.

Atualizacao 2026-07-03 (2) - Correcao de truncamento silencioso em loadTransfers
---------------------------------------------------------------------------------
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - `loadTransfers` buscava `stock_transfers` do periodo com `.limit(5000)` em uma unica chamada.
  - O Supabase/PostgREST deste projeto aplica um teto de 1000 linhas por resposta (`db-max-rows`), que sobrepoe silenciosamente qualquer `.limit()` maior pedido no codigo, sem retornar erro.
  - Com `order("entry_date", { ascending: true })`, o corte em 1000 linhas descartava todo o restante do periodo a partir de um certo ponto (ex.: em 2026, os 1000 primeiros registros do ano paravam em 03/06, apagando o resto de junho e os meses seguintes de todas as agregacoes do Dashboard Estoque: evolucao, dispersao, etc.).
  - Isso causava subcontagem severa (nao apenas para Requisicao) em qualquer mes cujos registros ficassem fora da janela das primeiras 1000 linhas do periodo filtrado.
- Depois:
  - `loadTransfers` passa a paginar de fato via `.range()` em lotes de 1000 (constante `DASH_TRANSFERS_MAX_ROWS = 20000`), ordenando por `entry_date, id` para paginacao estavel, seguindo o mesmo padrao ja usado em `src/app/api/app-users/search/route.ts`.
  - Validado com consulta direta ao Supabase: 2026 tinha 1358 `stock_transfers` reais, mas a query anterior retornava apenas 1000; apos a correcao, os 1358 sao carregados e a contagem de Requisicao em 06/2026 sobe de 5 para 50 (05/2026, que ja estava dentro da janela de 1000 linhas, permanece em 71, confirmando que o calculo em si estava correto).
- Impacto multi-tenant:
  - Sem mudanca de schema/RLS; a paginacao mantem o filtro por `tenant_id` existente em cada pagina.

Atualizacao 2026-07-06 - Modais de detalhamento, cores dinamicas e valor mensal
--------------------------------------------------------------------------------
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Antes:
  - Faixas dinamicas da dispersao acima do tamanho da paleta fixa reutilizavam a ultima cor, deixando faixas altas visualmente iguais.
  - A Curva ABC exibia apenas os totais por classe.
  - Materiais sem giro exibia apenas totais por faixa.
  - Evolucao mensal exibia somente quantidade de operacoes por tipo.
  - O endpoint calculava os agregados no Node, sem uma RPC consolidada especifica para o dashboard.
- Depois:
  - `buildScatterQuantityBands` passa a usar `getScatterQuantityPalette`; acima da paleta fixa, a cor e gerada por HSL deterministico e convertida para HEX, mantendo `fill` com transparencia.
  - A API adiciona `materials` em cada linha da Curva ABC e em cada bucket de sem giro, com codigo, descricao, UMB, tipo, saldo, valor estimado, preco unitario e ultima movimentacao.
  - Clique na linha da classe ABC abre modal com os materiais da classe e percentual individual da metrica ABC.
  - Clique na barra/faixa de Materiais sem giro abre modal com os materiais da faixa e dias sem movimento.
  - O modal de materiais passa a usar `CsvExportButton`, `buildCsvContent` e `downloadCsvFile` para exportar as linhas exibidas em CSV.
  - `movementEvolution` passa a incluir `estimatedValue` mensal, calculado por `quantity * unit_price` dos movimentos validos, e o frontend exibe o valor abaixo do mes.
  - O cabecalho da Evolucao recebe botao informativo `i` com tooltip nativo (`title`) explicando a formula do valor estimado mensal.
  - O tooltip customizado foi removido para evitar duas caixas simultaneas e reduzir codigo/CSS.
  - Documentada pendencia tecnica para migrar agregacoes do Dashboard Estoque para RPC/view materializada.
- Impacto multi-tenant:
  - Sem mudanca de schema, migration ou policy RLS.
  - As consultas permanecem filtradas por `tenant_id` e pela permissao da pagina `dash-estoque`.
  - O novo detalhe exibido em modal vem do mesmo payload ja escopado pelo endpoint autenticado.

Atualizacao 2026-07-03 - Correcao de subcontagem na evolucao de movimentacoes
------------------------------------------------------------------------------
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
- Antes:
  - A API ignorava `stock_transfers.operation_event_id` (persistido e mantido por trigger) e sempre recalculava a chave visual em runtime via `buildFallbackOperationEventId`.
  - O recalculo usava o `operationKind` ja classificado (`resolveOperationKind`, que infere Requisicao/Devolucao por comparacao de `stock_center_id` quando `operation_kind` da equipe esta nulo) como status da chave, divergindo da formula persistida no banco (`coalesce(operation_kind da equipe, movement_type)`).
  - Essa divergencia podia colapsar eventos operacionais distintos na mesma chave, subcontando tipos como Requisicao em determinados meses (ex.: 06/2026 exibia poucas unidades quando o esperado era 53).
- Depois:
  - A API volta a priorizar `transfer.operation_event_id` (persistido e sincronizado por trigger a cada insercao/alteracao de `stock_transfers` ou `stock_transfer_team_operations`); o recalculo em runtime (`buildFallbackOperationEventId`) so e usado como fallback quando a coluna estiver nula (registros sem backfill).
  - O fallback passa a montar o status com a mesma regra do banco (`operation_kind` da equipe ou `movement_type`), eliminando a divergencia de formula em relacao a `build_stock_transfer_operation_event_id`.
- Impacto multi-tenant:
  - Sem mudanca de schema, migration ou politica RLS; a leitura de `operation_event_id` ja respeita o filtro por `tenant_id` existente em `loadTransfers`.

Atualizacao 2026-07
-------------------
- Arquivos criados:
  - `supabase/migrations/287_add_stock_transfer_operation_event_id.sql`
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `README.md`
  - `TASKS.md`
  - `supabase/migrations/README.txt`
- Antes:
  - A evolucao e o total de operacoes contavam `transferId` distinto.
  - Varias transferencias do mesmo contexto operacional podiam aparecer como varias operacoes.
- Depois:
  - `stock_transfers.operation_event_id` passa a persistir o evento unico por `tenant_id + data + equipe + projeto + status`.
  - A evolucao e o total do Dashboard Estoque contam a chave `data + equipe + projeto + status` por mes e tipo, a partir dos eventos de transferencia validos.
  - A dispersao passa a contar operacoes pela chave `data + equipe + projeto + status`, evitando multiplicar varias transferencias do mesmo evento.
- Impacto multi-tenant:
  - O hash deterministico inclui `tenant_id`, evitando colisao funcional entre tenants.
  - A migration nao altera policies RLS; as consultas seguem filtradas pelo `tenant_id` autenticado.
  - Triggers mantem o identificador sincronizado para movimentacoes fisicas e operacoes de equipe.

Atualizacao 2026-06
-------------------
- Arquivos alterados:
  - `src/app/api/dash-estoque/route.ts`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Antes:
  - A legenda `Quantidade` ficava sobre a area plotada e podia esconder pontos.
  - A lista lateral nao mostrava a UMB.
  - O modal ampliado exibia controle de zoom.
  - A evolucao e o total tratavam cada item de material como uma operacao.
  - A dispersao nao exibia cards por UMB para separar as quantidades movimentadas em `Requisicao` e `Devolucao`.
  - O resumo por UMB do estoque ficava solto, sem bloco visual identificado.
  - A tabela da dispersao nao tinha exportacao direta.
- Depois:
  - A legenda fica em uma faixa acima do grafico.
  - A lista lateral exibe a UMB de cada material.
  - O controle e a regra de zoom foram removidos.
  - Evolucao e total contam cada evento operacional uma unica vez; Requisicao, Devolucao e Retorno de campo seguem a classificacao de `stock_transfer_team_operations`.
  - A dispersao conta operacoes distintas pela chave `data + equipe + projeto + status` para cada material.
  - A dispersao exibe cards por UMB com quantidade movimentada, alternando conforme o botao ativo `Requisicao` ou `Devolucao`.
  - A leitura dos itens de movimentacao passa a buscar `stock_transfer_items` e `materials` separadamente, evitando falha de relacionamento no carregamento do dashboard.
  - As leituras por listas de movimentacoes passam a usar lotes menores em filtros `.in(...)` e registram log tecnico quando `stock_transfer_items` falhar.
  - O resumo por UMB do estoque passa a ficar dentro do bloco `Estoque`.
  - A dispersao passa a exportar a tabela da operacao ativa em CSV compativel com Excel.
- Impacto multi-tenant:
  - Sem mudanca de schema, migration ou politica RLS.
  - As consultas permanecem filtradas pelo `tenant_id` autenticado.

Atalhos
-------
- Rota ativa: `/dash-estoque`
- Endpoint:
  - `GET /api/dash-estoque`
- Page key:
  - `dash-estoque`

Atualizacao 2026-05
-------------------
- Arquivos criados:
  - `src/app/(dashboard)/dash-estoque/page.tsx`
  - `src/app/api/dash-estoque/route.ts`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `supabase/migrations/186_create_stock_dashboard_page.sql`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
- Arquivos alterados:
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - `TASKS.md`
- Antes:
  - Nao existia tela dedicada de dashboard do estoque com os 6 graficos definidos.
  - Depois:
    - `/dash-estoque` passa a exibir os indicadores iniciais de saldo, giro, criticidade, ABC, evolucao e dispersao Requisicao/Devolucao.
    - A sidebar passa a exibir `Dashboard Estoque` em `Visao Geral`.
    - A matriz de permissoes passa a reconhecer `dash-estoque`.
    - A tela `Home` deixa de usar o rotulo visual `Dashboard Estoque`.
    - A dispersao passa a ter modal ampliado, escala `Raiz`/`Linear`, lista completa com rolagem, foco individual por material selecionado e legenda de cores por faixa de quantidade.
    - Os filtros passam a executar nova consulta somente pelo botao `Filtrar`, sem recarregamento automatico ao alterar campos.
    - A evolucao passa a contar operacoes distintas por mes e tipo, sem somar quantidades de UMB diferentes.
    - A Curva ABC passa a alternar entre classificacao por valor e por quantidade em estoque.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/app/api/dash-estoque/route.ts`
  - Responsavel por: autenticar usuario, validar permissao da pagina, carregar centros fisicos, saldos, movimentacoes, itens, operacoes de equipe, projetos e equipes, e devolver agregados para os 6 graficos.
- Caminho: `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - Responsavel por: composicao da pagina. Chama `useStockDashboard()` e distribui os dados para os componentes; nao contem estado, fetch, calculo nem formatacao.
- Caminho: `src/modules/dashboard/dash-estoque/hooks.ts`
  - Responsavel por: `useStockDashboard()` - estado dos filtros, `loadDashboard()`, memos derivados da dispersao, handlers de interacao e exportacoes CSV.
- Caminho: `src/modules/dashboard/dash-estoque/helpers.ts`
  - Responsavel por: formatadores, escalas, paleta e faixas de quantidade da dispersao. Funcoes puras, sem React.
- Caminho: `src/modules/dashboard/dash-estoque/types.ts`
  - Responsavel por: tipos do payload de `GET /api/dash-estoque` e tipos de estado compartilhados.
- Caminho: `src/modules/dashboard/dash-estoque/components/`
  - `DashboardFilters.tsx` - card de filtros.
  - `StockSummary.tsx` - cards de resumo e bloco `Estoque` por UMB.
  - `ScatterChart.tsx` - grafico de dispersao e lista lateral.
  - `ScatterUnitStrip.tsx` - filtro por UMB.
  - `ScatterViewControls.tsx` - controles segmentados de operacao e escala, compartilhados pelo card e pelo modal ampliado.
  - `ScatterExpandedModal.tsx` - modal ampliado da dispersao.
  - `BarList.tsx` - barras de criticos/zerados e top saldo.
  - `IdleChart.tsx` - colunas de materiais sem giro.
  - `AbcChart.tsx` - barra empilhada e tabela da Curva ABC.
  - `EvolutionChart.tsx` - barras mensais de evolucao.
  - `MaterialDetailsModal.tsx` - modal de detalhamento de materiais.
- Caminho: `src/app/(dashboard)/dash-estoque/page.tsx`
  - Responsavel por: expor a rota da tela no App Router.
- Caminho: `src/components/layout/AppShell.tsx`
  - Responsavel por: incluir item de menu e titulo/subtitulo da rota.
- Caminho: `src/lib/auth/authorization.ts`
  - Responsavel por: mapear `/dash-estoque` para `page_key = dash-estoque`.
- Caminho: `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - Responsavel por: exibir `Dashboard Estoque` na matriz de permissoes.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - Responsavel por:
    - `operationLabels`
    - `ScatterScale`
    - `evolutionKeys`
    - `evolutionEstimatedValueHelp`
    - `DASH_IN_FILTER_CHUNK_SIZE`
    - periodo inicial do ano corrente
- Caminho: `src/components/ui/ActionIcon.tsx`
  - Responsavel por: icones compartilhados de acoes; inclui `info` para ajuda contextual.
- Caminho: `supabase/migrations/186_create_stock_dashboard_page.sql`
  - Responsavel por: inserir/atualizar `app_pages`, `role_page_permissions` e `app_user_page_permissions` para `dash-estoque`.

Funcoes utilitarias relacionadas
- Caminho: `src/app/api/dash-estoque/route.ts`
  - Responsavel por:
    - `ensureDashPageAccess`
    - `resolveDashContext`
    - `loadPhysicalStockCenters`
    - `loadBalances`
    - `loadTransfers`
    - `loadTransferItems`
    - `loadMovementMaterials`
    - `loadTeamOperations`
    - `loadProjects`
    - `loadTeams`
    - `loadReversalSets`
    - `buildCriticalRows`
    - `buildTopBalanceRows`
    - `buildMaterialDetail`
    - `buildIdleBuckets`
    - `buildAbcRows`
    - `buildEvolutionRows`
    - `buildFallbackOperationEventId`
    - `OperationEventAggregate`
    - `buildScatterUnitSummary`
    - `buildScatterRows`
- Caminho: `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - Responsavel por:
    - `BarList`
    - `IdleChart`
    - `AbcChart`
    - `abcMode`
    - `EvolutionChart`
    - `ScatterChart`
    - `ScatterUnitStrip`
    - `exportScatterRows`
    - `formatExportDate`
    - `scaleScatterValue`
    - `scatterOffset`
    - `scatterQuantityPalette`
    - `buildScatterQuantityBands`
    - `getScatterQuantityPalette`
    - `getScatterQuantityBand`
    - `hslToHex`
    - `MaterialDetailsModal`
    - `openIdleMaterials`
    - `openAbcMaterials`
    - `exportMaterialModalRows`
    - `formatFileNamePart`
    - `normalizeTextForFileName`
    - `evolutionEstimatedValueHelp`
    - `selectedScatterMaterialId`
    - `materialModal`
    - `isExportingMaterialModal`
    - `selectedScatterPoint`
    - `handleScatterOperationChange`
    - `hasLoadedInitialDashboard`
    - `loadDashboard`

Servicos e integracoes externas
- Supabase Postgres com RLS.
- Tabelas consultadas:
  - `stock_center_balances`
  - `stock_transfers`
  - `stock_transfer_items`
  - `stock_transfer_team_operations`
  - `stock_transfer_reversals`
  - `stock_transfer_item_reversals`
  - `stock_centers`
  - `materials`
  - `teams`
  - `project`
  - `app_user_page_permissions`
  - `role_page_permissions`

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/(dashboard)/dash-estoque/page.tsx`
  - `src/app/api/dash-estoque/route.ts`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`
  - `src/modules/dashboard/dash-estoque/StockDashboardPageView.module.css`
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
  - `supabase/migrations/186_create_stock_dashboard_page.sql`
  - `docs/Tela_Dash_Estoque_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `StockDashboardPage`
  - `StockDashboardPageView`
  - `loadDashboard`
  - `BarList`
  - `IdleChart`
  - `AbcChart`
  - `EvolutionChart`
  - `ScatterChart`
  - `scaleScatterValue`
  - `scatterOffset`
  - `GET /api/dash-estoque`
  - `ensureDashPageAccess`
  - `resolveDashContext`
  - `loadPhysicalStockCenters`
  - `buildScatterRows`
  - `menuSections`
  - `titleMap`
  - `permissionCatalog`
  - `resolveDefaultPageAccess`
  - `ROUTE_PAGE_KEYS`
- Comportamento antes/depois:
  - Antes: nao havia rota `/dash-estoque`.
  - Depois: existe dashboard read-only com 6 graficos iniciais.
  - Antes: `dash-estoque` nao existia em menu/permissao.
  - Depois: `dash-estoque` esta cadastrado em sidebar, autorizacao client-side, catalogo de permissoes e migration de backfill.
  - Antes: a dispersao expandida mantinha muitos pontos sobrepostos e abreviava o eixo Y como `Ops`.
  - Depois: a dispersao expandida permite rolar e selecionar materiais na lista lateral, foca somente o circulo selecionado e exibe `Operacoes` por extenso na horizontal.
  - Antes: todos os circulos da dispersao usavam praticamente a mesma cor por operacao.
  - Depois: a cor dos circulos indica a faixa de quantidade movimentada, com legenda visivel no grafico.
  - Antes: cada alteracao de filtro podia disparar nova carga automaticamente por dependencia do `useEffect`.
  - Depois: a tela faz carga inicial uma unica vez e novas consultas dependem do clique em `Filtrar`.
  - Antes: a evolucao somava quantidades mensais, misturando unidades como `M`, `KG` e `UN`.
  - Depois: a evolucao mostra contagem de operacoes distintas por tipo, que e comparavel mesmo com UMB diferentes.
  - Antes: a Curva ABC era calculada apenas por valor estimado (`saldo x preco`).
  - Depois: a Curva ABC permite alternar entre valor estimado e quantidade em estoque.
  - Antes: a evolucao e o total podiam multiplicar uma operacao pela quantidade de materiais vinculados.
  - Depois: `buildEvolutionRows` e o resumo usam a chave `data + equipe + projeto + status`; a lista da dispersao mostra UMB, nao possui zoom e mantem a legenda fora da area plotada.
- Atualizacao 2026-07-04 - Modal compartilhado de exportacao:
  - Arquivo alterado: `src/modules/dashboard/dash-estoque/StockDashboardPageView.tsx`.
  - Funcoes/estado tocados: `exportScatterRows`, `isExportingScatter`.
  - Antes: exportacao da dispersao gerava CSV sem modal padrao.
  - Depois: os botoes `Exportar Excel` usam `ExportProgressModal` compartilhado durante a geracao.
  - Multi-tenant: sem mudanca em API, query, schema, RLS ou permissao.
