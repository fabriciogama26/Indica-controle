Tela: Atividades SaaS

Visao geral
-----------
- Tela de cadastro de atividades para contratos e servicos por tenant.
- Permite registrar e editar atividades com foco em: `codigo`, `Cod. SAP`, `descricao`, `tipo`, `categoria`, `grupo`, `valor`, `pontos` e `unidade`.
- Cadastro, lista, detalhes e exportacao exibem `Cod. SAP` com origem em `service_activities.code_idd`.
- `grupo` e obrigatorio no cadastro/edicao; `alcance` permanece opcional.
- Lista atividades cadastradas com filtro por codigo, descricao, tipo, categoria e status (`Ativo/Inativo`).
- Lista atividades com paginacao no servidor e botao `Exportar Excel (CSV)`.
- Lista, detalhes e exportacao exibem `Pontos` com origem em `service_activities.voice_point`.
- Lista com acoes de linha: `Detalhes`, `Editar`, `Historico` e `Cancelar/Ativar`.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/atividades/page.tsx`
- View: `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
- Estilo: `src/modules/dashboard/atividades/ActivitiesPageView.module.css`
- API: `src/app/api/activities/route.ts`
- API (metadados): `src/app/api/activities/meta/route.ts`
- Banco: `supabase/migrations/049_create_service_activities_and_page_permissions.sql`
- Banco (evolucao): `supabase/migrations/050_activity_code_precheck_and_optional_fields.sql`
- Banco (historico/status): `supabase/migrations/051_create_app_entity_history_and_activity_status.sql`
- Banco (tipo de equipe em atividades): `supabase/migrations/057_add_team_type_to_service_activities.sql`
- Banco (tipo de servico em atividades): `supabase/migrations/145_create_types_service_activities_and_link_service_activities.sql`
- Banco (categoria obrigatoria em atividades): `supabase/migrations/146_require_service_activity_category_and_update_rpc.sql`
- Banco (ajuste de voz/unidade por planilha JA10183409): `supabase/migrations/148_update_service_activities_voice_fields_ja10183409.sql`
- Banco (desativacao por escopo da carga 147): `supabase/migrations/149_deactivate_service_activities_not_in_147.sql`
- Banco (cadastro/edicao de pontos): `supabase/migrations/171_update_service_activity_voice_point_rpc.sql`
- Banco (grupo obrigatorio em atividades): `supabase/migrations/180_require_service_activity_group_in_rpc.sql`
- Banco (cadastro/edicao de Cod. SAP): `supabase/migrations/372_update_service_activity_code_idd_rpc.sql`
- Banco (correcao de drift do Cod. SAP): `supabase/migrations/406_fix_service_activities_code_idd_text.sql`
- Banco (RPC transacional de escrita/status): `supabase/migrations/077_create_admin_write_rpcs.sql`
- Navegacao e guard de acesso: `src/components/layout/AppShell.tsx`
- Guard de rota por permissao: `src/lib/auth/authorization.ts`
- Catalogo de permissoes por usuario: `src/modules/dashboard/permissoes/PermissionsPageView.tsx`

Cadastro
--------
- Campos obrigatorios:
  - `Codigo`
  - `Descricao`
  - `Tipo`
  - `Categoria`
  - `Grupo`
  - `Valor`
  - `Pontos`
  - `Unidade`
- Campo opcional:
  - `Cod. SAP`
- Regras:
  - `Codigo` e salvo em caixa alta.
  - `Cod. SAP` e opcional; texto vazio e salvo como `NULL`.
  - `Valor` deve ser numerico e maior ou igual a zero.
  - `Pontos` deve ser numerico e maior que zero.
  - `Grupo` deve ser preenchido.
  - `Tipo` deve existir e estar ativo em `team_types` no mesmo tenant.
  - `Categoria` deve existir e estar ativa em `types_service_activities` no mesmo tenant.
  - `Alcance` e opcional e aceita texto livre em formato similar a observacao.
  - `Codigo` deve ser unico por tenant.
  - Pre-validacao de codigo via RPC `precheck_activity_code_conflict` (bloqueia duplicidade antes de inserir/editar).
  - A edicao e a troca de status exigem `expectedUpdatedAt` para bloquear `last-write-wins`.
- Fluxo:
  - `POST /api/activities` para cadastro.
  - `PUT /api/activities` para edicao.
  - `PATCH /api/activities` para cancelamento/ativacao com motivo obrigatorio.

Filtros, auto-complete e listagem
---------------------------------
- Filtros na tela:
  - `Codigo`
  - `Descricao`
  - `Tipo`
  - `Categoria`
  - `Status` (`Todos`, `Ativo`, `Inativo`)
- As opcoes de `Categoria` sao carregadas em ordem alfabetica pelo nome.
- Listagem em tabela com colunas:
  - `Codigo`
  - `Cod. SAP`
  - `Descricao`
  - `Tipo`
  - `Categoria`
  - `Valor`
  - `Pontos`
  - `Unidade`
  - `Registrado em`
  - `Acoes` (`Detalhes`, `Editar`, `Historico`, `Cancelar/Ativar`)
- Filtro e listagem via `GET /api/activities`.
- `GET /api/activities` aceita `status=ATIVO` e `status=INATIVO` para restringir a listagem.
- Historico via `GET /api/activities?historyActivityId=<id>`.
- Query params de paginacao:
  - `page`
  - `pageSize`
- Resposta inclui:
  - `pagination.page`
  - `pagination.pageSize`
  - `pagination.total`
- Header da lista:
  - sem texto fixo de pagina no topo;
  - botao `Exportar Excel (CSV)` para exportar os registros filtrados.
  - o botao fica bloqueado por `10` segundos apos cada clique para evitar exportacoes duplicadas.

Boas praticas
-------------
- Garantir escopo por tenant em todas as operacoes (`tenant_id`).
- Manter validacao de campos obrigatorios no backend.
- Preservar auditoria (`created_by`, `updated_by`, `created_at`, `updated_at`) com trigger padrao.
- Evitar valores negativos no campo `valor`.
- Bloquear edicao de atividade inativa.
- Exigir motivo no cancelamento/ativacao.
- Recarregar a lista quando houver conflito de concorrencia antes de permitir nova tentativa de edicao.
- Preferir RPC transacional para `POST`, `PUT` e `PATCH`, concentrando escrita, historico e concorrencia no banco.

Cancelamento e saldo
--------------------
- Cancelamento/ativacao:
  - Motivo obrigatorio.
  - Atualiza `service_activities.ativo`, `cancellation_reason`, `canceled_at`, `canceled_by`.
  - Registra historico em `app_entity_history` (`change_type` = `CANCEL`/`ACTIVATE`).
- Saldo:
  - Nao se aplica ao modulo de atividades.

Atalhos
-------
- Rota ativa: `/atividades`.
- Exibida na secao `Cadastros` da sidebar.

Atualizacao 2026-04
-------------------
- Migration `163_move_meta_and_activities_to_cadastros.sql` move `Atividades` para a secao `Cadastros` no menu e no catalogo `app_pages`.
- Migration `145_create_types_service_activities_and_link_service_activities.sql` criada para:
  - criar o catalogo `types_service_activities` por tenant com RLS e auditoria;
  - semear os tipos fixos (`POSTE`, `ESTRUTURA`, `CONDUTOR(REDE)`, `EMENDA`, `EQUIPAMENTO`, `MQS CHAVES/PARA-RAIO`, `MANUTENÇÃO`, `RAMAL / CAIXA DAE`, `GERADOR`, `PODA`, `OUTROS`);
  - adicionar `service_activities.type_service` com FK composta (`type_service`, `tenant_id`) para `types_service_activities`.
- Migration `146_require_service_activity_category_and_update_rpc.sql` criada para:
  - tornar `service_activities.type_service` obrigatoria;
  - atualizar RPC `save_service_activity_record` para persistir `type_service`;
  - validar categoria ativa do tenant dentro da propria RPC.
- Migration `148_update_service_activities_voice_fields_ja10183409.sql` criada para:
  - atualizar `voice_point` com base na coluna `Pontos da Voz` de `anexos_JA10183409_as_total.xlsx`;
  - atualizar `unit` com base na planilha `VOZES_JA10183409.xlsx`;
  - atualizar `unit_value` pela regra de `VALOR DO PONTO` por `group_name` (`SOT AEREA`, `SOC`, `PODA`, `LLEE/LINHA VIVA`, `SEGURANCA`) para os codigos informados.
- Migration `149_deactivate_service_activities_not_in_147.sql` criada para:
  - considerar a lista de codigos da migration `147_upsert_service_activities_from_atividades_xlsx.sql` como escopo oficial;
  - desativar (`ativo = false`) atividades ativas fora dessa lista no tenant alvo;
  - preencher `cancellation_reason` e `canceled_at` para manter consistencia da constraint de status;
  - registrar historico em `app_entity_history` com `change_type = CANCEL`.
- Tela/API de `Atividades` atualizadas para:
  - exibir o campo obrigatorio `Categoria` no formulario;
  - retornar opcoes de categoria em `GET /api/activities/meta`;
  - listar, filtrar, detalhar e exportar atividades com a coluna `Categoria`.
- Antes: `service_activities` nao possuia classificacao de tipo de servico dedicada.
- Depois: `service_activities` passa a exigir vinculo de categoria ao catalogo `types_service_activities`, com backfill inicial em `OUTROS`.

Atualizacao 2026-04-13
----------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - `src/app/api/activities/route.ts`
  - `docs/Tela_Atividades_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `ActivityFilterState.status`
  - `buildQuery`
  - `parseActivityStatusFilter`
  - `GET /api/activities` (filtro por status)
- Comportamento antes/depois:
  - Antes: a tela de Atividades nao tinha filtro dedicado por status.
  - Depois: a tela passa a filtrar por `Status` com opcoes `Todos`, `Ativo` e `Inativo`, refletindo no backend e na exportacao filtrada.

Atualizacao 2026-03
-------------------
- Tela `Atividades` criada com formulario funcional e listagem.
- Layout visual ajustado para o mesmo padrao das telas `Projetos` e `Materiais` (cards, bloco de filtros e tabela principal).
- API `src/app/api/activities/route.ts` criada com `GET`, `POST` e `PUT`.
- API evoluida para `GET` (lista + historico), `POST`, `PUT` e `PATCH` (cancelar/ativar), com controle de concorrencia por `expectedUpdatedAt` e escrita delegada para RPC.
- API evoluida com `GET /api/activities/meta` para carregar opcoes de `Tipo` a partir de `team_types`.
- Listagem de atividades ajustada para paginacao server-side (`page/pageSize`).
- Hierarquia de botoes alinhada ao padrao da tela `Projetos` (`primary/secondary/ghost` conforme contexto de acao).
- Migration `049_create_service_activities_and_page_permissions.sql` criada para:
  - criar tabela `service_activities`;
  - aplicar RLS/auditoria;
  - adicionar `atividades` em `app_pages` e backfill de permissoes.
- Migration `050_activity_code_precheck_and_optional_fields.sql` criada para:
  - tornar `group_name` e `scope` opcionais;
  - criar RPC `precheck_activity_code_conflict` para bloqueio de codigo duplicado por tenant.
- Migration `051_create_app_entity_history_and_activity_status.sql` criada para:
  - adicionar campos de cancelamento/ativacao em `service_activities`;
  - criar tabela generica `app_entity_history` reutilizavel para historico de outras telas.
- Migration `057_add_team_type_to_service_activities.sql` criada para:
  - adicionar `service_activities.team_type_id` com FK composta (`team_type_id`, `tenant_id`) para `team_types`;
  - fazer backfill com tipo `PADRAO` por tenant para manter consistencia dos registros existentes.
- Migration `077_create_admin_write_rpcs.sql` criada para:
  - adicionar `save_service_activity_record` para cadastro/edicao transacional;
  - adicionar `set_service_activity_record_status` para cancelamento/ativacao com historico e concorrencia no banco.
- `AppShell`, `authorization.ts` e `PermissionsPageView` atualizados para incluir a nova rota.
- Antes: nao existia a pagina de `Atividades`.
- Depois: pagina ativa com cadastro e edicao por tenant, exigindo `Codigo`, `Descricao`, `Tipo`, `Valor` e `Unidade`.
- Depois: pagina ativa com cadastro e edicao por tenant, exigindo `Codigo`, `Descricao`, `Tipo`, `Valor` e `Unidade`, com lista paginada.
- Depois: pagina ativa com padrao visual de botoes aderente ao baseline de `Projetos`.
- Depois: pagina ativa com detalhes, historico e cancelamento/ativacao com motivo obrigatorio.
- Depois: lista com exportacao CSV e sem texto fixo de paginacao no cabecalho.
- Antes: atividade nao tinha campo `Tipo`.
- Depois: atividade exige `Tipo` no cadastro/edicao e permite filtrar/listar/exportar por `Tipo`.

Atualizacao 2026-04-30
----------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - `src/modules/dashboard/atividades/ActivitiesPageView.module.css`
  - `src/app/api/activities/route.ts`
  - `supabase/migrations/171_update_service_activity_voice_point_rpc.sql`
  - `supabase/migrations/README.txt`
  - `docs/Tela_Atividades_SaaS.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `ActivityItem.voicePoint`
  - `ActivityFormState.voicePoint`
  - `formatPoints`
  - `toInputPoints`
  - `buildActivitiesCsv`
  - `normalizePositiveDecimal`
  - `addDecimalChange`
  - `GET /api/activities`
  - `POST /api/activities`
  - `PUT /api/activities`
  - `save_service_activity_record`
  - `ActivityRow.voice_point`
- Comportamento antes/depois:
  - Antes: `GET /api/activities` nao retornava `service_activities.voice_point` e a tela nao exibia os pontos na lista/detalhes.
  - Antes: o cadastro/edicao de Atividades nao permitia informar `voice_point`.
  - Depois: `GET /api/activities` retorna `voicePoint` por atividade e a tela exibe `Pontos` na lista, no modal de detalhes e na exportacao CSV.
  - Depois: cadastro/edicao passam a exigir `Pontos` maior que zero e persistir `service_activities.voice_point` pela RPC transacional.
  - Depois: alteracoes de `Pontos` entram em `p_changes.voicePoint` e sao registradas em `app_entity_history` no historico da tela.

Atualizacao 2026-05
-------------------
- Arquivos alterados/criados:
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - `src/app/api/activities/route.ts`
  - `supabase/migrations/180_require_service_activity_group_in_rpc.sql`
  - `supabase/migrations/README.txt`
  - `docs/Tela_Atividades_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `ActivityFormState.group`
  - `ActivityFormState.scope`
  - `handleSubmit`
  - `parseActivityInput`
  - `POST /api/activities`
  - `PUT /api/activities`
  - `save_service_activity_record`
- Comportamento antes/depois:
  - Antes: o formulario de Atividades nao exibia `Grupo` nem `Alcance` no cadastro/edicao.
  - Antes: `Grupo` podia chegar vazio no backend/RPC.
  - Depois: o formulario exibe `Grupo *` e `Alcance`, exige `Grupo` no HTML, na API e na RPC transacional, e continua tratando `Alcance` como opcional.
  - Depois: `Alcance` passa a ser exibido como campo de texto longo para aceitar descricao similar a observacao.
  - Depois: alteracoes de `Grupo` e `Alcance` continuam entrando no historico via `p_changes.group` e `p_changes.scope`.

Atualizacao 2026-06
-------------------
- Arquivos alterados/criados:
  - `src/app/api/activities/meta/route.ts`
  - `docs/Tela_Atividades_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `GET /api/activities/meta`
- Comportamento antes/depois:
  - Antes: as categorias eram retornadas pela prioridade operacional de `types_service_activities.sort_order`.
  - Depois: as categorias ativas do tenant sao retornadas em ordem alfabetica por `types_service_activities.name`, refletindo no cadastro e no filtro de Categoria.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - Responsavel por: estado da tela, metadados de tipo/categoria, cadastro/edicao com `Cod. SAP`, `Grupo`, `Alcance` e `Pontos`, filtros (incluindo `Status`), listagem paginada com `Pontos` e `Cod. SAP`, detalhes com `Grupo`, `Alcance`, `Pontos` e `Cod. SAP`, exportacao CSV, historico e cancelamento/ativacao, incluindo envio de `expectedUpdatedAt` e refresh em caso de conflito.
- Caminho: `src/app/api/activities/route.ts`
  - Responsavel por: validar payload, validar `team_type_id`, `category_id`, `group` e `voicePoint`, normalizar `codeIdd`, listar com paginacao/historico, retornar `Grupo`, `Alcance`, `voicePoint` e `code_idd` da atividade, aplicar filtro opcional por status (`ativo`), executar pre-check de codigo e delegar cadastro/edicao/status para as RPCs `save_service_activity_record` e `set_service_activity_record_status`.
- Caminho: `src/app/api/activities/meta/route.ts`
  - Responsavel por: retornar opcoes de `Tipo` (`team_types`) e `Categoria` (`types_service_activities`) ativas do tenant, ordenando categorias alfabeticamente por nome.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - Responsavel por: `INITIAL_FORM` e funcoes de formatacao de valor/pontos.
- Caminho: `supabase/migrations/049_create_service_activities_and_page_permissions.sql`
  - Responsavel por: criar tabela, constraints, RLS, trigger de auditoria e permissao da pagina.
- Caminho: `supabase/migrations/050_activity_code_precheck_and_optional_fields.sql`
  - Responsavel por: flexibilizar campos opcionais e adicionar RPC de pre-validacao de codigo.
- Caminho: `supabase/migrations/051_create_app_entity_history_and_activity_status.sql`
  - Responsavel por: criar historico generico da aplicacao e suporte de status em atividades.
- Caminho: `supabase/migrations/057_add_team_type_to_service_activities.sql`
  - Responsavel por: adicionar vinculacao obrigatoria de atividade com tipo de equipe por tenant.
- Caminho: `supabase/migrations/145_create_types_service_activities_and_link_service_activities.sql`
  - Responsavel por: criar o catalogo de tipos de servico por tenant e vincular `service_activities.type_service` com consistencia de tenant.
- Caminho: `supabase/migrations/146_require_service_activity_category_and_update_rpc.sql`
  - Responsavel por: exigir `type_service` em atividades e atualizar `save_service_activity_record` com validacao de categoria ativa por tenant.
- Caminho: `supabase/migrations/148_update_service_activities_voice_fields_ja10183409.sql`
  - Responsavel por: atualizar `unit`, `voice_point` e `unit_value` para codigos da carga JA10183409, com validacao de codigos e de grupos mapeados.
- Caminho: `supabase/migrations/149_deactivate_service_activities_not_in_147.sql`
  - Responsavel por: desativar atividades fora do escopo de codigos da migration 147 no tenant alvo e registrar auditoria em `app_entity_history`.
- Caminho: `supabase/migrations/171_update_service_activity_voice_point_rpc.sql`
  - Responsavel por: atualizar `save_service_activity_record` para persistir `voice_point` no cadastro/edicao e manter historico por `p_changes`.
- Caminho: `supabase/migrations/180_require_service_activity_group_in_rpc.sql`
  - Responsavel por: atualizar `save_service_activity_record` para rejeitar cadastro/edicao de atividade sem `group_name`, mantendo `scope` opcional.
- Caminho: `supabase/migrations/372_update_service_activity_code_idd_rpc.sql`
  - Responsavel por: atualizar `save_service_activity_record` para persistir `code_idd` no cadastro/edicao e manter historico por `p_changes`.
- Caminho: `supabase/migrations/077_create_admin_write_rpcs.sql`
  - Responsavel por: criar `save_service_activity_record` e `set_service_activity_record_status` para escrita transacional da tela de atividades.
- Caminho: `src/lib/auth/authorization.ts`
  - Responsavel por: mapear `/atividades` para `pageKey = atividades`.

Funcoes utilitarias relacionadas
- Caminho: `src/hooks/useAuth.ts`
  - Responsavel por: fornecer token da sessao para chamadas autenticadas na tela.
- Caminho: `src/lib/server/appUsersAdmin.ts`
  - Responsavel por: resolver usuario autenticado e tenant ativo no backend.

Servicos e integracoes externas
- Supabase Postgres (RLS ON).
- Next.js Route Handler (`/api/activities`).

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/(dashboard)/atividades/page.tsx`
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - `src/modules/dashboard/atividades/ActivitiesPageView.module.css`
  - `src/app/api/activities/route.ts`
  - `src/app/api/activities/meta/route.ts`
  - `src/lib/server/concurrency.ts`
  - `supabase/migrations/049_create_service_activities_and_page_permissions.sql`
  - `supabase/migrations/050_activity_code_precheck_and_optional_fields.sql`
  - `supabase/migrations/051_create_app_entity_history_and_activity_status.sql`
  - `supabase/migrations/057_add_team_type_to_service_activities.sql`
  - `supabase/migrations/145_create_types_service_activities_and_link_service_activities.sql`
  - `supabase/migrations/146_require_service_activity_category_and_update_rpc.sql`
  - `supabase/migrations/148_update_service_activities_voice_fields_ja10183409.sql`
  - `supabase/migrations/149_deactivate_service_activities_not_in_147.sql`
  - `supabase/migrations/171_update_service_activity_voice_point_rpc.sql`
  - `src/components/layout/AppShell.tsx`
  - `src/lib/auth/authorization.ts`
  - `src/modules/dashboard/permissoes/PermissionsPageView.tsx`
- Funcoes/hooks/constantes tocadas:
  - `loadMeta`, `loadActivities`, `handleSubmit`, `startEdit`, `openHistoryModal`, `confirmStatusChange`, `setPage`
  - `GET` de `/api/activities/meta`
  - `GET`/`POST`/`PUT`/`PATCH` de `/api/activities`
  - `saveActivityViaRpc`, `setActivityStatusViaRpc`
  - `save_service_activity_record`, `set_service_activity_record_status`
  - `categoryId`, `categoryName`
  - `voicePoint`, `formatPoints`, `buildActivitiesCsv`
  - `service_activities.type_service`, `types_service_activities`
  - `service_activities.voice_point`, `service_activities.unit`, `service_activities.unit_value`
  - `service_activities.ativo`, `service_activities.cancellation_reason`, `service_activities.canceled_at`
  - `normalizeExpectedUpdatedAt`, `hasUpdatedAtConflict`, `buildConcurrencyConflictResponse`
  - `ROUTE_PAGE_KEYS`, `resolveDefaultPageAccess`, `permissionCatalog`
- Comportamento antes/depois:
  - Antes: rota inexistente para atividades.
  - Depois: rota ativa com cadastro e edicao no tenant atual, com 7 campos obrigatorios (`Codigo`, `Descricao`, `Tipo`, `Categoria`, `Valor`, `Pontos`, `Unidade`), precheck RPC anti-duplicidade, historico, controle de status e bloqueio de sobrescrita concorrente por `expectedUpdatedAt`.
  - Antes: atividade nao possuia tipo vinculado.
  - Depois: atividade passa a exigir tipo de equipe valido por tenant (`team_types`).
  - Antes: atividade nao possuia catalogo dedicado de tipo de servico.
  - Depois: atividade passa a exigir `type_service` (categoria) no cadastro/edicao, com validacao por tenant em API + RPC.
  - Depois: carga JA10183409 ajusta, para codigos especificos, `voice_point` (Pontos da Voz), `unit` (VOZES) e `unit_value` por regra do `VALOR DO PONTO` conforme `group_name`.
  - Depois: atividades fora do escopo de codigos da carga 147 passam a ser desativadas em lote (tenant alvo), com motivo tecnico e historico de cancelamento.
  - Depois: a escrita sensivel da tela passa a ser executada em RPC transacional no banco, em vez de update/historico distribuido na route.
  - Atualizacao (2026-03-24): exportacao `Exportar Excel (CSV)` passou a incluir campos obrigatorios do cadastro e metadados de auditoria (`Status`, `Registrado por/em`, `Atualizado por/em`) alem de `Grupo` e `Alcance`.
- Arquivos alterados/criados (2026-03-24):
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
- Funcoes/hooks/constantes tocadas (2026-03-24):
  - `buildActivitiesCsv`
- Comportamento antes/depois (2026-03-24):
  - Antes: CSV de Atividades nao trazia `Grupo`, `Alcance` nem dados de atualizacao.
  - Depois: CSV de Atividades exporta colunas operacionais completas com trilha de auditoria.
  - Depois: modal de detalhes e CSV de Atividades aplicam fallback `Nao identificado` para nomes de auditoria vazios.
- Atualizacao 2026-07-04 - Modal compartilhado de exportacao:
  - Arquivo alterado: `src/modules/dashboard/atividades/ActivitiesPageView.tsx`.
  - Funcoes/estado tocados: botao de exportacao, `isExporting`.
  - Antes: exportacao usava botao local sem o modal padrao.
  - Depois: exportacao usa `CsvExportButton`, herdando `ExportProgressModal`.
  - Multi-tenant: sem mudanca em API, query, schema, RLS ou permissao.

Atualizacao 2026-08-15 - Formulario nao e mais apagado em conflito de concorrencia
--------------------------------------------------------------------------------------------
- Achado transversal da Auditoria-Concorrencia/2026-08-15-relatorio.md (ponto 7, UX do
  conflito): `ActivitiesPageView.tsx` chamava `resetForm()` automaticamente ao receber
  409 de concorrencia, apagando o que o usuario tinha digitado antes dele decidir o que
  fazer.
- Depois: `resetForm()` so roda apos sucesso; a lista continua sendo recarregada, mas o
  formulario fica preenchido e a mensagem de erro fica visivel.
- Arquivos alterados: `src/modules/dashboard/atividades/ActivitiesPageView.tsx`.
- Validado com `npx tsc --noEmit` e `npm run lint` (0 erros; baseline reduzido via
  `npm run lint:size:update`).

Atualizacao 2026-08-17 - Cod. SAP no cadastro, lista e detalhes
---------------------------------------------------------------
- Arquivos alterados:
  - `src/app/api/activities/route.ts`
  - `src/modules/dashboard/atividades/ActivitiesPageView.tsx`
  - `docs/Tela_Atividades_SaaS.txt`
  - `TASKS.md`
  - `file-size-baseline.json`
  - `supabase/migrations/372_update_service_activity_code_idd_rpc.sql`
  - `supabase/migrations/README.txt`
- Funcoes/hooks/constantes tocadas:
  - `ActivityItem.codeIdd`
  - `ActivityFormState.codeIdd`
  - `buildActivitiesCsv`
  - `handleSubmit`
  - `GET /api/activities`
  - `POST /api/activities`
  - `PUT /api/activities`
  - `ActivityRow.code_idd`
  - `save_service_activity_record`
- Comportamento antes/depois:
  - Antes: `GET /api/activities` nao retornava `service_activities.code_idd`, a lista/modal de detalhes nao exibiam o codigo SAP e o formulario nao permitia cadastrar/editar esse campo.
  - Depois: a API retorna `codeIdd`, o formulario permite cadastrar/editar `Cod. SAP`, a lista de Cadastro de Atividades exibe a coluna, o modal `Detalhes da Atividade` exibe o mesmo campo, e a exportacao CSV inclui `Cod. SAP`.
  - Depois: alteracoes em `Cod. SAP` entram em `p_changes.codeIdd` e sao registradas em `app_entity_history` no historico da tela.
  - Multi-tenant: leitura e escrita continuam filtradas por `tenant_id` derivado da sessao, sem confiar em dados do cliente; a migration 372 restringe a RPC nova a `service_role`.

Cadastro em massa
-----------------
- Botao `Cadastro em massa` aparece no bloco de cadastro quando nao esta em modo de edicao, e fica desabilitado enquanto os catalogos da tela carregam.
- Modelo CSV: `codigo;cod_sap;descricao;tipo_equipe;categoria;grupo;valor;pontos;unidade;alcance`.
- Colunas obrigatorias: `codigo`, `descricao`, `tipo_equipe`, `categoria`, `grupo`, `valor`, `pontos` e `unidade`. `cod_sap` e `alcance` sao opcionais.
- `tipo_equipe` e `categoria` sao informados pelo nome e resolvidos contra os catalogos ativos do tenant.
- `valor` aceita zero ou mais, com virgula ou ponto decimal; `pontos` exige valor maior que zero.
- `codigo` e normalizado para maiusculo e codigo repetido dentro do arquivo e recusado.
- Envio: `POST /api/activities` com `action = BATCH_IMPORT` e `rows`.
- O backend revalida tipo, categoria e o pre-check de codigo duplicado (`precheck_activity_code_conflict`) antes de chamar `saveActivityViaRpc` linha a linha.
- Erros parciais geram CSV de retorno com `linha`, `coluna`, `valor` e `erro`; as linhas validas sao salvas mesmo assim.
- Infraestrutura compartilhada do cadastro em massa:
  - Modal: `src/components/ui/MassImportModal.tsx` (3 etapas: baixar modelo, preencher, enviar).
  - Orquestracao: `src/hooks/useMassImport.ts` (le o arquivo, chama o parser de dominio, envia o lote e monta o CSV de erros).
  - Helpers de CSV: `src/lib/utils/massImport.ts` (`readMassImportCsv`, `buildMassImportTemplateCsv`, `createMassImportErrorReport`).
  - Teto por arquivo: `MASS_IMPORT_ROW_LIMIT` = 500 linhas (`src/lib/constants/massImport.ts`).

Atualizacao 2026-08-25 - Cadastro em massa
--------------------------------------------------------------------------------------------
- `Atividades` passa a ter cadastro em massa por CSV, no mesmo padrao ja existente em `Materiais` e `Pessoas`.
- Antes: so era possivel cadastrar uma atividade por vez pelo formulario.
- Depois: o CSV cadastra ate 500 atividades por arquivo, com pre-validacao no frontend e CSV de erros por linha.
- `src/app/api/activities/route.ts`: novo `importActivityBatch`, que reaproveita `parseActivityInput`, `fetchTeamTypeById`,
  `fetchTypeServiceById`, `precheck_activity_code_conflict` e `saveActivityViaRpc`, com cache por id para nao repetir a mesma
  consulta de catalogo a cada linha do arquivo.
- `buildActivitiesCsv` saiu para `src/modules/dashboard/atividades/csv.ts` e os formatadores (`formatMoney`, `formatPoints`,
  `toInputMoney`, `toInputPoints`, `formatHistoryValue`) para `src/modules/dashboard/atividades/formatters.ts`,
  mantendo a view dentro do baseline.
- Arquivos alterados/criados: `src/app/api/activities/route.ts`, `src/modules/dashboard/atividades/ActivitiesPageView.tsx`,
  `src/modules/dashboard/atividades/massImport.ts`, `src/modules/dashboard/atividades/csv.ts`,
  `src/modules/dashboard/atividades/formatters.ts`.
- Validado com `npx tsc --noEmit`, `npm run lint` e `npm run build`. Validacao manual pendente:
  (1) importar CSV valido e conferir as atividades na lista; (2) importar CSV com `pontos` igual a zero e conferir o CSV de erros;
  (3) importar CSV com categoria inexistente e conferir a recusa da linha.

Atualizacao 2026-08-31 - Tipo passa a ter tela propria de cadastro
-----------------------------------------------------------------
- Etapa 1 do trabalho de dar tela de Cadastro Base aos campos `Tipo`, `Categoria` e `Grupo` de `Atividades`.
- Nenhum arquivo de `Atividades` foi alterado nesta etapa: o campo `Tipo` ja era um `select` alimentado por
  `team_types` atraves de `/api/activities/meta`, e continua igual.
- Antes: o catalogo por tras do campo `Tipo` nao tinha tela nenhuma; incluir ou renomear um tipo exigia acesso direto ao banco.
- Depois: a tela `/tipo-equipe` (Cadastro Base) cadastra, edita, cancela/ativa e audita esse catalogo.
  Detalhe completo em `docs/Tela_Tipo_Equipe_SaaS.txt`.
- A tela chegou a ser publicada como `/tipo-atividade` (migration `401`), mas foi consolidada em `/tipo-equipe`
  pela migration `402`: `Tipo de Atividade` e `Tipo de Equipe` sao a MESMA informacao, as duas liam
  `public.team_types`. O campo `Tipo` de `Atividades` e o campo `Tipo` de `Equipes` compartilham o catalogo.
- Efeito colateral esperado no formulario: um tipo cancelado em `/tipo-equipe` some do `select` `Tipo`, porque
  `/api/activities/meta` ja filtrava por `ativo = true`. Para evitar que uma atividade antiga fique impossivel de
  reeditar, a RPC `set_team_type_record_status` recusa o cancelamento (`TEAM_TYPE_IN_USE`) enquanto houver atividade
  ativa apontando para o tipo.
- Situacao dos outros dois campos nesta etapa:
  - `Categoria`: continua lendo `types_service_activities`, ainda sem tela (etapa 2).
  - `Grupo`: continua texto livre em `service_activities.group_name` (etapa 3).

Atualizacao 2026-08-31 - Categoria passa a ter tela propria de cadastro
-----------------------------------------------------------------------
- Etapa 2 do trabalho de dar tela de Cadastro Base aos campos `Tipo`, `Categoria` e `Grupo`.
- Nenhum arquivo de `Atividades` foi alterado nesta etapa: o campo `Categoria` ja era um `select`
  alimentado por `types_service_activities` atraves de `/api/activities/meta`, e continua igual.
- Antes: o catalogo por tras do campo `Categoria` nao tinha tela nenhuma; incluir ou renomear uma
  categoria exigia acesso direto ao banco.
- Depois: a tela `/categoria-atividade` (Cadastro Base) cadastra, edita, cancela/ativa e audita esse
  catalogo. Detalhe completo em `docs/Tela_Categoria_Atividade_SaaS.txt`.
- Nao foi reaproveitada a rota `/tipo-servico`: aquele placeholder e de `project_service_types`, o Tipo
  de Servico do PROJETO, outro dominio com outros leitores.
- Efeito colateral esperado no formulario: uma categoria cancelada em `/categoria-atividade` some do
  `select` `Categoria`, porque `/api/activities/meta` ja filtrava por `ativo = true`. Como `Categoria` e
  campo obrigatorio, a RPC `set_activity_category_record_status` recusa o cancelamento
  (`ACTIVITY_CATEGORY_IN_USE`) enquanto houver atividade ativa apontando para a categoria.
- Situacao do campo restante:
  - `Grupo`: continua texto livre em `service_activities.group_name` (etapa 3).

Atualizacao 2026-08-31 - Grupo passa a ser catalogo (conclui as 3 etapas)
-------------------------------------------------------------------------
- Etapa 3 do trabalho de dar tela de Cadastro Base aos campos `Tipo`, `Categoria` e `Grupo`.
  Diferente das etapas 1 e 2, esta ALTERA a tela `Atividades`.
- Antes: `Grupo` era `input` de texto livre gravado em `service_activities.group_name`; cada usuario
  digitava a sua variante, sem catalogo nem validacao.
- Depois: `Grupo` e `select` obrigatorio alimentado pela tabela nova `activity_groups`, mantida em
  `/grupo-atividade`. Detalhe completo em `docs/Tela_Grupo_Atividade_SaaS.txt`.
- `service_activities` ganhou `group_id` (FK composta com `tenant_id`), e `group_name` PERMANECE como
  snapshot do nome escolhido. Isso preserva intactos a RPC de faturamento minimo (`212`, que casa por
  `normalize_minimum_billing_token(group_name)`), `/api/locacao/activities/catalog` e
  `/api/apuracao-fator-minimo`.
- Quem grava as duas colunas e a RPC: a tela manda so o id e `save_service_activity_record` resolve o nome
  no catalogo, entao nao existe par id/nome inconsistente.
- Contrato da API mudou:
  - `POST`/`PUT /api/activities` passam a exigir `groupId` no lugar de `group`.
  - `GET /api/activities` devolve `groupId` e `groupName` (antes devolvia `group`).
  - `GET /api/activities/meta` passa a devolver `groups`.
  - Grupo invalido ou inativo devolve `422` com `INVALID_GROUP`.
- Importacao em massa: a coluna `grupo` do CSV continua sendo o NOME, resolvido contra o catalogo. Nome fora
  do catalogo agora e recusado com "Grupo invalido ou inativo" em vez de virar um grupo novo em texto.
- Efeito colateral esperado: um grupo cancelado em `/grupo-atividade` some do `select`. Como `Grupo` e
  obrigatorio, a RPC `set_activity_group_record_status` recusa o cancelamento (`ACTIVITY_GROUP_IN_USE`)
  enquanto houver atividade ativa vinculada.
- Renomear um grupo na tela de cadastro propaga o novo nome para o `group_name` das atividades vinculadas.
- `CatalogSelectField` foi extraido de `ActivitiesPageView`: os campos `Tipo`, `Categoria` e `Grupo` tinham
  marcacao identica. A extracao devolveu o espaco do campo novo — o arquivo caiu de 1267 para 1244 linhas e
  o baseline do ratchet foi travado para baixo com `npm run lint:size:update`.
- Com esta etapa, os tres campos de catalogo de `Atividades` tem tela propria em `Cadastro Base`:
  `Tipo` -> `/tipo-equipe`, `Categoria` -> `/categoria-atividade`, `Grupo` -> `/grupo-atividade`.

Atualizacao 2026-09-01 - CSV de erros do cadastro em massa
----------------------------------------------------------
- Ajustado o tratamento de erro do cadastro em massa de `Atividades` para nao retornar apenas
  "Falha ao salvar atividade." quando a RPC `save_service_activity_record` falha tecnicamente.
- Depois: erros conhecidos de salvamento sao mapeados para mensagens especificas e codigos de dominio:
  `ACTIVITY_RPC_SCHEMA_MISMATCH`, `ACTIVITY_CODE_IDD_TYPE_MISMATCH`, `DUPLICATE_ACTIVITY_CODE`,
  `INVALID_TEAM_TYPE`, `INVALID_CATEGORY`, `INVALID_GROUP` e `INVALID_ACTIVITY`.
- Depois: o CSV de erros aponta a coluna mais provavel (`codigo`, `tipo_equipe`, `categoria`, `grupo`,
  `cod_sap`, `dados` ou `salvamento`) conforme o codigo retornado pela API.
- Causa real do erro `42804`: em banco vivo, `service_activities.code_idd` estava como `bigint`, enquanto a
  RPC e a tela tratam `Cod. SAP` como `text`. A migration `406_fix_service_activities_code_idd_text.sql`
  converte a coluna para `text` e valida o tipo final.
- Para `ACTIVITY_CODE_IDD_TYPE_MISMATCH`, a mensagem devolvida ao CSV inclui o `EVENT MESSAGE` do Postgres
  com limite de tamanho, porque esse erro ja esta mapeado e o texto identifica a coluna com drift.
- Sem mudanca de RLS, grants ou permissao. O `tenant_id` continua derivado da sessao no backend.
