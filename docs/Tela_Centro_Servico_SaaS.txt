Tela: Centro de Servico SaaS

Visao geral
-----------
- Tela para cadastro e manutencao de centros de servico por tenant.
- Permite cadastrar, editar, consultar, cancelar/ativar, exportar CSV e auditar alteracoes.
- Mantem o catalogo `project_service_centers`, consumido por `Projetos`, `Equipes`, `Programacao` e telas operacionais que exibem base.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/centro-servico/page.tsx`
- View: `src/modules/dashboard/centro-servico/ServiceCentersPageView.tsx`
- CSV: `src/modules/dashboard/centro-servico/csv.ts`
- Fachada frontend: `src/modules/dashboard/centro-servico/index.ts`
- API principal: `src/app/api/service-centers/route.ts`
- Backend: `src/server/modules/service-centers/controller.ts`
- Fachada backend: `src/server/modules/service-centers/index.ts`
- Banco: `project_service_centers`, criada por `supabase/migrations/031_create_project_lookup_tables.sql`
- Historico generico: `app_entity_history`, criada por `supabase/migrations/051_create_app_entity_history_and_activity_status.sql`
- RPC transacional: `supabase/migrations/390_create_service_center_rpcs.sql`

Cadastro
--------
- Campo obrigatorio:
  - `Nome`
- Regras:
  - O nome e normalizado com `trim` antes do envio.
  - A constraint unica `(tenant_id, name_normalized)` impede duplicidade por tenant.
  - Edicao de registro inativo e bloqueada ate reativacao.
  - Edicao e troca de status exigem `expectedUpdatedAt`.
- Fluxos:
  - `POST /api/service-centers` para cadastro.
  - `PUT /api/service-centers` para edicao.
  - `PATCH /api/service-centers` para cancelamento/ativacao com motivo obrigatorio.
  - `POST` e `PUT` chamam `save_service_center_record`.
  - `PATCH` chama `set_service_center_record_status`.

Filtros, auto-complete e listagem
---------------------------------
- Filtros:
  - `Nome`
  - `Status`
- Listagem:
  - Paginacao server-side (`page`, `pageSize`, `total`).
  - Colunas: Nome, Status, Registrado em, Atualizado em, Acoes.
  - Acoes: `Detalhes`, `Editar`, `Historico`, `Cancelar/Ativar`.
  - Botao `Exportar Excel (CSV)` no cabecalho.
  - Exportacao usa `mode=export` e exige permissao `export` no backend.
  - O botao de exportacao fica bloqueado por `10` segundos apos cada clique.
- Historico:
  - Fonte: `app_entity_history`
  - `module_key`: `centro-servico`
  - `entity_table`: `project_service_centers`
  - Modal com paginacao fixa de `5` registros por pagina.

Boas praticas
-------------
- Todas as operacoes usam `tenant_id` derivado da sessao no backend.
- A tela usa `useAuth()` como unica fonte de sessao no client.
- Erros da tela sao registrados com `useErrorLogger("centro-servico")`.
- Icones de acoes reutilizam `ActionIcon`.
- Exportacao reutiliza `CsvExportButton` e `downloadCsvFile`.

Cancelamento e saldo
--------------------
- Cancelamento/ativacao usa o campo `ativo` de `project_service_centers`.
- O motivo fica registrado em `app_entity_history`, pois a tabela base nao possui colunas `cancellation_reason`, `canceled_at` e `canceled_by`.
- Saldo:
  - Nao se aplica ao modulo de centro de servico.

Atalhos
-------
- Rota principal: `/centro-servico`
- API:
  - `GET/POST/PUT/PATCH /api/service-centers`

Atualizacao 2026-08
-------------------
- Tela `Centro de Servico` saiu de placeholder para implementacao completa de cadastro base.
- Antes: a rota `/centro-servico` exibia apenas `ModulePlaceholder`.
- Depois: `/centro-servico` permite cadastrar, editar, listar, detalhar, consultar historico, cancelar/ativar e exportar centros de servico do tenant.
- Depois: a API `/api/service-centers` aplica autorizacao granular por `page_key = centro-servico` nas acoes `read`, `create`, `update`, `cancel` e `export`.
- Depois: alteracoes usam `expectedUpdatedAt` dentro das RPCs com `SELECT ... FOR UPDATE` para evitar sobrescrita silenciosa.
- Criada a migration `390_create_service_center_rpcs.sql` com `save_service_center_record` e `set_service_center_record_status`, ambas `SECURITY DEFINER`, com `EXECUTE` apenas para `service_role`.
- A tabela, RLS, constraint unica, auditoria basica e permissao de pagina ja existiam nas migrations historicas.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/app/(dashboard)/centro-servico/page.tsx`
  - Responsavel por: montar a view operacional da rota `Centro de Servico`.
- Caminho: `src/modules/dashboard/centro-servico/ServiceCentersPageView.tsx`
  - Responsavel por: formulario, filtros, listagem, exportacao, detalhes, historico e status.
- Caminho: `src/app/api/service-centers/route.ts`
  - Responsavel por: expor os handlers HTTP da API.
- Caminho: `src/server/modules/service-centers/controller.ts`
  - Responsavel por: validar sessao, permissao, tenant, paginar listagem e chamar as RPCs transacionais.
- Caminho: `supabase/migrations/390_create_service_center_rpcs.sql`
  - Responsavel por: salvar cadastro/status e registrar historico na mesma transacao.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/centro-servico/ServiceCentersPageView.tsx`
  - Responsavel por: `PAGE_SIZE`, `HISTORY_PAGE_SIZE`, `EXPORT_PAGE_SIZE`, `INITIAL_FORM`, `INITIAL_FILTERS`, `HISTORY_FIELD_LABELS`.

Funcoes utilitarias relacionadas
- Caminho: `src/modules/dashboard/centro-servico/csv.ts`
  - Responsavel por: montar o CSV de centros de servico.
- Caminho: `src/lib/server/concurrency.ts`
  - Responsavel por: normalizar e comparar `expectedUpdatedAt`.
- Caminho: `src/hooks/useErrorLogger.ts`
  - Responsavel por: registrar falhas da tela com tag `centro-servico`.

Servicos e integracoes externas
- Supabase Postgres com RLS.
- Next.js Route Handler (`/api/service-centers`).

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/(dashboard)/centro-servico/page.tsx`
  - `src/app/api/service-centers/route.ts`
  - `src/modules/dashboard/centro-servico/ServiceCentersPageView.tsx`
  - `src/modules/dashboard/centro-servico/csv.ts`
  - `src/modules/dashboard/centro-servico/index.ts`
  - `src/server/modules/service-centers/controller.ts`
  - `src/server/modules/service-centers/index.ts`
  - `supabase/migrations/390_create_service_center_rpcs.sql`
  - `docs/Tela_Centro_Servico_SaaS.txt`
  - `TASKS.md`
- Comportamento antes/depois:
  - Antes: tela informativa sem persistencia.
  - Depois: tela operacional completa no padrao SaaS de cadastros.
