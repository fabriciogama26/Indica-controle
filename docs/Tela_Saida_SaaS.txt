Tela: Operacoes de Equipe SaaS

Visao geral
-----------
- Tela operacional dedicada a `Requisicao`, `Devolucao` e `Retorno de campo` entre um `Centro de estoque` proprio, o `Centro de estoque proprio` vinculado a uma `Equipe` e o centro tecnico `CAMPO / INSTALADO`.
- Reaproveita o mesmo ledger de estoque (`stock_transfers`, `stock_transfer_items`, `stock_center_balances`) para manter saldo, historico e estorno consistentes.
- O fluxo exige `Projeto`, `Equipe`, `Centro de estoque`, `Material`, `Quantidade`, `Data da operacao` e respeita as mesmas regras de rastreio por serial da movimentacao de estoque.

Arquitetura
-----------
- Rota: `src/app/(dashboard)/saida/page.tsx`
- View: `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
- Estilo: `src/modules/dashboard/saida/TeamStockOperationsPageView.module.css`
- Tipos: `src/modules/dashboard/saida/types.ts`
- Constantes: `src/modules/dashboard/saida/constants.ts`
- Utilitarios: `src/modules/dashboard/saida/utils.ts`
- API (meta): `src/app/api/team-stock-operations/meta/route.ts`
- API (serial options): `src/app/api/team-stock-operations/serial-options/route.ts`
- API (create/list/history): `src/app/api/team-stock-operations/route.ts`
- API (batch import): `src/app/api/team-stock-operations/import/route.ts`
- API (reversal): `src/app/api/team-stock-operations/reversal/route.ts`
- Helper server: `src/lib/server/teamStockOperations.ts`
- Integracao em equipes: `src/app/api/teams/route.ts`
- Integracao em estoque atual: `src/app/api/stock-balance/route.ts`
- Banco:
  - `supabase/migrations/140_create_team_stock_operations.sql`
  - `supabase/migrations/143_create_team_stock_operation_batch_full.sql`
  - `supabase/migrations/144_add_field_return_team_operations.sql`

Cadastro
--------
- Campos obrigatorios:
  - `operationKind` (`REQUISITION`, `RETURN` ou `FIELD_RETURN`)
  - `stockCenterId`
  - `teamId`
  - `projectId`
  - `materialId`
  - `quantity`
  - `entryDate`
  - `entryType`
- Campos opcionais:
  - `serialNumber`
  - `lotCode`
  - `notes`
- Regras operacionais:
- `REQUISITION`: `Centro de estoque -> Centro da equipe`
- `RETURN`: `Centro da equipe -> Centro de estoque`
- `FIELD_RETURN`: `CAMPO / INSTALADO -> Centro de estoque`
- A equipe precisa ter `teams.stock_center_id` preenchido e ativo.
- O `Centro de estoque` da tela e sempre um centro `OWN` principal, diferente do centro proprio da equipe e tambem diferente de qualquer outro centro vinculado a equipe.
- O centro da equipe nao e selecionado manualmente na tela; ele e resolvido automaticamente a partir da equipe escolhida.
- O `Retorno de campo` usa o centro tecnico `CAMPO / INSTALADO` como origem tecnica, sem consumir saldo da equipe.
- No `Retorno de campo`, o `Tipo` segue o cadastro do material (`materials.tipo`) e nao e forcado para `SUCATA`.
- Cada operacao salva snapshot de `Equipe` e `Encarregado` no momento do registro, evitando que o historico mude quando o encarregado atual da equipe for alterado depois.
  - `Projeto` e obrigatorio mesmo sendo campo informativo do fluxo.
  - `Tipo` e preenchido automaticamente a partir de `materials.tipo`.
  - `quantity` deve ser maior que zero.
  - Quando `materials.serial_tracking_type <> NONE`, `quantity` deve ser exatamente `1`.
  - Quando `materials.serial_tracking_type <> NONE`, `serialNumber` e obrigatorio; `lotCode` tambem e obrigatorio somente para `TRAFO`.
  - Saldo pendente de identificacao de serial nao pode ser requisitado por equipe; antes da requisicao, a unidade precisa ser identificada no estoque de origem.
  - Em `REQUISITION`, Serial/LP deve existir no centro de estoque principal selecionado.
  - Em `RETURN`, Serial/LP deve existir no centro proprio atual da equipe selecionada.
  - `entryDate` nao pode ser futura.
  - O save ocorre via RPC transacional, sem fallback de gravacao parcial.

Filtros, auto-complete e listagem
---------------------------------
- Cadastro manual:
  - `Projeto` usa `input + datalist`.
  - `Material (codigo)` usa `input + datalist`.
  - `Serial` usa `input + datalist` para material rastreavel em `REQUISITION` e `RETURN`, carregando apenas unidades de `trafo_instances` que estao no estoque de origem real da operacao.
  - `LP` usa `input + datalist` quando o material exige LP (`TRAFO`), filtrando pelos seriais disponiveis na origem real.
  - `Descricao` e `Tipo` sao preenchidos automaticamente a partir do material.
  - O bloco de material funciona com `Adicionar material`, montando uma lista local antes do `Salvar operacao`, no mesmo raciocinio operacional da tela de `Medicao`.
  - A mesma operacao manual aceita varios itens, mas todos precisam compartilhar o mesmo `Tipo` (`NOVO` ou `SUCATA`) porque o ledger grava `entry_type` no cabecalho da transferencia.
  - Material comum nao pode repetir na mesma operacao manual; para material rastreavel a unicidade e por `material + serial` e, quando for TRAFO, tambem `LP`.
  - Em `Retorno de campo`, o frontend nao exige saldo previo no centro da equipe.
  - Em `Retorno de campo`, material rastreavel aceita unidade nova ou unidade previamente `Fora do estoque proprio`, mas bloqueia unidade ja registrada em centro proprio/equipe.
- `Centro de estoque` e `Equipe` usam `select`.
- O select de `Equipe` lista as equipes ativas do cadastro `teams`.
- O select de `Centro de estoque` nao lista centros vinculados a equipes, ativas ou inativas.
- Equipes novas ja nascem com `Centro de estoque proprio` automatico; se existir alguma equipe legada sem esse vinculo, o save continua bloqueado com aviso operacional.
- Cadastro em massa por CSV:
  - Cabecalhos do modelo:
    - `operacao`
    - `centro_estoque`
    - `equipe`
    - `projeto`
    - `material_codigo`
    - `quantidade`
    - `serial`
    - `lp`
    - `data_operacao`
    - `observacao`
  - Alias aceitos para retrocompatibilidade:
    - `operation`, `stock_center`, `team`, `project_code`, `material_code`, `quantity`, `serial_number`, `lot_code`, `entry_date`, `notes`
  - O import valida saldo e posicao da unidade em sequencia, considerando a ordem do arquivo.
  - O import permite repetir a mesma unidade TRAFO no arquivo somente quando a linha anterior ja moveu a unidade para o novo centro esperado.
  - O import e atomico: se qualquer linha falhar na pre-validacao ou no save transacional, nenhuma linha e salva.
  - Erros do lote sobem em modal e tambem geram `CSV` detalhado.
  - Em caso de erro, a tela gera `CSV` com `linha;coluna;valor;erro`.
  - O CSV aceita `RETORNO_DE_CAMPO`/`FIELD_RETURN` e grava `entry_type` conforme o tipo cadastrado do material (`NOVO` ou `SUCATA`).
  - A data do CSV aceita `YYYY-MM-DD`, `YYYY-M-D`, `DD/MM/YYYY` ou `DD/MM/AAAA` (com ou sem sufixo de hora), e o frontend normaliza para `YYYY-MM-DD` antes do envio.
- Filtros da lista:
  - `Data inicial`
  - `Data final`
  - `Operacao`
  - `Equipe`
  - `Projeto`
  - `Material (codigo)`
  - `Categoria`
  - `Subcategoria` (dependente da categoria)
  - `Tipo`
  - `Status de estorno`
- Lista:
  - `Centro estoque`
  - `Equipe`
  - `Origem apoio`
  - `Projeto`
  - `Material`
  - `Descricao`
  - `Quantidade`
  - `Sinalizacao`
  - `Data da operacao`
  - `Atualizado em`
  - `Acoes`
- Acoes por linha:
  - `Detalhes`
  - `Historico`
  - `Estornar`

Permissao por operacao (Requisicao direta)
------------------------------------------
- A operacao `Requisicao` exige a permissao propria `saida-requisicao` (migration 297); `Devolucao`
  e `Retorno de campo` continuam sob a permissao `saida`.
- `saida-requisicao` nasce bloqueada para todos (perfis administrativos seguem liberados por bypass
  de `is_admin`). O admin concede na matriz de permissoes ("Requisicao direta (Operacoes de Equipe)").
- Uso tipico: um solicitante recebe `requisicao-solicitacao` (tela de Solicitacao) + `saida`
  (devolucao/retorno), sem `saida-requisicao` -> ele nao faz Requisicao direta, so solicita.
- Enforcement:
  - `GET /api/team-stock-operations/meta` retorna `canDirectRequisition`; a tela esconde a opcao
    `Requisicao` do seletor do formulario quando falso (o filtro da lista mantem todas as opcoes).
  - `POST /api/team-stock-operations` recusa `operationKind = REQUISITION` sem a permissao (403).
  - `POST /api/team-stock-operations/import` recusa o lote que contiver qualquer linha `REQUISICAO`
    sem a permissao (403).
- Escopo do bloqueio: sem `saida-requisicao` o usuario perde SOMENTE a operacao `Requisicao`.
  `Devolucao` e `Retorno de campo` seguem liberados pela permissao `saida`.
- Default do formulario: `INITIAL_FORM.operationKind` e `REQUISITION`. Quando `canDirectRequisition`
  e falso, tanto a carga do meta quanto `resetForm()` forcam `RETURN`, porque a opcao `Requisicao`
  nao existe no seletor e o form ficaria com um valor invisivel ao usuario (o select exibiria
  `Devolucao` enquanto o estado enviaria `REQUISITION`, gerando 403 em todas as operacoes seguintes).

Boas praticas
-------------
- Manter todo o escopo filtrado por `tenant_id`.
- Escrever via RPC `save_team_stock_operation_record`, nunca com inserts avulsos de cabecalho/itens/saldo.
- No cadastro em massa, usar `save_team_stock_operation_batch_full` para garantir rollback total do lote.
- Manter o vinculo `teams.stock_center_id` exclusivo por equipe para evitar ambiguidade operacional.
- Usar `stock_transfer_team_operations` apenas como mapeamento funcional `transfer -> team`, sem duplicar o ledger principal.
- Registrar erros da tela com `useErrorLogger("operacoes_equipe")`.
- Respeitar cooldown de `10` segundos na exportacao.
- A exportacao CSV da lista chama `GET /api/team-stock-operations?mode=export`, sem percorrer paginas no frontend.
- A exportacao CSV da lista inclui `categoria` e `subcategoria` apos `descricao`, consumindo
  `materials.category_id/subcategory_id` e os catalogos `material_categories/material_subcategories`.
- No submit manual, consolidar todos os materiais com saldo zerado ou insuficiente antes de bloquear a operacao, informando codigo, saldo atual, quantidade solicitada e falta.
- Preservar os detalhes de `INSUFFICIENT_STOCK` retornados pela RPC quando o saldo muda entre a pre-validacao da tela e o save transacional.

Cancelamento e saldo
--------------------
- A correcao operacional e feita por `Estorno`, nao por edicao direta.
- O estorno cria nova movimentacao inversa dentro do mesmo ledger.
- O estorno novo ocorre por item/linha (`stock_transfer_items.id`), permitindo corrigir um material sem reverter os demais itens da mesma operacao.
- Estornos antigos por transferencia inteira continuam preservados em `stock_transfer_reversals`.
- Nao permite duplo estorno do mesmo item.
- Nao permite estorno integral da operacao quando ela ja possui estorno parcial por item.
- O estorno exige:
  - `Motivo padrao do estorno`
  - `Data do estorno`
  - `Observacao` quando o motivo exigir
- O estorno e permitido para `admin/master/user`.
- Impacto no saldo:
  - `REQUISITION`: baixa no centro de estoque e credita no centro da equipe.
  - `RETURN`: baixa no centro da equipe e credita no centro de estoque.
- `FIELD_RETURN`: credita o centro de estoque fisico a partir de `CAMPO / INSTALADO`, sem criar saldo previo na equipe.
- O `Estoque Atual` passa a ler essas operacoes e exibir `Requisicao`, `Devolucao` e `Retorno de campo` no historico.
- A validacao da `/saida` continua podendo consultar centro de equipe em modo tecnico para `Devolucao`, sem expor esses centros na tela publica de `Estoque Atual`.

Atalhos
-------
- Rota ativa: `/saida`
- Exibida na secao `Almoxarifado` da sidebar como `Operacoes de Equipe`
- Endpoints:
  - `GET/POST /api/team-stock-operations`
  - `GET /api/team-stock-operations?mode=history`
  - `GET /api/team-stock-operations?mode=export`
  - `GET /api/team-stock-operations/serial-options`
  - `POST /api/team-stock-operations/import`
  - `POST /api/team-stock-operations/reversal`

Atualizacao 2026-04
-------------------
- Antes: `/saida` apenas redirecionava para `/entrada`.
- Depois: `/saida` passa a operar como modulo proprio para `Requisicao`, `Devolucao` e `Retorno de campo`.
- A tela ganhou formulario manual, filtros, lista paginada, detalhes, historico, estorno e cadastro em massa.
- Ajuste de UX: o cadastro manual agora monta uma lista de itens (`Material + Descricao + Tipo + Serial + LP + Quantidade`) antes do submit final, no mesmo padrao mental de `adicionar e depois salvar` usado na `Medicao`.
- Ajuste de UX: o bloco de materiais passou a ficar visualmente separado do cabecalho da operacao, em um sub-card proprio no mesmo estilo de leitura da `Medicao`.
- Ajuste de UX: o modal de alerta operacional passou a destacar erros em vermelho e as mensagens de saldo/TRAFO passaram a citar explicitamente o codigo do material afetado.
- Ajuste de UX: os textos auxiliares abaixo dos campos `Centro de estoque`, `Equipe` e `Tipo` foram removidos para alinhar o formulario ao padrao visual da Movimentacao de Estoque.
- Ajuste de UX: o botao `Limpar formulario` foi removido e o select de `Equipe` passou a listar todas as equipes ativas, sinalizando quando a equipe ainda esta sem centro proprio.
- Ajuste de UX: o frontend da `/saida` agora reforca o filtro para aceitar somente equipes ativas no formulario e no cadastro em massa, mesmo que algum dado legado escape do backend.
- Ajuste de regra: o campo `Centro de estoque` passou a excluir qualquer centro vinculado a equipe e o centro da equipe ficou 100% implicito pela equipe selecionada.
- `Equipes` passa a gerar automaticamente o `Centro de estoque proprio` no mesmo save do cadastro.
- `Operacoes de Equipe` passa a registrar `team_name_snapshot` e `foreman_name_snapshot` para preservar o historico operacional.
- `Estoque Atual` passa a distinguir `Requisicao` e `Devolucao` no historico do saldo.
- Ajuste de integridade: o cadastro em massa passou a fazer pre-validacao sequencial de saldo/TRAFO e a gravar via RPC atomica, evitando sucesso parcial no lote.
- Ajuste de UX: erros de importacao com falha operacional agora tambem disparam modal, alem do `CSV` de erros por linha/coluna.
- Ajuste de importacao em massa: validacao de data no frontend com normalizacao para `YYYY-MM-DD`, evitando falso bloqueio com formatos variados (`YYYY-M-D`, `DD/MM/YYYY` e data com hora).
- Ajuste de permissao: o estorno da tela/API passou a aceitar tambem perfil `user`.
- Migration `140_create_team_stock_operations.sql`:
  - adiciona `teams.stock_center_id`
  - cria `stock_transfer_team_operations`
  - republica `save_team_record` com suporte ao centro proprio
- Migration `141_auto_create_team_stock_centers_and_foreman_snapshots.sql`:
  - cria/backfill automatico do centro proprio por equipe
  - adiciona snapshot de equipe/encarregado em `stock_transfer_team_operations`
  - republica as RPCs do fluxo para usar os snapshots no historico e no estorno
  - republica `save_team_stock_operation_record`, `reverse_team_stock_operation_record_v2` e `save_team_record`
- Migration `142_block_team_stock_center_as_main_operation_center.sql`:
  - bloqueia no banco o uso de centro vinculado a equipe como `Centro de estoque` principal da operacao
- Migration `143_create_team_stock_operation_batch_full.sql`:
  - cria a RPC atomica `save_team_stock_operation_batch_full` para o cadastro em massa da `/saida`
- Migration `144_add_field_return_team_operations.sql`:
  - adiciona `operation_kind` explicito em `stock_transfer_team_operations`
  - cria o centro tecnico `CAMPO / INSTALADO` por tenant quando necessario
  - adiciona `FIELD_RETURN` na RPC transacional de operacoes de equipe
  - preserva `Retorno de campo` e origem tecnica tambem nos estornos e historicos

Atualizacao 2026-05
-------------------
- O historico modal de `Operacoes de Equipe` passou a tratar o cadastro inicial como evento de `Cadastro`, sem listar todos os campos do registro como alteracoes.
- Campos contextuais como `Equipe`, `Encarregado`, `Operacao` e origem tecnica continuam aparecendo apenas quando fazem parte de evento posterior, como estorno.
- O campo `Quantidade` aceita decimal com virgula ou ponto (`1,5` ou `1.5`), mantendo ate 3 casas decimais e `TRAFO = 1`.
- Materiais `TRAFO`, `RELIGADOR` e `CHAVE` passam a entrar no controle por serial:
  - `TRAFO`: exige `Serial + LP` e quantidade `1`.
  - `RELIGADOR`/`CHAVE`: exigem `Serial` e quantidade `1`; o backend normaliza `LP = '-'`.
- `Retorno de campo` reconhece unidades RELIGADOR/CHAVE novas ou fora do estoque proprio e grava a posicao em `trafo_instances`.
- API/listagem/importacao passam a carregar `materials.serial_tracking_type` e o helper `materialSerialTracking`.
- Migration `172_generalize_serial_tracked_materials.sql` generaliza o rastreio unitario para materiais serializados.
- O modelo CSV do cadastro em massa passa a trazer exemplos de material comum decimal, RELIGADOR com Serial, TRAFO com Serial + LP e CHAVE no retorno de campo.
- O estorno passa a ser independente por item/linha da lista. A UI envia `transferItemId` e a API aciona a RPC de estorno por item, preservando o legado de estorno integral por `transferId`.
- A tabela `stock_transfer_item_reversals` registra o vinculo `item original -> item de estorno` sem alterar registros historicos existentes.
- A listagem passa a ter fallback para ambientes com schema em transicao: se o select completo de `stock_transfer_team_operations` falhar por colunas novas ausentes, a API tenta carregar `transfer_id/team_id` legados para evitar bloqueio total da tela.
- A API tambem registra no log do servidor a etapa tecnica que falhou (`team-operations`, `stock-transfers`, `stock-transfer-items` ou fallback de `materials`), mantendo a resposta publica sem detalhes sensiveis.
- Ajuste de regra: `Requisicao` passa a sugerir e validar Serial/LP a partir do centro de estoque principal, enquanto `Devolucao` passa a usar o centro proprio atual da equipe como origem real.
- Ajuste de UX: o campo `Serial` da tela passa a listar somente unidades registradas em estoque para a origem real da operacao; `LP` tambem recebe lista quando o material exige esse campo.
- Nova API `GET /api/team-stock-operations/serial-options` consulta `trafo_instances` por `tenant_id`, `material_id` e `current_stock_center_id`, sem expor centros de equipe na listagem publica de estoque.
- Ajuste de catalogo operacional: `GET /api/team-stock-operations/meta` passa a carregar materiais ativos em paginas de 1000 registros pelo helper `fetchActiveOperationalMaterials`, evitando que codigos acima do limite padrao do Supabase/PostgREST deixem de aparecer no cadastro manual e no cadastro em massa.
- O helper aplica fallback para schemas em transicao sem `materials.serial_tracking_type`, preservando a leitura por `is_transformer` quando necessario.
- Ajuste de UX: o `Salvar operacao` passa a travar imediatamente com `Salvando...`, spinner e `aria-busy` antes das validacoes assincronas de saldo/serial, evitando duplo clique e duplo envio.
- Atualizacao 2026-05: `save_team_stock_operation_record` passa a chamar `save_stock_transfer_record` com `p_direct_purchase => false`, evitando falha tecnica na resolucao da RPC apos a criacao da assinatura de compra direta.
- Antes: em ambientes com a migration de compra direta aplicada, a requisicao de equipe podia retornar erro generico antes de concluir a validacao transacional de saldo.
- Depois: requisicoes, inclusive para equipe CESTO, seguem pelo fluxo normal de `TRANSFER` entre centro principal e centro proprio da equipe, mantendo o escopo por `tenant_id` e retornando a mensagem real da regra de saldo quando houver bloqueio.
- Ajuste de feedback de saldo: `Salvar operacao` passa a listar todos os materiais bloqueados por saldo zerado ou insuficiente na pre-validacao e o helper `saveTeamStockOperationViaRpc` passa a detalhar `INSUFFICIENT_STOCK` usando os `details` retornados pela RPC base.
- Ajuste de regra: `Retorno de campo` deixa de forcar `entry_type = SUCATA` e passa a respeitar `materials.tipo` no cadastro manual, importacao e RPC `save_team_stock_operation_record`.
- Migration `203_respect_field_return_material_entry_type.sql` republica `save_team_stock_operation_record` preservando `p_entry_type` tambem para `FIELD_RETURN`.
- Atualizacao 2026-05: `save_team_stock_operation_record` passa a chamar a assinatura completa atual de `save_stock_transfer_record`, informando tambem `p_operation_purpose => 'NORMAL'` e `p_balance_correction_reason => null`.
- Antes: apos a migration `206`, requisicoes de equipe podiam cair em erro tecnico de assinatura/overload da RPC base e a API exibia apenas mensagem generica de falha ao salvar.
- Depois: a migration `208_fix_team_stock_operation_purpose_rpc_call.sql` desambigua a chamada para a RPC de estoque, preserva `operation_purpose = NORMAL` nas operacoes de equipe e o helper server mapeia conflito de assinatura para mensagem operacional clara.
- Ajuste complementar: a migration `209_fix_stock_transfer_operation_purpose_overload.sql` remove a ambiguidade interna da propria wrapper de `save_stock_transfer_record` criada na migration `206`, isolando a assinatura direta com compra direta em `save_stock_transfer_record_direct_purchase_v209`.

Mapa de Codigo (funcoes, hooks e constantes)
--------------------------------------------
Funcoes principais
- Caminho: `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - Responsavel por: formulario, filtros, consolidacao de erros de saldo por material, importacao CSV, lista, detalhes, historico, estorno da tela e trava visual/funcional do submit manual.
- Caminho: `src/app/api/team-stock-operations/route.ts`
  - Responsavel por: criar operacoes, listar historico operacional, montar o historico funcional por transferencia, omitir detalhes completos do cadastro inicial e aplicar fallback de leitura para schema em transicao.
- Caminho: `src/app/api/team-stock-operations/import/route.ts`
  - Responsavel por: processar importacao em lote de requisicoes/devolucoes, validar saldo/TRAFO em sequencia e devolver erros por linha.
- Caminho: `src/app/api/team-stock-operations/meta/route.ts`
  - Responsavel por: carregar centros principais, equipes ativas, projetos, catalogo paginado de materiais ativos e motivos de estorno.
- Caminho: `src/app/api/team-stock-operations/serial-options/route.ts`
  - Responsavel por: listar seriais e LPs disponiveis na origem real de `REQUISITION` ou `RETURN` para autocomplete e validacao previa.
- Caminho: `src/app/api/team-stock-operations/reversal/route.ts`
  - Responsavel por: validar payload de estorno e chamar a RPC transacional de reversao por item ou por transferencia legada.
- Caminho: `src/lib/server/teamStockOperations.ts`
  - Responsavel por: encapsular chamadas para `save_team_stock_operation_record`, detalhar `INSUFFICIENT_STOCK` por material, mapear falha tecnica de assinatura da RPC de estoque e chamar `reverse_team_stock_operation_record_v2`/`reverse_team_stock_operation_item_record_v1`.
- Caminho: `supabase/migrations/179_add_stock_transfer_item_reversal.sql`
  - Responsavel por: criar `stock_transfer_item_reversals`, RPC `reverse_stock_transfer_item_record_v1`, RPC `reverse_team_stock_operation_item_record_v1` e preservar estornos integrais legados.
- Caminho: `supabase/migrations/216_fix_reversal_operation_purpose_rpc_calls.sql`
  - Responsavel por: republicar chamadas internas de estorno para a assinatura completa de `save_stock_transfer_record`, mantendo `operation_purpose = NORMAL` e evitando ambiguidade tecnica apos as migrations `206/209`.
- Caminho: `supabase/migrations/203_respect_field_return_material_entry_type.sql`
  - Responsavel por: republicar `save_team_stock_operation_record` para o `Retorno de campo` respeitar o tipo cadastrado do material.
- Caminho: `supabase/migrations/208_fix_team_stock_operation_purpose_rpc_call.sql`
  - Responsavel por: republicar `save_team_stock_operation_record` para chamar `save_stock_transfer_record` com `p_operation_purpose => 'NORMAL'`, evitando conflito de assinatura apos a migration `206`.
- Caminho: `supabase/migrations/209_fix_stock_transfer_operation_purpose_overload.sql`
  - Responsavel por: republicar as wrappers de `save_stock_transfer_record` sem chamada interna ambigua entre a assinatura de 11 parametros e a assinatura de 13 parametros.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/saida/constants.ts`
  - Responsavel por: filtros iniciais, labels do historico e template CSV da tela.
- Caminho: `src/modules/dashboard/entrada/StockTransfersPageView.module.css`
  - Responsavel por: estilo compartilhado dos botoes principais da Movimentacao de Estoque e Operacoes de Equipe, incluindo spinner de salvamento.

Atualizacao 2026-06 - Bloqueio de duplo estorno por item
---------------------------------------------------------
- Causa:
  - A listagem consultava todos os IDs de vinculos de estorno em uma unica chamada `.in(...)`.
  - Quando a consulta ficava grande ou falhava, o erro de `stock_transfer_item_reversals` era ignorado e a linha era tratada como nao estornada.
  - O endpoint de estorno dependia somente da RPC e nao antecipava o estado atual do item.
- Correcao:
  - As consultas de `stock_transfer_reversals` e `stock_transfer_item_reversals` passam a ser executadas em blocos de ate 100 IDs.
  - Qualquer falha na leitura dos vinculos interrompe a listagem com mensagem segura, sem habilitar estorno indevidamente.
  - O endpoint valida se o item pertence a transferencia, se ja foi estornado, se e um item de estorno, se houve estorno integral e se pertence a `Operacoes de Equipe`.
  - A interface impede abrir o modal para linha estornada/estorno, atualiza a linha imediatamente apos o sucesso e recarrega a lista.
  - Erros de validacao e da RPC passam a aparecer dentro do modal de estorno.
- Regras:
  - Uma linha original pode ser estornada somente uma vez.
  - Uma linha criada por estorno nao pode ser estornada novamente.
  - Uma operacao estornada integralmente nao permite novo estorno por item.
  - `transferItemId` deve pertencer ao `transferId` e ao fluxo de `Operacoes de Equipe`.
  - A constraint/RPC no banco permanece como ultima protecao contra concorrencia e duplo estorno.
- Arquivos alterados:
  - `src/app/api/team-stock-operations/route.ts`
  - `src/app/api/team-stock-operations/reversal/route.ts`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `docs/Tela_Saida_SaaS.txt`
  - `TASKS.md`
- Funcoes e constantes:
  - `RELATION_QUERY_CHUNK_SIZE`
  - `chunkValues`
  - `loadRowsInChunks`
  - `loadTeamOperationList`
  - `POST /api/team-stock-operations/reversal`
  - `openReversalModal`
  - `handleConfirmReversal`
- Comportamento antes/depois:
  - Antes: falha silenciosa ao consultar vinculos podia reabilitar o botao de uma linha ja estornada.
  - Depois: a listagem reconhece o vinculo por item em lotes, bloqueia a acao e o backend retorna conflito mesmo em chamada direta.
- Impacto multi-tenant:
  - Todas as consultas de pre-validacao exigem `tenant_id = appUser.tenant_id`.
  - Nao houve mudanca de schema, RLS ou permissao.
- Validacao:
  - `npm.cmd run lint -- src/app/api/team-stock-operations/route.ts src/app/api/team-stock-operations/reversal/route.ts src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `npm.cmd run build`

Atualizacao 2026-06 - CSV de erros do cadastro em massa
--------------------------------------------------------
- Causa:
  - O CSV era criado somente quando existiam erros estruturados por linha.
  - Arquivo vazio, falha tecnica da API/RPC sem `failed_row_number` e excecao de leitura/comunicacao podiam retornar apenas mensagem geral.
  - Sem ocorrencias, `createMassImportErrorReport` retornava `null` e o botao `Baixar erros (CSV)` nao aparecia.
- Correcao:
  - Arquivo vazio ou invalido gera erro na coluna `arquivo`.
  - Falha global da API/RPC gera erro na coluna `processamento`.
  - Resultado tecnico com `rowNumber = 0` e normalizado para uma linha valida no relatorio.
  - Excecao de leitura, processamento ou comunicacao tambem cria um relatorio baixavel.
  - O fallback preserva os erros detalhados de `validationIssues` quando a API consegue identificar linha e coluna.
  - O quadro de resultado usa o relatorio consolidado para calcular o status final.
  - Se houver linhas salvas e linhas no CSV de erros, exibe `Importacao parcial`, total salvo e total com erro.
  - Resultado parcial ou com erro usa destaque visual de erro; somente lote sem ocorrencias usa `Incluido com sucesso`.
- Formato:
  - `linha;coluna;valor;erro`
  - Nome: `operacoes_equipe_import_erros_YYYY-MM-DD.csv`
- Comportamento antes/depois:
  - Antes: alguns erros exibiam alerta sem gerar o botao/arquivo de erros, e erros locais podiam coexistir com o quadro verde `Incluido com sucesso`.
  - Depois: toda falha produz ao menos uma ocorrencia, libera `Baixar erros (CSV)` e aparece no resumo visual com contagem correta.
- Arquivos alterados:
  - `src/app/api/team-stock-operations/import/route.ts`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `docs/Tela_Saida_SaaS.txt`
  - `TASKS.md`
- Impacto multi-tenant:
  - Nenhuma regra de tenant, RLS ou permissao foi alterada.
  - O lote continua atomico; a mudanca afeta somente o diagnostico retornado ao usuario.
- Validacao:
  - `npm.cmd run lint -- src/app/api/team-stock-operations/import/route.ts src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `npm.cmd run build`

Atualizacao 2026-06 - Filtro funcional de Projeto
--------------------------------------------------
- O filtro `Projeto` passa a seguir o comportamento da tela `Medicao`.
- O campo continua digitavel e exibe os projetos ativos do tenant em `datalist`.
- Ao aplicar:
  - resolve o codigo digitado para o `projectId`;
  - exige correspondencia exata com um projeto da lista;
  - bloqueia codigo inexistente com a mensagem `Projeto invalido no filtro. Selecione um projeto da lista.`;
  - normaliza o texto exibido para o codigo oficial do projeto.
- A API recebe `projectId` e compara por igualdade, removendo a busca parcial por texto em `projectCode`.
- A exportacao CSV reutiliza o mesmo filtro efetivo por `projectId` e usa `mode=export` para retornar todos os registros filtrados em uma unica chamada.
- `Limpar` remove tanto o texto pesquisado quanto o identificador aplicado.
- Arquivos alterados:
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `src/modules/dashboard/saida/types.ts`
  - `src/modules/dashboard/saida/constants.ts`
  - `src/app/api/team-stock-operations/route.ts`
  - `docs/Tela_Saida_SaaS.txt`
  - `TASKS.md`
- Impacto multi-tenant:
  - As opcoes continuam vindo do meta restrito ao tenant autenticado.
  - A listagem continua limitada por `tenant_id` e agora filtra pelo UUID exato do projeto.
- Validacao:
  - `npm.cmd run lint -- src/modules/dashboard/saida/TeamStockOperationsPageView.tsx src/modules/dashboard/saida/types.ts src/modules/dashboard/saida/constants.ts src/app/api/team-stock-operations/route.ts`
  - `npm.cmd run build`
- Caminho: `supabase/migrations/140_create_team_stock_operations.sql`
  - Responsavel por: modelo relacional, politicas RLS e RPCs do fluxo operacional por equipe.
- Caminho: `supabase/migrations/141_auto_create_team_stock_centers_and_foreman_snapshots.sql`
  - Responsavel por: auto-criar/backfill do centro proprio da equipe e persistir snapshot de equipe/encarregado nas operacoes.
- Caminho: `supabase/migrations/143_create_team_stock_operation_batch_full.sql`
  - Responsavel por: garantir save atomico no cadastro em massa da tela.
- Caminho: `supabase/migrations/144_add_field_return_team_operations.sql`
  - Responsavel por: abrir `Retorno de campo`, centro tecnico e persistencia explicita do tipo operacional.
- Caminho: `supabase/migrations/196_fix_team_stock_operation_direct_purchase_rpc_call.sql`
  - Responsavel por: republicar `save_team_stock_operation_record` com chamada explicita a `save_stock_transfer_record(..., p_direct_purchase => false)`.

Funcoes utilitarias relacionadas
- Caminho: `src/modules/dashboard/saida/utils.ts`
  - Responsavel por: formatacao de datas, parser CSV, normalizacao da operacao, validacao decimal de quantidade e geracao do relatorio de erros.
- Caminho: `src/lib/server/materialCatalog.ts`
  - Responsavel por: carregar materiais ativos do tenant em paginas, normalizar `serial_tracking_type` e montar opcoes operacionais usadas no datalist e no parser de CSV.
- Caminho: `src/hooks/useErrorLogger.ts`
  - Responsavel por: registrar falhas do modulo.

Servicos e integracoes externas
- Supabase Postgres com RLS
- RPC `save_team_stock_operation_record`
- RPC `reverse_team_stock_operation_record_v2`
- RPC `reverse_team_stock_operation_item_record_v1`
- RPC `reverse_team_stock_operation_batch_v1`
- RPC `reverse_team_stock_operation_batch_v2`

Ajuda contextual
- Arquivos alterados/criados:
  - `src/app/(dashboard)/saida/page.tsx`
  - `src/modules/dashboard/saida/types.ts`
  - `src/modules/dashboard/saida/constants.ts`
  - `src/modules/dashboard/saida/utils.ts`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.module.css`
  - `src/lib/server/materialCatalog.ts`
  - `src/app/api/team-stock-operations/meta/route.ts`
  - `src/app/api/team-stock-operations/serial-options/route.ts`
  - `src/app/api/team-stock-operations/route.ts`
  - `src/app/api/team-stock-operations/import/route.ts`
  - `src/app/api/team-stock-operations/reversal/route.ts`
  - `src/lib/server/teamStockOperations.ts`
  - `src/app/api/teams/route.ts`
  - `src/app/api/teams/meta/route.ts`
  - `src/modules/dashboard/equipes/TeamsPageView.tsx`
  - `src/app/api/stock-balance/route.ts`
  - `src/modules/dashboard/estoque/CurrentStockPageView.tsx`
  - `src/modules/dashboard/estoque/types.ts`
  - `supabase/migrations/140_create_team_stock_operations.sql`
  - `supabase/migrations/141_auto_create_team_stock_centers_and_foreman_snapshots.sql`
  - `supabase/migrations/143_create_team_stock_operation_batch_full.sql`
  - `supabase/migrations/196_fix_team_stock_operation_direct_purchase_rpc_call.sql`
  - `supabase/migrations/208_fix_team_stock_operation_purpose_rpc_call.sql`
  - `supabase/migrations/209_fix_stock_transfer_operation_purpose_overload.sql`
  - `supabase/migrations/216_fix_reversal_operation_purpose_rpc_calls.sql`
  - `docs/Tela_Saida_SaaS.txt`
  - `supabase/migrations/README.txt`
  - `TASKS.md`
- Funcoes/hooks/constantes tocadas:
  - `TeamStockOperationsPageView`, `handleSubmit`, `handleImportCsv`, `handleExportHistory`, `openHistoryModal`, `handleConfirmReversal`
  - `handleSerialNumberChange`, `handleLotCodeChange`, `handleAddFormItem`, `ensureSourceStockAvailability`
  - `formatQuantityValue`, `formatSourceAvailabilitySummary`
  - `GET/POST` de `/api/team-stock-operations`
  - `fetchActiveOperationalMaterials`, `toOperationalMaterialOption`
  - `GET` de `/api/team-stock-operations/serial-options`
  - `POST` de `/api/team-stock-operations/import`
  - `POST` de `/api/team-stock-operations/reversal`
  - `saveTeamStockOperationViaRpc`, `saveTeamStockOperationBatchViaRpc`, `reverseTeamStockOperationViaRpc`
  - `mapTeamOperationRpcErrorMessage`
  - `formatInsufficientStockMessage`
  - `save_team_stock_operation_record`
  - `reverse_stock_transfer_item_record_v1`
  - `reverse_stock_transfer_record_v2`
- Comportamento antes/depois:
- Antes: `/saida` nao tinha fluxo proprio.
- Depois: `/saida` passa a registrar requisicoes, devolucoes e retornos de campo reais com saldo, historico, snapshot de `Equipe/Encarregado` e estorno no mesmo padrao operacional do almoxarifado.
- Antes: o import podia encaminhar data sem normalizacao, delegando toda validacao ao backend.
- Depois: o import ja normaliza/valida data na tela e envia `entryDate` no formato canonico (`YYYY-MM-DD`).
- Antes: o estorno era restrito a perfil administrativo.
- Depois: o estorno passa a aceitar tambem perfil `user`, mantendo os mesmos bloqueios de integridade do fluxo.
- Antes: o historico de cadastro podia listar todos os campos iniciais como `De: - / Para: valor`.
- Depois: o cadastro inicial aparece como evento resumido e o detalhamento fica reservado para alteracoes/estornos reais.
- Antes: o input numerico dependia do comportamento do navegador para aceitar virgula.
- Depois: a quantidade manual aceita virgula ou ponto com validacao propria de decimal positivo.
- Antes: o botao de estorno ficava preso a primeira linha da transferencia e revertia todos os itens da mesma operacao.
- Depois: cada linha pode ser estornada separadamente por `transferItemId`, mantendo bloqueio de duplo estorno por item e preservando os estornos antigos por transferencia inteira.
- Antes: falha de select em colunas recentes de `stock_transfer_team_operations` derrubava a listagem de `/saida` com mensagem generica.
- Depois: a API tenta fallback legado e registra a etapa tecnica no log do servidor para diagnostico sem expor detalhes ao usuario.
- Antes: a validacao manual de serial reutilizava o rastreio publico, que mostra centro fisico de referencia quando a unidade esta com equipe.
- Depois: a tela usa uma consulta propria por centro atual real; `REQUISITION` baixa do centro principal e `RETURN` baixa do centro proprio da equipe antes de creditar o centro principal.
- Antes: o catalogo de materiais ativos do `meta` era carregado em uma unica consulta e podia ficar limitado pelo teto padrao da API.
- Depois: o catalogo e carregado em paginas de 1000 registros, garantindo que materiais ativos do tenant continuem disponiveis no cadastro manual e no CSV mesmo com base grande.
- Antes: `save_team_stock_operation_record` chamava `save_stock_transfer_record` sem informar `p_direct_purchase`, podendo cair em erro tecnico apos a assinatura nova de compra direta.
- Depois: a RPC de equipe informa `p_direct_purchase => false` e preserva a regra de saldo da movimentacao de equipe.
- Antes: falhas de saldo no save final podiam chegar como erro generico, sem apontar os materiais afetados.
- Depois: a UI e o helper server mostram materiais zerados/insuficientes com saldo atual, solicitado e falta.
- Antes: apos a inclusao de `operation_purpose`, a requisicao de equipe podia falhar por chamada ambigua da RPC base `save_stock_transfer_record` ou por chamada interna ambigua da wrapper de estoque.
- Depois: as migrations `208` e `209` chamam a assinatura completa da RPC de estoque como operacao `NORMAL`, removem a ambiguidade interna do overload e o helper server mostra mensagem tecnica acionavel se o banco ainda estiver sem as migrations/schema atualizado.
- Antes: o estorno por item de Operacoes de Equipe podia chegar em erro tecnico generico quando a RPC de estorno chamava `save_stock_transfer_record` sem `p_operation_purpose`.
- Depois: a migration `216` atualiza as RPCs de estorno para informar `p_direct_purchase`, `p_operation_purpose = NORMAL` e `p_balance_correction_reason = null`; o helper de equipe tambem mostra mensagem acionavel quando o banco ainda esta sem a migration/schema atualizado.

Atualizacao 2026-06
===================
- O cadastro em massa passa a reutilizar a tabela temporaria de analise de seriais RET durante todas as linhas da mesma transacao.
- A migration `222_reuse_retired_serial_temp_table_in_batches.sql` altera `save_stock_transfer_record_direct_purchase_v209` para criar `tmp_retired_serial_transfer_items` somente quando necessario e executar `TRUNCATE` antes de cada chamada.
- A migration `223_reuse_stock_transfer_temp_table_in_batches.sql` aplica a mesma correcao em `save_stock_transfer_record_base_v181`, que cria a tabela principal `tmp_stock_transfer_items`.
- Antes: a primeira linha do lote mantinha a tabela temporaria ate o commit e a segunda linha falhava com `relation "tmp_retired_serial_transfer_items" already exists`.
- Diagnostico complementar: depois da `222`, o fluxo avancava ate a funcao-base e falhava pelo mesmo motivo com `relation "tmp_stock_transfer_items" already exists`.
- Depois: cada linha inicia as duas tabelas temporarias vazias, preservando o rollback integral do lote, as validacoes de serial, os saldos e o escopo por `tenant_id`.

Mapa da atualizacao 2026-06
---------------------------
- Caminho: `supabase/migrations/222_reuse_retired_serial_temp_table_in_batches.sql`
  - Responsavel por: corrigir a reutilizacao da tabela temporaria de seriais RET dentro de `save_team_stock_operation_batch_full`.
- Caminho: `supabase/migrations/223_reuse_stock_transfer_temp_table_in_batches.sql`
  - Responsavel por: corrigir a reutilizacao da tabela temporaria principal de itens na funcao-base de estoque.
- Caminho: `supabase/migrations/README.txt`
  - Responsavel por: registrar a finalidade das migrations `222` e `223`.
- Caminho: `docs/Tela_Saida_SaaS.txt`
  - Responsavel por: mapear a causa, a correcao e o comportamento antes/depois da tela.
- Caminho: `TASKS.md`
  - Responsavel por: registrar a correcao concluida conforme o estado atual do codigo.
- Funcoes tocadas: `save_stock_transfer_record_direct_purchase_v209` e `save_stock_transfer_record_base_v181`.
- Constantes tecnicas tocadas: `pg_temp.tmp_retired_serial_transfer_items` e `pg_temp.tmp_stock_transfer_items`.
- Servico relacionado: RPC `save_team_stock_operation_batch_full`.
- Impacto multi-tenant: nenhuma mudanca de autorizacao ou RLS; a RPC continua validando e movimentando somente dados do `p_tenant_id`.

Atualizacao 2026-06 - Estorno em lote por requisicao
-----------------------------------------------------
- A linha da lista passa a abrir o modal de estorno agrupado pela requisicao: `transferId` no cadastro manual e `operation_batch_id` no cadastro em massa.
- O modal lista todos os materiais da requisicao com:
  - codigo e descricao;
  - quantidade;
  - Serial e LP;
  - status `Ativo` ou `Estornado`.
- O fluxo preserva duas acoes:
  - `Estornar material selecionado`: mantem o estorno individual existente.
  - `Estornar lote`: estorna todos os itens ainda ativos da requisicao.
- Lotes parcialmente estornados podem ser concluidos pelo botao de lote; os itens ja estornados sao exibidos e ignorados.
- O estorno em lote e atomico:
  - bloqueia a transferencia e os itens originais durante a operacao;
  - reutiliza as validacoes de saldo, Serial, LP, motivo e data do estorno individual;
  - se qualquer item falhar, todos os estornos executados na mesma chamada sao desfeitos.
- A API passa a exigir `requirePageAction` para `saida/read` no carregamento e `saida/reverse` na gravacao.
- A RPC valida `p_actor_user_id` ativo no mesmo tenant e possui EXECUTE somente para `service_role`.
- Nao ha backfill: a migration adiciona somente uma RPC e usa os vinculos existentes em `stock_transfer_item_reversals`.

Mapa da atualizacao 2026-06 - Estorno em lote
----------------------------------------------
Funcoes principais
- Caminho: `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - Responsavel por: abrir o lote ao clicar na linha, carregar os materiais, manter o estorno individual e executar o estorno em lote.
  - Funcoes tocadas: `openReversalModal`, `closeReversalModal`, `handleConfirmReversal`, `handleConfirmBatchReversal`.
- Caminho: `src/app/api/team-stock-operations/reversal/route.ts`
  - Responsavel por: `GET` dos itens/status do lote e `POST` dos modos `ITEM` e `BATCH`, sempre restritos ao tenant autenticado.
  - Funcoes tocadas: `resolveReversalContext`, `GET`, `POST`.
- Caminho: `src/lib/server/teamStockOperations.ts`
  - Responsavel por: selecionar a RPC individual, integral legada ou em lote e mapear o resultado por item.
  - Funcoes tocadas: `reverseTeamStockOperationViaRpc`, `mapTeamOperationErrorMessage`.
- Caminho: `supabase/migrations/236_add_team_stock_operation_batch_reversal.sql`
  - Responsavel por: criar `reverse_team_stock_operation_batch_v1` com lock, rollback integral, validacao de ator/tenant e retorno dos itens estornados.

Constantes e configuracoes
- Caminho: `src/modules/dashboard/saida/types.ts`
  - Responsavel por: contratos `TeamOperationBatchReversalItem` e `TeamOperationBatchReversalResponse`.
- Caminho: `src/modules/dashboard/saida/TeamStockOperationsPageView.module.css`
  - Responsavel por: linha clicavel, tabela do lote, chips de status e botao de estorno em lote.

Funcoes utilitarias relacionadas
- `requirePageAction`: valida `saida/read` e `saida/reverse`.
- `reverse_team_stock_operation_item_record_v1`: executa e audita cada item dentro da transacao do lote.

Servicos e integracoes externas
- Next.js Route Handler `GET/POST /api/team-stock-operations/reversal`.
- Supabase Postgres via RPC `reverse_team_stock_operation_batch_v1`.

Ajuda contextual
- Arquivos alterados/criados:
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.module.css`
  - `src/modules/dashboard/saida/types.ts`
  - `src/app/api/team-stock-operations/reversal/route.ts`
  - `src/lib/server/teamStockOperations.ts`
  - `supabase/migrations/236_add_team_stock_operation_batch_reversal.sql`
  - `supabase/migrations/README.txt`
  - `docs/Tela_Saida_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Comportamento antes/depois:
  - Antes: o modal mostrava somente o item selecionado e permitia apenas estorno individual.
  - Depois: o modal mostra toda a requisicao e permite escolher entre estorno individual e estorno atomico dos itens ativos.
  - Antes: a RPC integral legada bloqueava qualquer transferencia com estorno parcial.
  - Depois: a nova RPC conclui somente os itens restantes sem duplicar os que ja foram estornados.

Atualizacao 2026-06 - Correcao do agrupamento do cadastro em massa
------------------------------------------------------------------
- Causa:
  - O cadastro manual com varios materiais grava um unico `stock_transfers.id`.
  - O cadastro em massa grava uma transferencia por linha do CSV, embora todas sejam salvas na mesma transacao.
  - O primeiro estorno em lote consultava somente o `transferId` selecionado e, por isso, encontrava apenas um material importado.
- Correcao:
  - `stock_transfer_team_operations.operation_batch_id` passa a identificar as linhas do mesmo contexto de requisicao dentro do arquivo.
  - Novos imports geram um UUID por contexto funcional:
    - operacao;
    - centro principal;
    - equipe;
    - projeto;
    - data;
    - tipo;
    - observacao.
  - O backfill agrupa registros existentes somente quando possuem o mesmo timestamp transacional, usuario e contexto operacional.
  - O modal consulta todos os `transferId` do `operation_batch_id`.
  - Operacoes manuais continuam usando o proprio `transferId`.
  - A RPC `reverse_team_stock_operation_batch_v2` bloqueia e estorna atomicamente os itens ativos de todas as transferencias do grupo.
- Arquivos alterados/criados:
  - `supabase/migrations/237_group_team_stock_imports_for_batch_reversal.sql`
  - `src/lib/server/teamStockOperations.ts`
  - `src/app/api/team-stock-operations/reversal/route.ts`
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `src/modules/dashboard/saida/types.ts`
  - `supabase/migrations/README.txt`
  - `docs/Tela_Saida_SaaS.txt`
  - `README.md`
  - `TASKS.md`
- Funcoes e contratos tocados:
  - `saveTeamStockOperationBatchViaRpc`
  - `reverseTeamStockOperationViaRpc`
  - `GET /api/team-stock-operations/reversal`
  - `handleConfirmBatchReversal`
  - `save_team_stock_operation_batch_full`
  - `reverse_team_stock_operation_batch_v2`
  - `TeamOperationBatchReversalResponse`
- Impacto multi-tenant:
  - `operation_batch_id` sempre e consultado junto de `tenant_id`.
  - O backfill nao agrupa linhas entre tenants.
  - A RPC continua validando ator ativo e acesso ao tenant.
- Comportamento antes/depois:
  - Antes: import com tres materiais criava tres transferencias e o modal mostrava somente uma.
  - Depois: as tres transferencias compartilham `operation_batch_id` e aparecem juntas no modal.

Atualizacao 2026-06 - Regra de serial em Operacoes de Equipe
------------------------------------------------------------
Visao geral
- Operacoes de Equipe continuam exigindo `Serial` para todo material rastreavel.
- A pendencia criada na Movimentacao de Estoque representa estoque fisico sem serial identificado.
- Para `RELIGADOR` e `CHAVE`, quando o usuario informa um serial que ainda nao existe em `trafo_instances`, a RPC pode identificar a unidade no ato consumindo saldo pendente de `stock_serial_pending_balances`.
- `TRAFO` permanece fora da flexibilizacao e continua exigindo unidade previamente registrada no centro de origem com `Serial + LP`.

Cadastro
- `REQUISITION`, `RETURN` e `FIELD_RETURN` rejeitam item rastreavel sem `serialNumber`.
- `TRAFO` continua exigindo `Serial + LP`.
- `RELIGADOR` e `CHAVE` tambem exigem serial quando usados em Operacoes de Equipe, mas o serial pode ser novo quando houver quantidade pendente suficiente no centro de origem, projeto e tipo.
- O autocomplete continua listando unidades ja identificadas no centro de origem, mas `RELIGADOR`/`CHAVE` podem receber digitacao manual do serial para consumir pendencia.

Arquitetura
- A migration `247_allow_pending_serial_identification.sql` reforca `save_team_stock_operation_record` para rejeitar item rastreavel sem serial antes da gravacao.
- A migration `308_identify_pending_serial_in_team_operations.sql` republica `save_team_stock_operation_record` para:
  - validar todos os itens rastreaveis sem efeito colateral;
  - identificar `RELIGADOR`/`CHAVE` pendente via `identify_pending_serial_tracked_unit` quando a unidade ainda nao existe no centro de origem;
  - executar identificacao + `save_stock_transfer_record` em subtransacao, evitando consumir pendencia se o save da operacao falhar;
  - preservar `TRAFO` com validacao de unidade existente no centro de origem.
- A tela e as APIs continuam usando `trafo_instances` para listar unidades ja identificadas e `stock_center_balances` para validar saldo fisico agregado.

Comportamento antes/depois
- Antes: a regra funcional ja exigia serial para material rastreavel na tela.
- Depois da migration `247`: a regra ficou protegida no banco contra chamadas diretas da RPC com item sem serial, mas `RELIGADOR`/`CHAVE` sem unidade identificada ainda eram bloqueados.
- Depois da migration `308`: `RELIGADOR`/`CHAVE` com saldo pendente podem ser identificados no ato da operacao de equipe; sem saldo pendente suficiente, a operacao continua bloqueada.
- Depois da migration `318`: a identificacao tenta consumir primeiro a pendencia do mesmo projeto da operacao e, se ela nao existir, consome pendencia geral do centro (`project_id = null`). A unidade identificada continua ficando vinculada ao projeto da operacao em `trafo_instances.last_project_id`.

Mapa de Codigo (funcoes, hooks e constantes)
- `supabase/migrations/247_allow_pending_serial_identification.sql`
  - `save_team_stock_operation_record`: prevalidacao de `p_items` para bloquear material rastreavel sem serial.
- `supabase/migrations/308_identify_pending_serial_in_team_operations.sql`
  - `save_team_stock_operation_record`: identifica pendencia de serial para `RELIGADOR`/`CHAVE` antes de salvar Requisicao/Devolucao.
- `supabase/migrations/319_allow_generic_pending_serial_identification.sql` (renomeada de 318_*)
  - `identify_pending_serial_tracked_unit`: usa pendencia geral do centro como fallback quando nao ha pendencia especifica do projeto.
- `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `ensureSourceStockAvailability`: `TRAFO` continua validando unidade em `serial-options`; `RELIGADOR`/`CHAVE` validam saldo agregado e deixam a RPC identificar pendencia quando necessario.
- `src/app/api/team-stock-operations/import/route.ts`
  - importacao CSV aceita `RELIGADOR`/`CHAVE` com serial informado ainda nao listado em `trafo_instances`, desde que o saldo de origem seja suficiente.
- `src/lib/server/teamStockOperations.ts`
  - mensagens operacionais para pendencia insuficiente ou identificacao nao permitida.
- `docs/Tela_Saida_SaaS.txt`
  - documenta identificacao de pendencia durante Operacoes de Equipe.

Atualizacao 2026-06 (correcao de brechas de permissao de pagina)
-----------------------------------------------------------------
- Adicionado `requirePageAction` com action `"read"` nas funcoes `loadTeamOperationList` e `loadTeamOperationHistory`
  em `src/app/api/team-stock-operations/route.ts`. Antes: qualquer usuario autenticado podia listar/consultar
  historico de operacoes de equipe ignorando o campo `can_read` no perfil de permissoes da pagina `saida`.
- Adicionado `requirePageAction` com action `"create"` no handler `POST` de
  `src/app/api/team-stock-operations/route.ts`. Antes: qualquer usuario autenticado podia criar operacoes de equipe
  ignorando o campo `can_create` no perfil.
- Importado `requirePageAction` de `@/lib/server/pageAuthorization` no arquivo de rota.
- Todas as outras rotas de `saida` ja possuiam protecao adequada:
  - `PUT` sempre retorna 409 (edicao direta bloqueada por design).
  - `team-stock-operations/reversal/route.ts` ja usava `requirePageAction` com `"reverse"` e `"read"` desde o
    commit de Estornos.

Atualizacao 2026-06-30 (Performance: filtros no banco e limite no historico)
-----------------------------------------------------------------------------
- Arquivo alterado: `src/app/api/team-stock-operations/route.ts`
- O que mudou em `loadTeamOperationList`:
  - `loadTeamOperationRows` ganhou parametro `operationKindFilter`: quando informado, aplica `.eq("operation_kind", ...)` no schema novo; schema legado nao aplica o filtro mas o in-memory guard continua ativo para derivar o tipo via `resolveOperationKind`.
  - Pré-resolucao de `materialCode` agora ocorre em paralelo com `loadTeamOperationRows`: query em `materials WHERE codigo ILIKE '%...%'` retorna os IDs antes de carregar transfers; se nenhum material bater, retorna vazio imediatamente sem tocar `stock_transfers` ou `stock_transfer_items`.
  - `stock_transfers` migrado de query direta `.in("id", transferIds)` para `loadRowsInChunks` (chunks de 100), com `projectId` e `entryType` agora aplicados no banco em vez de em memoria.
  - `stock_transfer_items` migrado para `loadRowsInChunks` com filtro adicional `.in("material_id", materialIdFilter)` quando `materialCode` esta ativo.
  - Filtros in-memory para `operationKind`, `projectId`, `entryType` e `materialCode` mantidos como guards de seguranca (corretos para schema legado; redundantes mas inofensivos para schema novo).
  - `reversalStatus` continua filtrado em memoria (depende de dados de reversoes carregados para os items da pagina).
- O que mudou em `loadTeamOperationHistory`:
  - As 3 queries paralelas em `material_history` (tabela append-only) receberam `.limit(200)`: evita varredura irrestrita para um `transferId` especifico que na pratica tem poucas dezenas de entradas de historico.
- Como validar:
  - Abrir `/saida` sem filtros e verificar que a lista carrega.
  - Filtrar por `Equipe`, `Operacao`, `Material`, `Tipo` e verificar resultados corretos.
  - Abrir historico de uma operacao e verificar que o modal exibe as entradas de historico.
  - DevTools: `GET /api/team-stock-operations` deve retornar `pagination.total` correto.
- Multi-tenant: sem impacto em RLS; todos os filtros incluem `tenant_id`.

Atualizacao 2026-07-16 (Correcao: bloqueio de Requisicao direta afetava Devolucao e Retorno de campo)
-----------------------------------------------------------------------------------------------------
- Causa:
  - `INITIAL_FORM.operationKind` e `REQUISITION` em `src/modules/dashboard/saida/constants.ts`.
  - A carga do meta ja corrigia o valor para `RETURN` quando `canDirectRequisition` era falso, mas
    `resetForm()` reaplicava `INITIAL_FORM` inteiro apos cada save bem sucedido.
  - Como o seletor nao renderiza a opcao `Requisicao` sem a permissao, o `<select>` passava a exibir
    `Devolucao` enquanto o estado do formulario continuava `REQUISITION`.
  - Resultado: a partir da segunda operacao o usuario sem `saida-requisicao` recebia 403
    ("Sem permissao para Requisicao direta") tambem ao tentar Devolucao e Retorno de campo.
- Correcao:
  - `resetForm()` passa a respeitar `canDirectRequisition`: `operationKind: canDirectRequisition ? INITIAL_FORM.operationKind : "RETURN"`.
  - O estado do formulario nunca mais fica com um valor que o seletor nao exibe.
- Comportamento antes/depois:
  - Antes: sem `saida-requisicao`, a primeira operacao funcionava e as seguintes eram bloqueadas.
  - Depois: sem `saida-requisicao`, somente a operacao `Requisicao` fica indisponivel; `Devolucao` e
    `Retorno de campo` funcionam de forma ilimitada.
- Mapa de Codigo:
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
    - `resetForm()`: default de `operationKind` derivado de `canDirectRequisition`.
- Multi-tenant:
  - Sem mudanca de schema, RLS ou permissao. O enforcement do backend
    (`requirePageAction` com `saida-requisicao`) continua identico; a correcao e apenas de estado da UI.
- Como validar:
  - `npx tsc --noEmit`
  - Na matriz de permissoes, deixar um usuario nao admin com `saida` marcado e `Requisicao direta (Operacoes de Equipe)` desmarcado.
  - Logar com esse usuario em `/saida`: o seletor `Operacao` deve mostrar apenas `Devolucao` e `Retorno de campo`.
  - Salvar uma `Devolucao`, aguardar o reset do formulario e salvar uma segunda `Devolucao` / `Retorno de campo`:
    ambas devem concluir sem 403.

Atualizacao 2026-07-27 (Correcao: Atendimento encerrado nao aparecia na lista)
-----------------------------------------------------------------------------
- Causa:
  - `GET /api/team-stock-operations` carregava `stock_transfer_team_operations` sem `range()` e sem
    ordenacao por `created_at`.
  - O Supabase/PostgREST retornava apenas a primeira janela implicita de 1000 linhas; em tenant com mais de
    1000 operacoes, lancamentos recentes gerados pelo Atendimento de Requisicoes podiam ficar fora da tela.
  - Diagnostico em producao: tenant com 1256 operacoes; o pedido `RC0323050671 / MK-03` encerrado em
    `2026-07-27` gerou 38 transferencias de `2026-07-23`, mas 0 apareciam na janela implicita antiga.
- Correcao:
  - `loadTeamOperationRows()` passa a buscar `stock_transfer_team_operations` em paginas de 1000 via
    `.range()` e ordenar por `created_at desc`, mantendo filtros por `tenant_id`, `team_id` e
    `operation_kind`.
  - O fallback de schema legado tambem passa a paginar e ordenar por `created_at desc`.
  - `loadTeamOperationRowByTransfer()` passou a selecionar `created_at` para manter o contrato do tipo unico.
- Comportamento antes/depois:
  - Antes: Atendimento de Requisicoes podia encerrar corretamente e movimentar saldo, mas a lista de
    Operacoes de Equipe nao mostrava os lancamentos se eles estivessem fora das 1000 linhas implicitas.
  - Depois: a lista encontra os lancamentos recentes gerados pelo atendimento; no caso diagnosticado,
    a janela ordenada encontra as 38 transferencias aceitas.
- Mapa de Codigo:
  - `src/app/api/team-stock-operations/route.ts`
    - `TEAM_OPERATION_PAGE_SIZE`: tamanho de pagina interno para leitura segura.
    - `loadTeamOperationRows()`: leitura paginada/ordenada das operacoes de equipe.
    - `loadTeamOperationRowByTransfer()`: select alinhado com `created_at`.
- Multi-tenant:
  - Sem mudanca de schema, RLS ou permissoes. Todas as leituras continuam filtradas por `tenant_id` resolvido
    no servidor.
- Como validar:
  - `cmd /c npx tsc --noEmit`
  - `cmd /c npm run lint`
  - Consultar `/saida` sem filtros ou filtrando por `Equipe = MK-03` e `Projeto = RC0323050671`; devem aparecer
    os lancamentos de Requisicao com data de operacao `2026-07-23`.


Atualizacao 2026-08 - Encarregado do dia pela Composicao de Equipe
------------------------------------------------------------------
- Arquivos alterados/criados:
  - `supabase/migrations/375_stock_operation_foreman_from_composition.sql` (novo)
  - `src/server/modules/composicao-equipe/dayForemen.ts` e `index.ts` (novos)
  - `src/app/api/team-stock-operations/day-foremen/route.ts` (novo)
  - `src/modules/dashboard/saida/components/OperationDetailModal.tsx` (novo)
  - `src/modules/dashboard/saida/components/OperationHistoryModal.tsx` (novo)
  - `src/modules/dashboard/saida/TeamStockOperationsPageView.tsx`
  - `src/modules/dashboard/saida/utils.ts`
- Funcoes/hooks/constantes tocadas:
  - `resolve_team_foreman` (nova) e `resolve_team_foreman_snapshot` (passa a delegar)
  - RPC `save_team_stock_operation_record` (republicada)
  - `fetchDayForemen` (novo modulo server)
  - `handleForemanChange`, `dayForemanTeamId`, efeito de carga de `dayForemen`
  - `resolvePrimaryStockCenterName`, `resolveSupportCenterName` (movidas para `utils.ts`)
- Comportamento antes/depois:
  - Antes: a equipe era escolhida direto no select, e a RPC gravava o encarregado
    lido de `teams.foreman_person_id` — o encarregado do CADASTRO.
  - Depois: novo campo `Encarregado do dia` acima de `Equipe`, alimentado pelas
    composicoes ativas e `WORKING` daquela data. Escolher o encarregado preenche a
    equipe; escolher a equipe direto continua funcionando; trocar a data recarrega
    a lista.
  - Depois: a RPC resolve o encarregado pela cadeia da migration 374 (composicao do
    dia -> historico de nomeacao -> cadastro), usando o `p_entry_date` que ela ja
    recebia. Sem isso a tela deixava escolher pelo encarregado do dia e gravava o do
    cadastro, e a exportacao CSV saia com outra pessoa.
  - Sem composicao lancada na data, o campo fica desabilitado com o texto
    `Sem composicao lancada nesta data` e a selecao manual de equipe continua igual
    ao comportamento anterior. Falha na consulta tambem nao bloqueia a tela.
- Decisoes de estado:
  - O formulario NAO ganhou um campo `foremanPersonId`. O select de encarregado
    deriva de `form.teamId`, entao nao existe um segundo estado capaz de guardar
    valor que a tela nao renderiza.
  - O atalho passa pelo `handleTeamChange` existente, entao a regra de remover os
    itens antes de trocar a equipe continua valendo — o atalho nao e um caminho
    paralelo que fura validacao.
  - Composicao apontando equipe inativa no cadastro e recusada com o nome da
    equipe, em vez de preencher um valor que o select de Equipe nao lista.
- Estrutura:
  - Os modais de detalhe e de historico sairam do `PageView` para `components/`.
    O arquivo caiu de 2.832 para 2.821 linhas mesmo com o campo novo.
  - `resolvePrimaryStockCenterName` e `resolveSupportCenterName` foram para
    `utils.ts`. Elas alimentam tanto o modal quanto a exportacao CSV; mante-las
    dentro do componente de modal deixava a exportacao da tela dependendo de um
    modal.
- Auditoria de CSV (2026-08):
  - As telas que exportam encarregado e leem SNAPSHOT herdaram a correcao sem
    codigo novo: Estornos e Posicao Trafo pela 375; Medicao, Controle de APR,
    Apuracao de Fator Minimo e Dashboard de Equipes pela 374.
  - `Saldo por Equipe` continua no cadastro de proposito: saldo e uma posicao de
    "agora", sem data para resolver.
  - `Mapa de Programacao` segue no cadastro — unico caso em aberto. La a resolucao
    e de LEITURA por linha (equipe + data da ultima etapa de cada obra), nao um
    snapshot gravado, e exige RPC recebendo os pares (equipe, data).
- Limite declarado:
  - Snapshots ja gravados nao mudam. A correcao vale para o que for salvo daqui em
    diante, consequencia da decisao de resolver o encarregado na ESCRITA.
- Impacto multi-tenant:
  - `fetchDayForemen` filtra por `tenant_id` da sessao; nenhum tenant vem do cliente.
  - O endpoint valida `page_key` `saida` + acao `read` antes de qualquer consulta —
    quem opera a Saida nao precisa de acesso a tela de Composicao de Equipe.
  - `resolve_team_foreman`, `resolve_team_foreman_snapshot` e
    `save_team_stock_operation_record` ficaram com EXECUTE apenas para
    `service_role`.

Atualizacao 2026-08-19 (Performance: join no banco e chunks paralelos na listagem/exportacao)
---------------------------------------------------------------------------------------------
- Problema medido:
  - `loadTeamOperationList` carregava TODAS as operacoes do tenant em `stock_transfer_team_operations`
    (paginas de 1000, sequenciais) e so depois buscava os cabecalhos em `stock_transfers` em chunks de
    100 ids, tambem sequenciais. Os filtros de data (`startDate`/`endDate`), `projectId` e `entryType`
    so entravam na SEGUNDA etapa, entao filtrar por periodo nao reduzia o trabalho da primeira.
  - `loadRowsInChunks` era um `for...of` com `await` dentro: zero paralelismo. Com chunk de 100, as
    duas queries de `stock_transfer_item_reversals` (sobre ids de ITEM, nao de transfer) dominavam o
    tempo — em uma base com ~30 mil operacoes e ~80 mil itens o caminho somava mais de 1.400
    requisicoes HTTP sequenciais ao PostgREST.
  - `mode=export` sofre o mesmo custo e ainda devolve todas as linhas (`route.ts`, retorno de
    `loadTeamOperationList`), por isso a exportacao era a operacao mais lenta da tela.
- O que mudou em `src/app/api/team-stock-operations/route.ts`:
  - Nova `loadTeamOperationsWithHeaders`: um unico SELECT em `stock_transfer_team_operations` com
    embed `stock_transfers!inner(...)` (a FK existe desde a migration 140: `transfer_id` e PK e
    referencia `stock_transfers(id)`). Os filtros de cabecalho passam a ser aplicados NO BANCO
    (`stock_transfers.entry_date`, `stock_transfers.project_id`, `stock_transfers.entry_type`),
    junto com `team_id` e `operation_kind`. Isso elimina a etapa de chunks de `stock_transfers`.
  - A funcao mantem FALLBACK explicito: se o embed falhar (relacionamento nao resolvido pelo
    PostgREST, schema legado), ela loga `team-operations-embedded-select` com
    `fallback: "two-step-select"` e executa exatamente o caminho antigo em duas etapas. Nenhuma
    consulta perde comportamento; o pior caso e voltar ao desempenho anterior, nao quebrar.
  - `loadRowsInChunks` passou a rodar em lotes paralelos: `RELATION_QUERY_CHUNK_SIZE` de 100 para
    500 e `RELATION_QUERY_MAX_PARALLEL = 4`. Erro em qualquer chunk do lote continua abortando com
    o mesmo contrato de retorno de antes.
  - Novo helper `loadAllPages`: o loop de paginacao de 1000 em 1000 existia duplicado em tres
    lugares (`loadTeamOperationRows` full, `loadTeamOperationRows` legado, e seria um terceiro no
    caminho novo). Agora e um so.
  - Nova constante `TRANSFER_HEADER_COLUMNS`: a lista de colunas do cabecalho era repetida entre o
    embed e o fallback.
- O que NAO mudou (de proposito):
  - Contrato da rota, nomes de parametros, formato da resposta e `pagination`.
  - `reversalStatus` e `materialCode` continuam como pos-filtro em memoria — dependem de dados de
    estorno e do codigo do material ja resolvidos. A ordenacao final por `updatedAt` segue em JS.
  - Autorizacao: `requirePageAction({ pageKey: "saida", action: "read" })` continua antes de
    qualquer SELECT; nenhum `tenant_id` vem do cliente. O embed ainda aplica
    `.eq("stock_transfers.tenant_id", tenantId)` como barreira redundante alem da RLS.
- Funcoes e constantes:
  - `loadTeamOperationsWithHeaders` (nova)
  - `loadAllPages` (nova)
  - `TRANSFER_HEADER_COLUMNS` (nova)
  - `RELATION_QUERY_MAX_PARALLEL` (nova)
  - `RELATION_QUERY_CHUNK_SIZE` (100 -> 500)
  - `loadRowsInChunks` (sequencial -> lotes paralelos)
  - `loadTeamOperationRows` (agora usa `loadAllPages`; assinatura inalterada)
- Como validar:
  - Abrir `/saida` sem filtros: a lista carrega e `pagination.total` bate com o volume esperado.
  - Filtrar por Equipe, Operacao, Projeto, Material, Tipo e Status de estorno, um a um e combinados,
    e conferir que o resultado e o mesmo de antes da mudanca.
  - Filtrar por periodo curto (ex.: ultimos 7 dias) e comparar o tempo de resposta com o periodo
    aberto: agora o periodo curto deve ser visivelmente mais rapido, o que antes nao acontecia.
  - Exportar CSV com filtro de periodo e sem filtro, conferindo que a contagem de linhas do arquivo
    bate com `pagination.total` da listagem equivalente.
  - Conferir no log do servidor que NAO aparece `[team-stock-operations] load error` com
    `step: "team-operations-embedded-select"`. Se aparecer, o embed nao foi aceito pelo PostgREST e a
    rota esta rodando no fallback lento — o resultado continua correto, mas o ganho nao se aplica.
- Limite declarado:
  - Isto e otimizacao do caminho existente, nao a solucao final da exportacao. O custo continua
    crescendo com o volume porque as tabelas de estorno e de itens ainda sao lidas por chunk e o
    CSV ainda e montado no navegador a partir de JSON. A proxima etapa (RPC dedicada de export
    resolvendo todos os joins no Postgres + resposta `text/csv` direto da rota + indice
    `(tenant_id, transfer_id)` em `stock_transfer_team_operations`) esta registrada em `TASKS.md`.
- Impacto multi-tenant: nenhum. Todas as queries seguem filtrando `tenant_id` da sessao e a RLS
  permanece como ultima barreira.

Atualizacao 2026-08-19b (Exportacao: RPC no Postgres e resposta text/csv em stream)
------------------------------------------------------------------------------------
- Problema que restava depois da 2026-08-19:
  - O embed cortou os round trips de cabecalho, mas `mode=export` ainda percorria TODO o caminho da
    listagem: itens e as duas tabelas de estorno lidas por chunk de ids, `flatMap`/`filter`/`sort`
    em JS, e a rota devolvendo o dataset inteiro em JSON para o navegador montar o CSV linha a linha
    em `handleExportHistory`. O custo continuava crescendo com o volume.
- Migration `379_create_team_operations_export_rpc.sql`:
  - Indice `idx_stock_transfer_team_operations_tenant_transfer (tenant_id, transfer_id)`. Os indices
    que existiam eram `(tenant_id, team_id, created_at desc)` e `(tenant_id, operation_kind,
    created_at desc)` — nenhum servia ao join real, que e por `transfer_id`.
  - Indices `idx_stock_transfer_item_reversals_tenant_original_item` e
    `..._tenant_reversal_item`, que sustentam os dois `EXISTS` de estorno por item.
  - RPC `list_team_operations_export`: `language sql`, `stable`, `security definer`,
    `set search_path = public`. Resolve os joins de material, centro de estoque, projeto, equipe e os
    dois niveis de estorno, aplica todos os filtros da tela e devolve as 15 colunas do CSV ja
    formatadas. Recebe `p_limit`/`p_offset` para a rota paginar e transmitir em pedacos, com default
    de 1.000 — o teto de linhas por resposta do PostgREST deste projeto.
  - Grants: `revoke all` de `public`/`anon`/`authenticated`, `grant execute` apenas para
    `service_role`. `p_tenant_id` vem SEMPRE da sessao resolvida no servidor.
  - Ordenacao `sort_at desc, transfer_id, item_id`: sem ordem total, paginar por `p_offset`
    duplicaria ou puliria linhas entre chamadas sucessivas.
- Novo modulo `src/server/modules/saida/`:
  - `teamOperationExport.ts` + fachada `index.ts`. Contem `buildTeamOperationExportStream`, que
    chama a RPC pagina a pagina e monta um `ReadableStream`. A rota so decide o status HTTP e os
    cabecalhos — regra de negocio saiu do `route.ts`, conforme CLAUDE.md secao 5.
  - A extracao tambem foi necessaria pelo ratchet: com o export inline, `route.ts` chegou a 1.522
    linhas e estourou o teto de 1.500. Com o modulo, ficou em 1.419.
- Rota `GET /api/team-stock-operations?mode=export`:
  - Passa a responder `text/csv; charset=utf-8` com `Content-Disposition: attachment`, em vez de
    JSON. O BOM UTF-8 continua no inicio do arquivo (Excel pt-BR abre acentuacao corrompida sem ele).
  - Lista vazia responde 404 com `{ message }` — o frontend ja tratava essa mensagem.
  - Falha na PRIMEIRA pagina responde 500 em JSON. Falha numa pagina POSTERIOR nao pode virar erro
    HTTP (status e cabecalhos ja foram enviados): o stream e abortado com `controller.error`, para o
    navegador acusar download incompleto em vez de salvar um CSV truncado que parece integro.
  - O ramo de export sai ANTES de `loadTeamOperationsWithHeaders` — nenhuma query do caminho de
    listagem e executada numa exportacao.
- `TeamStockOperationsPageView.handleExportHistory`:
  - Deixou de montar as linhas do CSV. Agora le `response.blob()` e chama `downloadBlobFile` de
    `src/lib/utils/csv.ts`. O tratamento de erro por `response.ok` + `{ message }` foi mantido.
  - Cooldown de 10s preservado (`useExportCooldown`).
- Divergencia assumida (registrar antes que alguem tropece):
  - Os rotulos do CSV (`Requisicao`/`Devolucao`/`Retorno de campo`, `Estorno`/`Estornada`/`Ativa`,
    e a regra de centro primario x centro de apoio) agora existem em DOIS lugares: na RPC, para a
    exportacao, e em `src/modules/dashboard/saida/utils.ts`, para a tabela na tela. Sao consumos
    diferentes (o CSV grava `Ativa`, a tabela nao renderiza rotulo nenhum nesse caso), mas a regra
    de fundo e a mesma. Se um dia mudar o rotulo de uma operacao, os dois lados precisam mudar
    juntos. A alternativa — RPC devolver dados crus e o Node formatar — foi descartada porque
    reintroduziria o passo de transformacao linha a linha que esta tarefa existe para eliminar.
- Autorizacao: sem mudanca. A exportacao continua exigindo `page_key` `saida` + acao `read`, como
  antes. NAO foi promovida para a acao `export` porque isso tiraria a exportacao de quem hoje tem
  apenas leitura — mudanca de permissao e decisao de produto, nao efeito colateral de performance.
- Como validar:
  - `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm run db:security-check`.
  - Aplicar a migration 379 e conferir em `npm run db:migration-list`.
  - Exportar sem filtro e conferir que o arquivo baixa com cabecalho identico ao anterior:
    `operacao;centro_estoque;equipe;encarregado;origem_apoio;projeto;material_codigo;descricao;quantidade;serial;lp;data_operacao;tipo;status;observacao`.
  - Exportar com CADA filtro isolado (equipe, operacao, projeto, material, tipo, periodo, status de
    estorno) e conferir que a contagem de linhas do CSV bate com `pagination.total` da listagem
    equivalente. Esse e o teste que pega divergencia entre a RPC e o caminho da listagem.
  - Exportar um recorte com observacao contendo `;` e conferir que as colunas nao deslocam.
  - Exportar com filtro que nao retorna nada e conferir a mensagem "Nao ha registros para exportar
    com os filtros atuais.".
  - Abrir o CSV no Excel pt-BR e conferir acentuacao.
  - Exportacao acima de 1.000 linhas: conferir que o arquivo tem TODAS as linhas (a rota pagina a
    RPC de 1.000 em 1.000; um bug de offset apareceria como linha repetida ou faltando na virada).
- Lacuna conhecida:
  - A migration 379 NAO foi aplicada nesta maquina: `npm run db:check-link` reporta que o Supabase
    CLI nao esta linkado, entao `db:migration-list`, `db:lint` e `db:security-check-live` nao
    puderam rodar. So o check ESTATICO de `SECURITY DEFINER` foi executado (98 migrations, verde).
    Enquanto a migration nao for aplicada, `mode=export` responde 500 e loga
    `team-operations-export-rpc` — a listagem continua funcionando normalmente.
- Impacto multi-tenant:
  - `p_tenant_id` vem de `resolveAuthenticatedAppUser`, nunca do cliente. Todo join dentro da RPC
    casa `tenant_id` nos dois lados (`stock_transfers`, `stock_transfer_items`, `teams`, `project`,
    `materials` e as duas tabelas de estorno).
  - A RPC e `SECURITY DEFINER` e so `service_role` pode executa-la, entao a RLS nao a protege por
    si — o isolamento vem do `p_tenant_id` obrigatorio no corpo da funcao, e o acesso a rota
    continua barrado por `requirePageAction`.

Correcao 2026-08-19c (Exportacao truncava em 1.000 linhas em silencio)
------------------------------------------------------------------------
- Sintoma que teria ido para producao:
  - A entrega 2026-08-19b pedia `p_limit: 5000` a RPC. O PostgREST deste projeto entrega no maximo
    1.000 linhas por resposta (`db-max-rows`) e NAO sinaliza o corte: devolve 200 com menos linhas
    do que o SQL produziu. A condicao de parada do laco em `buildTeamOperationExportStream` era
    `pageRowCount === EXPORT_RPC_PAGE_SIZE`, ou seja, `1000 === 5000` -> falso na primeira volta.
  - Resultado: o laco nunca rodava, o CSV saia com exatamente 1.000 linhas e nada acusava o
    problema — nem erro, nem aviso, nem log. Num tenant com 4.046 registros o arquivo pareceria
    completo e teria perdido 3.046 linhas.
- Este e o MESMO erro ja cometido no Dashboard Estoque (`TASKS.md`, item do `loadTransfers` com
  `.limit(5000)`), e o mesmo motivo do commit `98783c9`, que tirou `loadTeamOperationRows` da
  "janela implicita de 1000 linhas do PostgREST". A regra do repositorio ja era paginar de 1.000
  em 1.000; a entrega anterior a ignorou.
- Correcao:
  - `EXPORT_RPC_PAGE_SIZE` de 5000 para 1000, com o motivo escrito no proprio arquivo.
  - Default de `p_limit` na RPC de 5000 para 1000 (a migration 379 ainda nao havia sido aplicada
    em nenhum banco, entao foi corrigida no lugar em vez de virar uma 380 para consertar um
    default que nunca rodou).
  - A condicao de parada deixou de comparar com o limite pedido: agora o laco para so em pagina
    VAZIA e avanca o offset pelo numero de linhas REALMENTE recebidas. Assim a exportacao continua
    correta mesmo se o teto do PostgREST for alterado, para mais ou para menos. Custa uma chamada
    extra no fim de cada exportacao, o que e barato perto de truncar sem avisar.
- Licao para as proximas telas:
  - Nunca usar "a pagina veio menor que a pedida" como fim de resultado quando existe um teto de
    linhas imposto pelo servidor no meio do caminho. O unico sinal confiavel de fim e pagina vazia.

Atualizacao 2026-08-19d (Migration 380: realinhar o default de p_limit)
--------------------------------------------------------------------------
- Fato novo: a migration 379 ja havia sido aplicada MANUALMENTE no banco (Dashboard/SQL editor),
  com o default original `p_limit integer default 5000`, antes de a correcao 2026-08-19c ser feita.
  A correcao alterou o arquivo da 379 no lugar, sob a premissa — errada — de que ela nunca tinha
  rodado em lugar nenhum. Resultado: o arquivo em git e o banco vivo passaram a divergir.
- Por que isso importa mesmo sem efeito visivel: `guia_sql.md` regra 2 — migration aplicada e
  historico do banco. Arquivo corrigido no lugar significa que `db reset`, branch de preview ou
  projeto novo nascem com um estado diferente do de producao. Foi exatamente o caso de
  `v_stock_conflicts` (migrations 007 -> 377).
- Correcao: nova migration `380_realign_team_operations_export_default_limit.sql`, que redeclara
  `list_team_operations_export` com o default 1000. Ela foi GERADA por script a partir do corpo real
  da 379, para nao transcrever a funcao a mao (mesmo padrao da 324 gerada a partir da 318). O corpo
  e identico exceto pelo default, e o bloco revoke/grant e repetido conforme `guia_sql.md` regra 17.
- A 380 converge os dois cenarios possiveis sem depender de saber qual versao da 379 foi executada
  em cada ambiente:
  - banco com default 5000 -> passa a 1000;
  - banco com default 1000 -> redeclaracao sem efeito;
  - banco novo montado das migrations -> 379 cria, 380 redeclara, estado final igual ao de producao.
- Efeito no caminho da aplicacao: nenhum. A rota sempre passa `p_limit` explicito
  (`EXPORT_RPC_PAGE_SIZE = 1000`), entao o default nunca e exercido pela tela. O que a 380 remove e
  a armadilha para o proximo chamador — humano no SQL editor ou codigo novo — que invoque a RPC sem
  `p_limit`, peca 5000 e receba 1000 achando que recebeu tudo. E a mesma armadilha da 2026-08-19c,
  so que um nivel abaixo.
- Como conferir o estado real do banco antes de aplicar:
  `select pg_get_function_arguments(oid) from pg_proc where proname = 'list_team_operations_export';`
- Situacao do CLI: o link do Supabase continua pendente nesta maquina, entao `db:migration-list`,
  `db:lint` e `db:security-check-live` seguem sem rodar. O check ESTATICO de `SECURITY DEFINER`
  passou com 99 migrations (era 98 antes da 380).
- Licao: antes de corrigir uma migration no lugar, confirmar se ela ja foi aplicada em ALGUM
  ambiente — inclusive manualmente pelo Dashboard, que nao aparece em `db:migration-list`. Na duvida,
  migration nova; ela e barata e sempre converge, enquanto a edicao no lugar so e segura no caso em
  que a migration comprovadamente nunca rodou.

Atualizacao 2026-08-20 (Categoria/Subcategoria em filtros e CSV)
-----------------------------------------------------------------
- Filtros novos:
  - `Categoria`: select carregado de `material_categories` ativo no tenant.
  - `Subcategoria`: select dependente da categoria, carregado de `material_subcategories`.
- Listagem:
  - `GET /api/team-stock-operations` aceita `categoryId` e `subcategoryId`.
  - A API aplica o recorte na query de `stock_transfer_items` por join `materials!inner`, usando
    `tenant_id`, `category_id`, `subcategory_id` e `codigo` quando combinados. Isso evita montar
    uma lista grande de IDs de material em `.in("material_id", ...)` para categorias volumosas.
  - O modal de detalhes exibe Categoria/Subcategoria do material.
- Exportacao:
  - Migration `381_team_operations_category_filters_export.sql` republica
    `list_team_operations_export` com `p_category_id` e `p_subcategory_id`.
  - O CSV passa a incluir as colunas `categoria` e `subcategoria` apos `descricao`.
  - A RPC continua `SECURITY DEFINER`, `search_path = public`, com EXECUTE somente para
    `service_role`; o `tenant_id` continua vindo da sessao resolvida na API, nunca do cliente.

Atualizacao 2026-08-21 - Itens sumiam da listagem por teto de 1.000 linhas
--------------------------------------------------------------------------
ACHADO (ver `Auditoria/15-supabase-implicit-row-cap.md`)
- A listagem de historico carregava os itens quebrando a lista de transferencias em lotes
  de 500 (`RELATION_QUERY_CHUNK_SIZE`) e consultando `stock_transfer_items` com
  `.in("stock_transfer_id", lote)`, SEM `.range()` e SEM `.limit()`.
- Chunk de parametro nao e paginacao de resposta: quebrar a lista de ids limita o tamanho
  da CONSULTA, nao o numero de LINHAS que ela devolve. O PostgREST corta em 1.000 por
  resposta e nao sinaliza — status 200 com menos linhas do que o banco produziu.
- Medido em 2026-08-21: 2.987 transferencias, 5.791 itens reais, 5.748 entregues.
  **43 itens sumiam da listagem e da exportacao CSV, sem nenhum aviso na tela.**
- Dos 6 lotes, 1 estourava o teto. A media por lote era 965 linhas contra teto de 1.000:
  a tela estava na BORDA, com folga de 3,5%, e qualquer crescimento de volume levaria os
  outros 5 lotes por cima.

CORRECAO
- `loadRowsInChunks` deixou de ser copia privada desta rota e passou a ser o helper
  compartilhado de `src/lib/server/apiHelpers.ts`, que pagina a RESPOSTA de cada lote ate
  pagina vazia, alem de quebrar os parametros em lotes.
- Os 6 pontos de leitura desta rota foram migrados com ORDEM TOTAL explicita, requisito da
  paginacao por offset. Atencao a `stock_transfer_team_operations`: a PK dela e
  `transfer_id`, nao `id`, entao a ordem usa `transfer_id`.
- O tamanho de lote (500) e o paralelismo (4) foram PRESERVADOS de proposito: a correcao e
  sobre paginacao de resposta, e mudar o lote junto tornaria impossivel atribuir qualquer
  diferenca de comportamento a uma causa.

SEGUNDA CORRECAO NA MESMA ROTA: `loadAllPages`
- A leitura das operacoes usava um segundo helper privado, `loadAllPages`, com a condicao de
  parada que a auditoria classifica como proibida: `pageRows.length < TEAM_OPERATION_PAGE_SIZE`.
- A consulta e um embed com `stock_transfers!inner`. Com join interno, pagina mais curta que a
  pedida e NORMAL, nao excepcional — entao a primeira pagina curta encerrava a leitura e levava
  junto todas as operacoes seguintes.
- Substituido por `loadAllRows`, que para em pagina VAZIA e avanca pelas linhas RECEBIDAS.

RESULTADO VERIFICADO NA TELA — 2026-08-21
- ANTES das correcoes: `Total: 4193` sem filtro.
- DEPOIS: `Total: 5787` (290 paginas). A correcao de `loadAllPages` era o defeito
  PRINCIPAL desta tela: recuperou ~1.594 linhas, contra as 43 da correcao de
  `loadRowsInChunks`.
- Causa: a parada `pageRows.length < TEAM_OPERATION_PAGE_SIZE` sob um embed
  `stock_transfers!inner`. Com join interno, pagina mais curta que a pedida e o caso
  NORMAL — entao a primeira pagina curta encerrava a leitura de operacoes e levava junto
  todos os itens correspondentes.

RESIDUO DE 4 LINHAS — em apuracao, sem impacto conhecido
- Esperado 5.791, exibido 5.787. `npm run db:residual-live` confirmou que a semantica
  exata da rota (filtro de tenant nos dois lados, join interno) da 5.791: nao ha operacao
  orfa nem item de outro tenant. As 4 linhas somem na APLICACAO.
- Causa nao identificada. Descartadas por leitura: filtro em memoria (todos guardados por
  `if (filtro)`), a guarda `if (!transfer || !teamOperation)` (os dois mapas saem do mesmo
  resultado embutido) e item com `tenant_id` nulo (a coluna e `not null`).
- Severidade baixa: 4 linhas em 5.791 (0,07%), sem impacto conhecido em soma ou saldo.
- Para retomar, instrumentar o pipeline da rota e comparar cada estagio com o esperado
  `2.987 / 2.987 / 2.987 / 5.791 / 5.791 / 5.791`. Nao formular nova hipotese por leitura.
